---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - apps/desktop/src-tauri/src/commands/automation.rs
  - crates/automation/src/errors.rs
  - crates/automation/src/executor.rs
  - apps/desktop/src/features/recorder/hud.tsx
  - apps/desktop/src-tauri/tests/record_path_self_heal_false.rs
autonomous: false
user_setup: []
requirements: [PHASE-11.3, PHASE-11.4, PHASE-11.5]
tags: [recording, executor, self-heal, record-path, hud, error-surface]
must_haves:
  truths:
    - "The recording path (commands/automation.rs launch_automation) passes self_heal=false to Executor::run_with_story_path."
    - "A primary-miss during recording produces an AutomationError::PrimaryMissNoHeal { step_ordinal, step_id, verb } variant; the recorder never calls try_promote_fallback on the record path."
    - "The recording run does NOT mutate .story.targets.json: pre-run and post-run mtimes are byte-equal in the regression test."
    - "The HUD surfaces the D-06 error copy verbatim (UI-SPEC §Record-path primary-miss) with an 'Open in Simulator' action link."
    - "Any record-time call to picker_stamp_step_id is removed (grep for picker_stamp_step_id call sites in record-flow modules returns zero hits)."
  artifacts:
    - path: apps/desktop/src-tauri/src/commands/automation.rs
      provides: "Record path launches Executor with self_heal=false and converts PrimaryMissNoHeal into a StepFailed event carrying the D-06 copy"
      contains: "self_heal"
    - path: crates/automation/src/errors.rs
      provides: "new AutomationError::PrimaryMissNoHeal variant"
    - path: apps/desktop/src/features/recorder/hud.tsx
      provides: "Destructive HUD block with 'Open in Simulator' link per UI-SPEC §5"
    - path: apps/desktop/src-tauri/tests/record_path_self_heal_false.rs
      provides: "Integration test: primary-miss on record path yields PrimaryMissNoHeal, targets.json mtime unchanged"
  key_links:
    - from: "apps/desktop/src-tauri/src/commands/automation.rs launch_automation"
      to: "automation::run_with_story_path"
      via: "self_heal: false argument at the Executor::run call site"
      pattern: "self_heal.*false"
    - from: "crates/automation/src/executor.rs run_command"
      to: "AutomationError::PrimaryMissNoHeal"
      via: "self_heal=false gate, raised on wait_actionable timeout"
      pattern: "PrimaryMissNoHeal"
    - from: "apps/desktop/src/features/recorder/hud.tsx"
      to: "StepFailed event with D-06 verbatim copy"
      via: "error_message rendering + Open-in-Simulator action"
      pattern: "Open in Simulator"
---

<objective>
Flip the record path to read-only semantics per D-06 and D-07. This plan depends on Phase 10-01's executor parameterization (which ships `self_heal: bool` through `run_with_story_path`) — if that signature is not yet in the tree at execution time, the task checklist pauses at a human-verify checkpoint.

Purpose: Recording runs today silently self-heal against `.story.targets.json`. After Phase 11 that is forbidden; primary-miss must surface as an actionable error directing the author into Simulator. This is the core D-06/D-07 guarantee.

Output:
- Record-path `Executor::run_with_story_path` call rewritten with `self_heal: false`.
- `AutomationError::PrimaryMissNoHeal` variant + executor-side raise site.
- HUD error block with the locked UI-SPEC copy + "Open in Simulator" action link that opens the editor on the failed step.
- Integration test that proves `.story.targets.json` is not touched on record-path primary-miss.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-author-time-element-picker-relocate-pick-to-preview-panel-ro/11-CONTEXT.md
@.planning/phases/11-author-time-element-picker-relocate-pick-to-preview-panel-ro/11-RESEARCH.md
@.planning/phases/11-author-time-element-picker-relocate-pick-to-preview-panel-ro/11-PATTERNS.md
@.planning/phases/11-author-time-element-picker-relocate-pick-to-preview-panel-ro/11-UI-SPEC.md
@.planning/phases/10-author-time-simulator-step-preview-dry-run-walkthrough/10-01-PLAN.md
@apps/desktop/src-tauri/src/commands/automation.rs
@apps/desktop/src/features/recorder/hud.tsx

<interfaces>
<!-- From 10-01 (assumed shipped by the time this plan executes): -->
```rust
pub async fn run_with_story_path(
    story, story_path, primary, fallback, persistence, screenshot_dir, launch_opts, control,
    stop_after_ordinal: Option<u32>, capture_frames: bool, frame_dir: Option<PathBuf>,
    self_heal: bool, tx: mpsc::Sender<ExecutorEvent>,
) -> Result<()>;
```

<!-- New variant to add (this plan) in crates/automation/src/errors.rs -->
```rust
pub enum AutomationError {
    // ... existing variants ...
    PrimaryMissNoHeal { step_ordinal: u32, step_id: Option<uuid::Uuid>, verb: String },
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Confirm Phase 10-01 executor signature is merged</name>
  <what-built>Prerequisite check — `Executor::run_with_story_path` with the `self_heal: bool` parameter must exist in `crates/automation/src/executor.rs`.</what-built>
  <how-to-verify>
    Operator runs: `grep -n "self_heal" crates/automation/src/executor.rs` and expects at least one hit in the `run_with_story_path` (or `run_story`) public signature. If zero hits: 10-01 is not merged; pause and hand back to orchestrator.
  </how-to-verify>
  <resume-signal>Reply "merged" (proceed to Task 1) or "not merged" (abort plan).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add AutomationError::PrimaryMissNoHeal + raise on record-path primary miss</name>
  <files>
    crates/automation/src/errors.rs (EDIT),
    crates/automation/src/executor.rs (EDIT — raise site under self_heal=false gate)
  </files>
  <read_first>
    - crates/automation/src/errors.rs (entirety — existing AutomationError variants)
    - crates/automation/src/executor.rs (run_command / wait_actionable_or_heal — where 10-01 Task 2 introduced the self_heal gate)
    - .planning/phases/11-.../11-RESEARCH.md §Pattern 3 + §Code Example 3 (error-shape template)
    - .planning/phases/11-.../11-UI-SPEC.md §Record-path primary-miss (locked verbatim copy)
  </read_first>
  <action>
    In `crates/automation/src/errors.rs`, add:
    ```rust
    #[error("Step {step_ordinal}: \"{verb}\" could not match any element. Self-healing is disabled during recording. Open this story in Simulator, use \"Promote to fallback\" on step {step_ordinal}, then try again.")]
    PrimaryMissNoHeal {
        step_ordinal: u32,
        step_id: Option<uuid::Uuid>,
        verb: String,
    },
    ```
    NOTE: the `Display` string is the UI-SPEC-locked copy verbatim (double-check against §Record-path primary-miss lines 172-173 of 11-UI-SPEC.md). No paraphrasing. Add to `Serialize`/`Deserialize`/`specta::Type` derives consistent with sibling variants.

    In `crates/automation/src/executor.rs`, locate the `self_heal=false` gate introduced by 10-01 Task 2 (search for `self_heal`). When the gate fires on `wait_actionable` timeout, return:
    ```rust
    return Err((
        AutomationError::PrimaryMissNoHeal {
            step_ordinal: ordinal,
            step_id: cmd.step_id(),
            verb: command_verb_label(cmd),   // existing helper used for other error messages
        },
        attempts,
    ));
    ```
    Reuse the existing `command_verb_label(cmd)` / equivalent helper that formats `click "Save"` for other error messages — do not duplicate the formatter.

    If 10-01's gate does not yet emit a typed error and instead emits a generic `AutomationError::SelectorFailed` with a `self_heal_disabled` flag, convert in-place — the variant shape above is authoritative for Phase 11.

    Do not change any behavior for `self_heal=true` code paths (those remain for the simulator/legacy paths as appropriate).
  </action>
  <verify>
    <automated>cargo test -p automation --lib 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    `cargo test -p automation --lib` passes. `grep "PrimaryMissNoHeal" crates/automation/src/ -r | wc -l` >= 2 (variant decl + raise site).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip record path to self_heal=false + integration test</name>
  <files>
    apps/desktop/src-tauri/src/commands/automation.rs (EDIT — line ~328 Executor call site),
    apps/desktop/src-tauri/tests/record_path_self_heal_false.rs (NEW)
  </files>
  <read_first>
    - apps/desktop/src-tauri/src/commands/automation.rs lines 300-360 (current Executor::run call)
    - .planning/phases/11-.../11-RESEARCH.md §Code Example 3
    - crates/automation/src/executor.rs (Phase 10-01-updated signature)
  </read_first>
  <behavior>
    - Test 1 (self_heal gate): build a fake story with one step whose primary will miss (e.g., role=button name="Nonexistent"). Launch the record path via `launch_automation`. Consume events until `StepFailed` fires. Assert: (a) the emitted error is `AutomationError::PrimaryMissNoHeal`, (b) the error Display string matches the UI-SPEC copy verbatim (sprintf with ordinal+verb), (c) `.story.targets.json` file (if pre-existing from a seed) has byte-identical contents + unchanged mtime after the run completes.
    - Test 2 (self_heal never true on record path): with the same fixture, assert `try_promote_fallback` is NEVER called during the run. Since `try_promote_fallback` is a pub fn, this is verifiable by wrapping it with a test-only counter OR by confirming no targets.json write occurred and no `ExecutorEvent::FallbackPromoted` (or equivalent) event fires.
  </behavior>
  <action>
    Edit the Executor call site in `launch_automation` (automation.rs ~line 328). Current code:
    ```rust
    let mut events = Executor::run(story, primary, fallback, persistence, screenshot_dir, launch_opts, Some(control.clone()));
    ```

    Replace with the Phase 10-01 signature:
    ```rust
    let mut events = Executor::run_with_story_path(
        story,
        Some(story_path.clone()),
        primary,
        fallback,
        Some(project_db),
        screenshot_dir,
        launch_opts,
        Some(control.clone()),
        /* stop_after_ordinal */ None,
        /* capture_frames    */ false,
        /* frame_dir         */ None,
        /* self_heal         */ false,   // D-06 — record path read-only
        tx,
    );
    ```
    The exact parameter list must match 10-01's merged signature — cross-check against `crates/automation/src/executor.rs`. Use inline parameter-name comments for the boolean flags only (concise-comment rule).

    Also audit this file for any `picker_stamp_step_id` invocation (grep within file). If present (it should NOT be, since picker stamping is called from the renderer after a pick, not from record-flow), remove — record path must not stamp.

    Create `apps/desktop/src-tauri/tests/record_path_self_heal_false.rs`. The test launches a real executor against a headless driver fixture (same pattern 10-01 tests should establish). If that harness is not yet usable, downgrade to `#[tokio::test]` that calls `run_with_story_path` directly with a NoopDriver pair that always returns ElementNotFound, captures events, and asserts `PrimaryMissNoHeal`. Model the fixture after `crates/automation/tests/*.rs` structure.

    Seed a `.story.targets.json` with a single stale target, read bytes + mtime, run, read bytes + mtime again — assert equality on both.
  </action>
  <verify>
    <automated>cargo test -p storycapture-desktop --test record_path_self_heal_false 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    Test passes. `grep "self_heal:.*false" apps/desktop/src-tauri/src/commands/automation.rs | wc -l` >= 1. `grep "picker_stamp_step_id" apps/desktop/src-tauri/src/commands/automation.rs | wc -l` = 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: HUD Record-path error block with Open-in-Simulator action</name>
  <files>
    apps/desktop/src/features/recorder/hud.tsx (EDIT)
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/hud.tsx (entirety — existing StepFailed event rendering)
    - .planning/phases/11-.../11-UI-SPEC.md §5 Record-path primary-miss HUD (verbatim copy + layout)
    - apps/desktop/src/ipc/simulator.ts (if available — to learn how to open the editor on a step ordinal; if not, invoke a generic "open editor" route the Editor page already supports)
  </read_first>
  <action>
    In `hud.tsx`, when receiving a `StepFailed` event whose error string matches the `PrimaryMissNoHeal` Display pattern (or carries a typed discriminant — prefer typed), render the specific HUD block specified by UI-SPEC §5:

    - Container: `rounded-[var(--radius-md)] border-l-4 border-[var(--color-danger)] bg-[var(--color-surface-200)] px-3 py-2 flex gap-3`
    - Left icon: `lucide-react` `AlertTriangle` 16px, `--color-danger`
    - Heading line (13px/500): `Step {N}: ` + inline mono-pill of the verb excerpt (`click "Save"`)
    - Body (12px/400, `--color-fg-muted`): the second sentence verbatim from UI-SPEC: `Self-healing is disabled during recording. Open this story in Simulator, use "Promote to fallback" on step {N}, then try again.`
    - Right-aligned action: `<button>` with label `Open in Simulator →`, styled 12px/500 `--color-accent-primary` underline-on-hover, `aria-label="Open story in Simulator on step {N}"`.
    - Action onClick: dispatch whatever route/store action the Editor page already uses to open the current story on a specific step (search for an existing "open on step" primitive in `apps/desktop/src/features/editor/*` — do NOT auto-start the simulator; UI-SPEC says user decides). If none exists, fire a single `openStoryOnStep({ ordinal })` action on the editor store; leave the simulator start to the user.
    - Also emit a Sonner destructive toast with the same copy, 12 s auto-dismiss, `action` slot labelled `Open in Simulator` with the same onClick.

    Copy MUST be verbatim UI-SPEC lines 172-173 — no paraphrasing. Use a single source-of-truth constant (e.g., `RECORD_PATH_MISS_BODY = "Self-healing is disabled during recording. ..."`) to avoid drift between HUD block and toast.

    Accessibility: HUD block `tabIndex={0}` with `role="region" aria-labelledby`; the action `<button>` must be reachable via Tab and `Enter`.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    `pnpm --filter @storycapture/desktop typecheck` passes. `grep -c "Open in Simulator" apps/desktop/src/features/recorder/hud.tsx` >= 2 (button label + toast action). The copy literal `"Self-healing is disabled during recording"` appears exactly once (source-of-truth constant).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor record-path -> filesystem (.story.targets.json) | Previously wrote on self-heal; Phase 11 forbids this. |
| StepFailed event -> HUD renderer | Error payload is trusted (originates in Rust), but the `step_ordinal` + `verb` feeds into a click action that opens the editor on an ordinal — validate range on the renderer side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | record path silently mutating `.story.targets.json` on primary miss | mitigate | `self_heal: false` at Executor call site + integration test asserting targets.json byte/mtime invariance (Task 2). |
| T-11-02-02 | Information Disclosure | Verb excerpt in HUD could leak sensitive strings if `type` verb uses raw `"password"` arg | accept | Existing `command_verb_label` helper is already reused across other error paths with the same disclosure profile; no new surface introduced by Phase 11. |
| T-11-02-03 | Repudiation | "Open in Simulator" action navigates on untrusted ordinal | mitigate | Clamp `ordinal` to `[1, story.steps.len()]` on the renderer before dispatching the open-editor action; reject invalid with a toast. |

No ASVS V2/V3/V6 additions. V5 (Input Validation) is touched by ordinal clamping.
</threat_model>

<verification>
- `cargo test -p automation --lib` passes (Task 1).
- `cargo test -p storycapture-desktop --test record_path_self_heal_false` passes (Task 2).
- `pnpm --filter @storycapture/desktop typecheck` passes (Task 3).
- `grep -c "self_heal" apps/desktop/src-tauri/src/commands/automation.rs` >= 1.
- `grep -c "picker_stamp_step_id" apps/desktop/src-tauri/src/commands/automation.rs` = 0.
- `grep -c "Open in Simulator" apps/desktop/src/features/recorder/hud.tsx` >= 2.
</verification>

<success_criteria>
- Record runs provably do not modify `.story.targets.json`.
- Primary-miss surfaces UI-SPEC-locked copy, no paraphrase.
- `try_promote_fallback` is unreachable from the record path.
- HUD offers a one-click route into the Editor on the failed step (no auto-simulator-start).
</success_criteria>

<output>
After completion, create `.planning/phases/11-author-time-element-picker-relocate-pick-to-preview-panel-ro/11-02-SUMMARY.md` documenting: the merged Executor signature used, the exact error variant shape, and confirmation that the targets.json mtime invariance test is green.
</output>
