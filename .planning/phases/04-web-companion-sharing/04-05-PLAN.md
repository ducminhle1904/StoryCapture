---
phase: 04-web-companion-sharing
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/web/src/app/watch/[slug]/page.tsx
  - apps/web/src/app/embed/[id]/page.tsx
  - apps/web/src/app/api/oembed/route.ts
  - apps/web/src/components/video-player.tsx
  - apps/web/src/components/chapter-nav.tsx
  - apps/web/src/components/embed-code.tsx
  - apps/web/src/components/privacy-toggle.tsx
  - apps/web/src/app/(dashboard)/videos/[videoId]/page.tsx
autonomous: true
requirements:
  - WEB-04

must_haves:
  truths:
    - "User can view a video at /watch/<slug> with chapter navigation derived from DSL scene boundaries"
    - "Private videos (default) have noindex meta tag and are unlisted (per D-02)"
    - "Owner can toggle video between private and public (per D-02)"
    - "Owner can edit the vanity slug (per D-02)"
    - "Embed code (iframe snippet) is copyable from the video detail page (per D-03)"
    - "oEmbed endpoint at /api/oembed returns rich type response for Notion/Slack/Discord unfurl (per D-03)"
    - "Thumbnail from R2 is displayed as poster image and in oEmbed response (per D-03)"
  artifacts:
    - path: "apps/web/src/app/watch/[slug]/page.tsx"
      provides: "Public viewer page"
    - path: "apps/web/src/app/embed/[id]/page.tsx"
      provides: "Minimal embeddable viewer (no chrome)"
    - path: "apps/web/src/app/api/oembed/route.ts"
      provides: "oEmbed endpoint for auto-unfurl"
    - path: "apps/web/src/components/video-player.tsx"
      provides: "Video player with chapter support"
    - path: "apps/web/src/components/chapter-nav.tsx"
      provides: "Chapter navigation from DSL scenes"
  key_links:
    - from: "apps/web/src/app/watch/[slug]/page.tsx"
      to: "apps/web/src/trpc/routers/video.ts"
      via: "video.getBySlug query"
      pattern: "getBySlug"
    - from: "apps/web/src/app/api/oembed/route.ts"
      to: "apps/web/src/lib/prisma.ts"
      via: "direct Prisma query for video by slug"
      pattern: "prisma.video.findUnique"
---

<objective>
Build shareable viewer pages (/watch/<slug>), embeddable iframe (/embed/<id>), oEmbed endpoint, and video management controls (privacy toggle, slug editor, embed code).

Purpose: The viewer page is how recordings reach their audience. Per D-02, videos are private-by-default (unlisted, noindex). Per D-03, iframe embeds + oEmbed enable integration with Notion, Slack, and Discord.
Output: Working viewer page with chapter navigation, embed endpoint, oEmbed auto-unfurl, privacy controls, and slug management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-companion-sharing/04-CONTEXT.md
@.planning/phases/04-web-companion-sharing/04-RESEARCH.md
@.planning/phases/04-web-companion-sharing/04-01-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-02-SUMMARY.md

<interfaces>
From apps/web/src/trpc/routers/video.ts (Plan 04-04):
```typescript
// video.getBySlug returns:
{ id, slug, projectName, sceneBoundaries, videoUrl, thumbnailUrl, isPublic }
```

From apps/web/prisma/schema.prisma:
```prisma
model Video {
  slug String @unique
  isPublic Boolean @default(false) // D-02: private by default
  sceneBoundaries Json // [{sceneIndex, label, startTimeSec}]
  thumbnailR2Key String?
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viewer page + embed page + video player with chapters</name>
  <files>
    apps/web/src/app/watch/[slug]/page.tsx
    apps/web/src/app/embed/[id]/page.tsx
    apps/web/src/components/video-player.tsx
    apps/web/src/components/chapter-nav.tsx
  </files>
  <action>
    1. Create components/video-player.tsx:
       - HTML5 `<video>` element with native controls
       - Accept props: `src: string`, `poster: string` (thumbnail), `chapters: { label: string, startTimeSec: number }[]`
       - Poster image from thumbnail R2 URL
       - Responsive: 16:9 aspect ratio, max-width 100%
       - On play/pause/seek/ended events, dispatch analytics events (POST to /api/analytics/ingest — wired in Plan 04-08; for now, export a callback prop `onAnalyticsEvent`)
       - Dark background matching the app theme

    2. Create components/chapter-nav.tsx:
       - Horizontal pill/tab navigation showing scene labels from DSL scene boundaries
       - Clicking a chapter seeks the video to that timestamp
       - Active chapter highlighted based on current playback time
       - Accept props: `chapters`, `currentTime`, `onSeek: (timeSec: number) => void`
       - Scrollable horizontally if many chapters

    3. Create app/watch/[slug]/page.tsx (Server Component):
       - Fetch video by slug via Prisma (server-side, no tRPC needed for public page)
       - If video not found or status !== READY: return 404
       - Generate metadata:
         - If video.isPublic: normal SEO meta tags (title, description, og:image from thumbnail)
         - If NOT video.isPublic (D-02): add `<meta name="robots" content="noindex, nofollow" />`, still render the page (unlisted = accessible via link, not indexed)
       - Render VideoPlayer + ChapterNav
       - Video src: if public, use R2 public URL (cdn.storycapture.app). If private, generate presigned GET URL (server-side)
       - Show project name as title, "Made with StoryCapture" footer link
       - Pass chapters from video.sceneBoundaries

    4. Create app/embed/[id]/page.tsx:
       - Minimal chrome version of the viewer (no header, no footer, no chapter nav — just the video player)
       - Used inside iframes (per D-03)
       - Same video fetch logic as watch page but by ID instead of slug
       - Set `X-Frame-Options: ALLOWALL` or omit it (allow embedding)
       - Dark background, no padding
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - /watch/<slug> renders video player with chapter navigation
    - Private videos have noindex meta tag (D-02)
    - /embed/<id> renders minimal player for iframe embedding (D-03)
    - Chapter navigation syncs with video playback position
    - Thumbnail displayed as poster image
  </done>
</task>

<task type="auto">
  <name>Task 2: oEmbed endpoint + privacy toggle + slug editor + embed code UI</name>
  <files>
    apps/web/src/app/api/oembed/route.ts
    apps/web/src/components/embed-code.tsx
    apps/web/src/components/privacy-toggle.tsx
    apps/web/src/app/(dashboard)/videos/[videoId]/page.tsx
    apps/web/src/trpc/routers/video.ts
  </files>
  <action>
    1. Create app/api/oembed/route.ts per RESEARCH.md oEmbed example:
       - GET endpoint accepting `url`, `format` (json only in v1), `maxwidth`, `maxheight` query params
       - Extract slug from URL pattern `/watch/<slug>`
       - Only return oEmbed for PUBLIC videos (isPublic: true) — private videos return 404
       - Return oEmbed `rich` type response with iframe HTML pointing to /embed/<id>
       - Include thumbnail_url, provider_name: "StoryCapture", provider_url
       - Aspect-ratio-aware sizing (16:9)
       - Follow oEmbed spec exactly (version: "1.0", type: "rich")

    2. Add tRPC procedures to video.ts:
       `updatePrivacy` (protectedProcedure, mutation):
       - Input: `{ videoId, isPublic: boolean }`
       - Verify user is owner/editor of the video's workspace
       - Update video.isPublic
       - Per D-02: toggling to public makes it SEO-indexed; toggling to private adds noindex

       `updateSlug` (protectedProcedure, mutation):
       - Input: `{ videoId, newSlug: string }`
       - Validate slug format (lowercase alphanumeric + hyphens, 3-60 chars)
       - Check uniqueness
       - Update video.slug
       - Per D-02: defaults to slugified project name, owner can edit

    3. Create components/embed-code.tsx:
       - Displays the iframe embed snippet per D-03: `<iframe src="storycapture.app/embed/<id>" width="..." height="..." allowfullscreen>`
       - Copy-to-clipboard button
       - Width/height selector (presets: 640x360, 960x540, 1280x720)
       - Show oEmbed discovery URL for reference

    4. Create components/privacy-toggle.tsx:
       - Toggle switch with labels: "Private (unlisted)" / "Public (searchable)"
       - Explanation text: private = only people with the link; public = indexed by search engines
       - Calls updatePrivacy mutation on change
       - Per D-02: no password protection, no link expiry in v1

    5. Create app/(dashboard)/videos/[videoId]/page.tsx:
       - Video detail/management page (auth-gated via dashboard layout)
       - Shows: video player preview, privacy toggle, slug editor, embed code section
       - Slug editor: editable text input with "Save" button, shows full URL preview (`storycapture.app/watch/<slug>`)
       - Link to analytics page (wired in Plan 04-08)
       - Delete video button with confirmation modal
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - /api/oembed returns valid oEmbed JSON for public videos
    - Privacy toggle switches between private (noindex) and public (indexed) per D-02
    - Slug editor validates format and uniqueness per D-02
    - Embed code section shows copyable iframe snippet per D-03
    - Video detail page shows all management controls
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> /watch/<slug> | Anyone with the URL can view unlisted videos |
| Public internet -> /api/oembed | oEmbed only returns data for public videos |
| Authenticated user -> video management | Only workspace editors/owners can modify privacy/slug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | I (Info Disclosure) | unlisted videos | accept | Per D-02: unlisted = accessible via direct link (by design). No password protection in v1. |
| T-04-18 | T (Tampering) | slug squatting | mitigate | Slug uniqueness enforced at DB level (unique constraint); slug format validated (alphanumeric + hyphens) |
| T-04-19 | E (Elevation) | video management | mitigate | updatePrivacy and updateSlug check workspace membership + editor/owner role |
| T-04-20 | I (Info Disclosure) | oEmbed for private | mitigate | oEmbed endpoint only returns data for isPublic: true videos; private videos return 404 |
</threat_model>

<verification>
- TypeScript compiles
- /watch/test-slug route renders (with mock data or seed)
- /api/oembed returns valid oEmbed JSON
- Privacy toggle and slug editor components render correctly
</verification>

<success_criteria>
Viewer page renders videos with chapter navigation. Private videos have noindex. Public videos have oEmbed auto-unfurl. iframe embed works. Owner can toggle privacy, edit slug, and copy embed code.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-companion-sharing/04-05-SUMMARY.md`
</output>
