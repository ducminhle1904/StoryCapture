---
phase: 04-web-companion-sharing
plan: 10
type: execute
wave: 6
depends_on: ["04-04", "04-05", "04-06", "04-07", "04-08", "04-09"]
files_modified:
  - apps/web/src/app/(dashboard)/page.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/next.config.ts
autonomous: false
requirements:
  - DIST-06
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-04
  - WEB-05
  - WEB-06
  - WEB-07
  - WEB-08

must_haves:
  truths:
    - "Web app boots and serves all routes without errors"
    - "Auth flow works (sign-in, sign-out, session persistence)"
    - "Dashboard shows videos, workspace switcher, and navigation to all features"
    - "All tRPC routers respond correctly to queries and mutations"
    - "TypeScript compiles without errors across the entire web app"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/page.tsx"
      provides: "Polished dashboard with navigation to all features"
  key_links:
    - from: "apps/web/src/app/(dashboard)/page.tsx"
      to: "All feature routes"
      via: "Navigation links"
      pattern: "workspace|templates|analytics|sync|videos"
---

<objective>
Final integration pass: connect all dashboard navigation, polish landing page, verify typecheck across the full web app, and human-verify the complete web companion.

Purpose: After all feature plans (04-01 through 04-09) are complete, this plan ties everything together into a cohesive web app with proper navigation, consistent styling, and a human-verify checkpoint for the complete flow.
Output: Polished, navigable web companion ready for deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-companion-sharing/04-CONTEXT.md
@.planning/phases/04-web-companion-sharing/04-01-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-02-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-03-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-04-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-05-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-06-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-07-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-08-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard integration + landing page + global navigation + final typecheck</name>
  <files>
    apps/web/src/app/(dashboard)/page.tsx
    apps/web/src/app/layout.tsx
    apps/web/src/app/page.tsx
    apps/web/next.config.ts
  </files>
  <action>
    1. Polish app/(dashboard)/page.tsx:
       - Workspace switcher in header (from Plan 04-06)
       - Video grid as primary content (from Plan 04-04/04-06)
       - Sidebar or top navigation with links to:
         - Dashboard (home) -- video grid
         - Templates -- /templates
         - Sync -- /sync (live recording status)
       - Per-video quick actions: View (/watch/slug), Analytics (/analytics/videoId), Edit (/videos/videoId)
       - User avatar + dropdown with: Profile, Sign Out

    2. Polish app/page.tsx (landing page):
       - Simple marketing page for unauthenticated visitors:
         - Hero: "Turn stories into shareable demo videos"
         - 3 feature cards: Record, Polish, Share
         - CTA: "Get Started" button -> /sign-in
         - Footer: "Made with StoryCapture" + links
       - Styled with Tailwind dark theme
       - If user is already authenticated, redirect to /dashboard

    3. Verify next.config.ts is production-ready:
       - Standalone output for Vercel
       - Proper transpilePackages for monorepo packages
       - Security headers (X-Frame-Options for non-embed pages, CSP, etc.)
       - Image domain configuration for R2 public URL

    4. Run full typecheck and fix any cross-plan integration issues:
       - `pnpm --filter @storycapture/web typecheck`
       - Verify all tRPC routers are merged in _app.ts
       - Verify all routes are accessible
       - Fix any import/type issues from plan-to-plan boundaries

    5. Run desktop typecheck to verify Plan 04-03/04-04/04-09 desktop changes integrate:
       - `pnpm --filter @storycapture/desktop typecheck`
       - `cd apps/desktop/src-tauri && cargo check`
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture && pnpm --filter @storycapture/web typecheck && pnpm --filter @storycapture/desktop typecheck && cd apps/desktop/src-tauri && cargo check</automated>
  </verify>
  <done>
    - All TypeScript compiles (web + desktop)
    - All Rust compiles (desktop)
    - Dashboard has full navigation to all features
    - Landing page renders for unauthenticated visitors
    - All tRPC routers merged and responding
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete web companion</name>
  <action>Human verifies the complete Phase 4 web companion by running through the verification checklist below. No automated implementation -- this is a verification gate.</action>
  <what-built>Complete Phase 4 Web Companion: OAuth auth, video upload, shareable viewer pages with embed/oEmbed, team workspaces with RBAC, template marketplace, analytics dashboard, and desktop-web sync</what-built>
  <how-to-verify>
    1. Start the web app: `cd apps/web && pnpm dev` -- verify it boots at http://localhost:3000

    2. Auth flow (WEB-02):
       - Visit http://localhost:3000 -- see landing page
       - Click "Get Started" -> redirected to sign-in
       - (If OAuth credentials configured) Sign in with GitHub or Google
       - Verify redirect to /dashboard
       - Verify personal workspace auto-created

    3. Dashboard navigation:
       - Workspace switcher visible in header
       - Navigation links to Templates, Sync pages work
       - Sign-out works and redirects to landing page

    4. Templates (WEB-06):
       - Visit /templates -- see category grid with 9 categories
       - Template cards show name, description, category badge
       - (If DB seeded) Click "Use Template" -- see fork dialog with .story content

    5. Desktop accounts (UI-06):
       - Start desktop app: `pnpm --filter @storycapture/desktop tauri dev`
       - Navigate to Settings -> Accounts tab
       - Verify "Connect Web Account" button visible
       - (Full OAuth flow requires deployed web companion)

    6. Visual check:
       - Dark theme consistent across all pages
       - Responsive layout (resize browser window)
       - No console errors
       - All pages render without blank screens

    Note: Full upload, viewer, analytics, and sync flows require deployed infrastructure (R2, Neon, Vercel) and cannot be fully verified in local dev. This checkpoint verifies the code compiles, routes render, and UI is cohesive.
  </how-to-verify>
  <verify>Human inspection of running application</verify>
  <done>Human has verified web companion boots, navigates, and renders correctly</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet -> landing page | Unauthenticated public access |
| Authenticated user -> dashboard | Session-gated access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-34 | I (Info Disclosure) | security headers | mitigate | X-Frame-Options DENY on non-embed pages; CSP configured; HTTPS enforced on Vercel |
| T-04-35 | S (Spoofing) | session fixation | mitigate | NextAuth handles session rotation on sign-in automatically |
</threat_model>

<verification>
- Full typecheck passes (web + desktop)
- Rust check passes
- Web app boots and serves landing page
- Human verifies the complete flow
</verification>

<success_criteria>
Web companion is fully integrated: all routes accessible, navigation consistent, dark theme applied, TypeScript and Rust compile without errors. Human has verified the UI is cohesive and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-companion-sharing/04-10-SUMMARY.md`
</output>
