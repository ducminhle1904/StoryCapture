---
phase: 04-web-companion-sharing
plan: 07
type: execute
wave: 4
depends_on: ["04-06"]
files_modified:
  - apps/web/src/trpc/routers/template.ts
  - apps/web/src/trpc/routers/_app.ts
  - apps/web/src/app/(dashboard)/templates/page.tsx
  - apps/web/src/components/template-card.tsx
  - apps/web/src/components/template-grid.tsx
  - apps/web/prisma/seed.ts
  - apps/web/package.json
autonomous: true
requirements:
  - WEB-06

must_haves:
  truths:
    - "User can browse templates organized in a category grid (9 categories per D-05)"
    - "10-15 seed templates exist across all 9 categories"
    - "User can fork a template into their workspace (deep copy of story source per D-05)"
    - "Fork count is tracked and displayed per template"
    - "No search functionality (per D-05 -- deferred)"
    - "No community submissions (per D-05 -- deferred)"
  artifacts:
    - path: "apps/web/src/trpc/routers/template.ts"
      provides: "Template list + fork procedures"
      exports: ["templateRouter"]
    - path: "apps/web/src/app/(dashboard)/templates/page.tsx"
      provides: "Template marketplace page"
    - path: "apps/web/prisma/seed.ts"
      provides: "Seed script with 10-15 curated templates"
  key_links:
    - from: "apps/web/src/app/(dashboard)/templates/page.tsx"
      to: "apps/web/src/trpc/routers/template.ts"
      via: "template.listByCategory query"
      pattern: "listByCategory"
---

<objective>
Build the template marketplace with curated seed data, category grid browse, and fork-to-workspace functionality.

Purpose: Templates lower the barrier to entry for new users by providing starting points for common demo types. Per D-05, v1 is a curated seed set only (10-15 templates, 9 categories, fork = deep copy, no search, no community submissions).
Output: Working template marketplace page with seed data and fork functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-web-companion-sharing/04-CONTEXT.md
@.planning/phases/04-web-companion-sharing/04-RESEARCH.md
@.planning/phases/04-web-companion-sharing/04-01-SUMMARY.md
@.planning/phases/04-web-companion-sharing/04-06-SUMMARY.md

<interfaces>
From apps/web/prisma/schema.prisma (Plan 04-01):
```prisma
model Template {
  id, name, description, category (TemplateCategory enum), storySource,
  thumbnailUrl, workspaceId (null = system), forkCount
}
enum TemplateCategory {
  SAAS_ONBOARDING, ECOMMERCE_CHECKOUT, API_WALKTHROUGH,
  MOBILE_DEMO, CLI_TOOL, LANDING_PAGE,
  FEATURE_ANNOUNCEMENT, BUG_REPRODUCTION, INTERNAL_TRAINING
}
```

From apps/web/src/trpc/routers/workspace.ts (Plan 04-06):
```typescript
// workspaceMemberProcedure — validates workspace membership
// workspaceEditorProcedure — requires editor or owner role
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template tRPC router + seed script</name>
  <files>
    apps/web/src/trpc/routers/template.ts
    apps/web/src/trpc/routers/_app.ts
    apps/web/prisma/seed.ts
    apps/web/package.json
  </files>
  <action>
    1. Create trpc/routers/template.ts:

       `listByCategory` (publicProcedure, query):
       - Input: `{ category?: TemplateCategory }` (optional filter)
       - Return templates grouped by category if no filter, or filtered list
       - Include: id, name, description, category, forkCount, thumbnailUrl
       - Order by forkCount desc within each category

       `getById` (publicProcedure, query):
       - Input: `{ id }`
       - Return full template details including storySource preview (first 500 chars)

       `fork` (protectedProcedure, mutation):
       - Input: `{ templateId, workspaceId }` (defaults to personal workspace)
       - Per D-05: fork = deep copy of storySource into user's workspace
       - Verify user has EDITOR or OWNER role in target workspace
       - Create a new "project" entry (or store as a new Video draft with status DRAFT — planner discretion: since there is no Project model in the web schema, create the forked story as a downloadable text response or add a ForkedProject model. Simplest approach: return the storySource content for the user to paste into their desktop editor. Add a helper tRPC procedure or return value.)
       - Actually: the cleanest approach for v1 is to return the storySource as a downloadable `.story` file. The user saves it in their desktop project folder. No new model needed.
       - Increment forkCount on the source template
       - Return `{ storySource, fileName: template.name + '.story' }`

    2. Merge templateRouter into _app.ts

    3. Create prisma/seed.ts with 12 curated templates (at least 1 per category, some categories get 2):
       Each template needs:
       - `name`: descriptive title (e.g., "SaaS Onboarding Flow", "E-Commerce Checkout Demo")
       - `description`: 1-2 sentence description
       - `category`: matching enum value
       - `storySource`: realistic `.story` DSL content demonstrating the category's use case (5-15 lines of DSL, using real verbs: navigate, click, type, wait, assert, etc.)
       - `workspaceId`: null (system template)
       - `forkCount`: 0

       Template distribution:
       - SAAS_ONBOARDING: 2 (signup flow + feature tour)
       - ECOMMERCE_CHECKOUT: 1 (add to cart + checkout)
       - API_WALKTHROUGH: 1 (Swagger/Postman demo)
       - MOBILE_DEMO: 1 (responsive viewport demo)
       - CLI_TOOL: 1 (terminal recording concept)
       - LANDING_PAGE: 2 (hero section + pricing page)
       - FEATURE_ANNOUNCEMENT: 1 (new feature walkthrough)
       - BUG_REPRODUCTION: 2 (form validation bug + navigation bug)
       - INTERNAL_TRAINING: 1 (CRM data entry training)

    4. Add `prisma.seed` config to package.json:
       ```json
       "prisma": { "seed": "npx tsx prisma/seed.ts" }
       ```
       Ensure `tsx` is in devDependencies.
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx tsc --noEmit && npx tsx prisma/seed.ts --help 2>&1 | head -5</automated>
  </verify>
  <done>
    - Template router has listByCategory, getById, fork procedures
    - Seed script contains 12 templates across all 9 categories
    - Fork returns storySource as downloadable content
    - forkCount incremented on fork
    - Seed script is runnable via `npx prisma db seed`
  </done>
</task>

<task type="auto">
  <name>Task 2: Template marketplace UI</name>
  <files>
    apps/web/src/app/(dashboard)/templates/page.tsx
    apps/web/src/components/template-card.tsx
    apps/web/src/components/template-grid.tsx
  </files>
  <action>
    1. Create components/template-card.tsx:
       - Card showing: template name, description (truncated), category badge, fork count
       - Thumbnail placeholder (gradient based on category color)
       - "Use Template" button
       - Hover state with subtle scale/shadow animation
       - Dark theme consistent with dashboard

    2. Create components/template-grid.tsx:
       - Category section headers (e.g., "SaaS Onboarding", "E-Commerce Checkout")
       - Grid layout: 3 columns on desktop, 2 on tablet, 1 on mobile
       - Category filter tabs at top (horizontal scrollable pills showing all 9 categories + "All")
       - Per D-05: browse by category grid, NO search functionality
       - Renders TemplateCard for each template

    3. Create app/(dashboard)/templates/page.tsx:
       - Page title: "Template Marketplace"
       - Subtitle: "Start with a proven demo pattern. Fork any template into your project."
       - TemplateGrid component
       - When user clicks "Use Template":
         a. Call template.fork mutation
         b. Show a modal/dialog with the forked .story content
         c. "Download .story file" button (generates a Blob download)
         d. "Copy to clipboard" button
         e. Brief instructions: "Open this file in your StoryCapture desktop app to start customizing"
       - Handle unauthenticated users: show templates but "Use Template" redirects to sign-in

    Category color mapping for badges and thumbnail gradients:
    - SAAS_ONBOARDING: blue
    - ECOMMERCE_CHECKOUT: green
    - API_WALKTHROUGH: purple
    - MOBILE_DEMO: pink
    - CLI_TOOL: gray
    - LANDING_PAGE: orange
    - FEATURE_ANNOUNCEMENT: yellow
    - BUG_REPRODUCTION: red
    - INTERNAL_TRAINING: teal
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Templates page shows category grid with all 9 categories
    - Template cards display name, description, category badge, fork count
    - "Use Template" triggers fork and shows downloadable .story content
    - Category filter tabs work
    - No search bar (per D-05)
    - Responsive grid layout
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public -> template listing | Templates are publicly viewable |
| Authenticated user -> fork | Fork requires auth and workspace membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | T (Tampering) | fork count | mitigate | forkCount incremented atomically via Prisma; no user input for count value |
| T-04-25 | I (Info Disclosure) | template source | accept | Template storySource is intentionally public (curated system templates) |
</threat_model>

<verification>
- TypeScript compiles
- Templates page renders with category grid
- Fork returns valid .story content
</verification>

<success_criteria>
Template marketplace displays 12 curated templates across 9 categories. Users can browse by category, view details, and fork templates as downloadable .story files. No search, no community submissions (per D-05 deferrals).
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-companion-sharing/04-07-SUMMARY.md`
</output>
