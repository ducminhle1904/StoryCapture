---
phase: 04-web-companion-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/styles/globals.css
  - apps/web/prisma/schema.prisma
  - apps/web/src/generated/prisma/
  - apps/web/src/lib/prisma.ts
  - apps/web/src/trpc/init.ts
  - apps/web/src/trpc/client.tsx
  - apps/web/src/trpc/query-client.ts
  - apps/web/src/trpc/server.tsx
  - apps/web/src/trpc/routers/_app.ts
  - apps/web/src/trpc/routers/health.ts
  - apps/web/src/app/api/trpc/[trpc]/route.ts
  - apps/web/.env.example
autonomous: true
requirements:
  - WEB-01

must_haves:
  truths:
    - "Next.js 15 App Router boots with `pnpm --filter @storycapture/web dev` and serves a page at localhost:3000"
    - "Prisma schema has all 12 models (User, Account, Session, VerificationToken, Workspace, WorkspaceMember, WorkspaceInvite, Video, ViewEvent, DailyVideoStats, Template, SyncedProject) and migrations apply cleanly"
    - "tRPC health.ping query returns {ok: true} via /api/trpc/health.ping"
    - "Tailwind v4 styling applies with shared design tokens from @storycapture/ui"
  artifacts:
    - path: "apps/web/prisma/schema.prisma"
      provides: "Full database schema for all Phase 4 features"
      contains: "model Video"
    - path: "apps/web/src/trpc/routers/_app.ts"
      provides: "Root tRPC router"
      exports: ["appRouter", "AppRouter"]
    - path: "apps/web/src/trpc/init.ts"
      provides: "tRPC context and procedure creation"
      exports: ["createTRPCContext", "t", "protectedProcedure"]
    - path: "apps/web/src/lib/prisma.ts"
      provides: "Prisma singleton"
      exports: ["prisma"]
  key_links:
    - from: "apps/web/src/app/api/trpc/[trpc]/route.ts"
      to: "apps/web/src/trpc/routers/_app.ts"
      via: "fetchRequestHandler"
      pattern: "fetchRequestHandler.*appRouter"
    - from: "apps/web/src/trpc/init.ts"
      to: "apps/web/src/lib/prisma.ts"
      via: "context creation"
      pattern: "prisma"
---

<objective>
Scaffold the Next.js 15 App Router web companion with tRPC 11, Prisma schema (all 12 models), and Tailwind v4 styling.

Purpose: Establishes the foundational web app that every subsequent plan builds upon -- auth, upload, viewer, workspaces, analytics, and sync all depend on this scaffold being correct.
Output: Bootable Next.js app with working tRPC endpoint, complete Prisma schema, and Tailwind v4 styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-companion-sharing/04-CONTEXT.md
@.planning/phases/04-web-companion-sharing/04-RESEARCH.md

<interfaces>
<!-- Existing stub to replace -->
From apps/web/package.json:
```json
{
  "name": "@storycapture/web",
  "private": true,
  "version": "0.1.0",
  "type": "module"
}
```

<!-- Shared design tokens to import -->
From packages/ui/src/tokens.css: Tailwind v4 @theme tokens (dark-first palette)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Install dependencies + Next.js App Router scaffold</name>
  <files>
    apps/web/package.json
    apps/web/tsconfig.json
    apps/web/next.config.ts
    apps/web/src/app/layout.tsx
    apps/web/src/app/page.tsx
    apps/web/src/styles/globals.css
    apps/web/.env.example
    apps/web/.gitignore
  </files>
  <action>
    1. Replace apps/web/package.json with proper Next.js dependencies per RESEARCH.md Standard Stack.
       Pin Prisma versions explicitly (blocker fix):
       - `next@15.3.x` (use 15.x line per CLAUDE.md committed stack -- Q1 RESOLVED), `react@19.x`, `react-dom@19.x`, `typescript@5.7+`
       - `@trpc/server@11.16.x`, `@trpc/client@11.16.x`, `@trpc/tanstack-react-query@11.16.x`, `@tanstack/react-query@5.x`
       - `@prisma/client@^6.0.0` (pin to ^6 per CLAUDE.md committed stack -- NOT 7.x), `next-auth@5.0.0-beta.31` (pin exact per Pitfall 1), `@auth/prisma-adapter`
       - `zod@3.x` (use zod 3.x for tRPC 11 compatibility -- zod 4.x may have breaking changes with tRPC), `superjson`, `jose`
       - `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`
       - `@maxmind/geoip2-node`, `pino`, `tailwindcss@4.x`, `client-only`, `server-only`
       - `resend` (for workspace invite emails per Q3 RESOLVED)
       - Dev deps: `prisma@^6.0.0` (pin to ^6 matching @prisma/client), `pino-pretty`, `@tailwindcss/vite`, `@types/node`, `@types/react`, `tsx`
       - Scripts: `dev`, `build`, `start`, `typecheck` (tsc --noEmit), `db:migrate` (prisma migrate dev), `db:push`, `db:generate` (prisma generate), `db:seed` (npx tsx prisma/seed.ts)

    2. Create tsconfig.json extending from monorepo base with `paths` alias: `@/*` -> `./src/*`

    3. Create next.config.ts with:
       - `output: 'standalone'` for Vercel
       - `transpilePackages: ['@storycapture/shared-types', '@storycapture/ui']`
       - Experimental: `serverComponentsExternalPackages: ['@maxmind/geoip2-node']`

    4. Create root layout.tsx with HTML + body, import globals.css, set metadata (title: "StoryCapture", description)

    5. Create landing page.tsx with simple "StoryCapture Web" heading (placeholder)

    6. Create globals.css importing Tailwind v4 with `@import "tailwindcss"` and `@import "@storycapture/ui/tokens.css"` if the tokens package exports CSS. If not, define a minimal dark theme inline.

    7. Create .env.example listing all required env vars (DATABASE_URL, AUTH_SECRET, AUTH_GITHUB_ID, AUTH_GITHUB_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET, R2_PUBLIC_URL, JWT_SECRET, MAXMIND_LICENSE_KEY, RESEND_API_KEY). Include comments describing where each value comes from. Do NOT create .env -- per D-08, env config via Vercel.

    8. Add to .gitignore: `node_modules`, `.next`, `src/generated`, `.env`, `.env.local`, `public/geolite2/`

    9. Run `pnpm install` from monorepo root to verify dependencies resolve.
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture && pnpm install && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>
    - package.json has all Phase 4 dependencies with correct versions
    - @prisma/client pinned to ^6.0.0 and prisma devDep pinned to ^6.0.0
    - next pinned to 15.3.x per CLAUDE.md
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 1b: Prisma 6 schema (12 models) + prisma.ts singleton</name>
  <files>
    apps/web/prisma/schema.prisma
    apps/web/src/lib/prisma.ts
  </files>
  <action>
    1. Create the FULL Prisma schema exactly as specified in RESEARCH.md (all 12 models):
       - Auth.js models: User, Account, Session, VerificationToken
       - Workspace models: Workspace, WorkspaceMember, WorkspaceInvite
       - Video model with slug, r2Key, status enum, isPublic (default false per D-02), sceneBoundaries JSON
       - ViewEvent + DailyVideoStats for analytics
       - Template with TemplateCategory enum (9 categories per D-05)
       - SyncedProject for desktop-web sync
       - Generator: output to `../src/generated/prisma`
       - Datasource: postgresql with env("DATABASE_URL")

    2. Create lib/prisma.ts with the global singleton pattern per RESEARCH.md Pattern 3. Add `import 'server-only'` at top per Pitfall 4.

    3. Run `cd apps/web && npx prisma generate` to verify schema compiles.
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx prisma generate && npx tsc --noEmit</automated>
  </verify>
  <done>
    - Prisma schema has 12 models, 3 enums (Role, VideoStatus, TemplateCategory), generates without errors
    - lib/prisma.ts exports singleton PrismaClient with server-only guard
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: tRPC 11 App Router setup with health endpoint</name>
  <files>
    apps/web/src/trpc/init.ts
    apps/web/src/trpc/client.tsx
    apps/web/src/trpc/query-client.ts
    apps/web/src/trpc/server.tsx
    apps/web/src/trpc/routers/_app.ts
    apps/web/src/trpc/routers/health.ts
    apps/web/src/app/api/trpc/[trpc]/route.ts
    apps/web/src/app/layout.tsx
  </files>
  <action>
    1. Create trpc/init.ts per RESEARCH.md Pattern 1:
       - Define `createTRPCContext` accepting `{ headers: Headers }`, returning `{ prisma, session: null, user: null }` (session wiring comes in Plan 04-02)
       - Export `t = initTRPC.create({ transformer: superjson })` (superjson for Date/BigInt support)
       - Export `publicProcedure = t.procedure`
       - Export `protectedProcedure` placeholder that throws UNAUTHORIZED (replaced in Plan 04-02 with real auth check)

    2. Create trpc/routers/health.ts with a `ping` query returning `{ ok: true, timestamp: new Date() }`

    3. Create trpc/routers/_app.ts merging `healthRouter`. Export `appRouter` and `type AppRouter`.

    4. Create trpc/client.tsx:
       - TRPCReactProvider component wrapping QueryClientProvider + httpBatchLink + splitLink for SSE subscriptions
       - Use `httpBatchLink` for queries/mutations, `httpSubscriptionLink` for subscriptions (import from `@trpc/client`)
       - Use superjson transformer
       - Export `useTRPC` hook via `createTRPCContext<AppRouter>()`

    5. Create trpc/query-client.ts: QueryClient factory with default staleTime: 30_000

    6. Create trpc/server.tsx: `createTRPCOptionsProxy` for RSC server-side calls

    7. Create app/api/trpc/[trpc]/route.ts with fetchRequestHandler per RESEARCH.md Pattern 1. Export GET and POST handlers.

    8. Update app/layout.tsx to wrap children with TRPCReactProvider

    ANTI-PATTERN: Do NOT use `@trpc/next` package -- that is Pages Router only. Use fetch adapter per RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/ducmle/Workspace/StoryCapture/apps/web && npx tsc --noEmit && pnpm dev &amp; sleep 5 && curl -s http://localhost:3000/api/trpc/health.ping | grep -q "ok" && kill %1</automated>
  </verify>
  <done>
    - tRPC health.ping returns `{"result":{"data":{"ok":true,...}}}` via GET /api/trpc/health.ping
    - TRPCReactProvider wraps the app layout
    - splitLink configured for future SSE subscriptions
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> tRPC API | Untrusted input from browser/desktop crosses into server procedures |
| Server -> Neon DB | Prisma queries cross into managed PostgreSQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Info Disclosure) | .env file | mitigate | .env in .gitignore, .env.example has no real values, env vars via Vercel only (D-08) |
| T-04-02 | T (Tampering) | tRPC input | mitigate | All procedures use zod input validation via tRPC |
| T-04-03 | I (Info Disclosure) | Prisma client in browser | mitigate | `import 'server-only'` in prisma.ts per Pitfall 4 |
</threat_model>

<verification>
- `pnpm --filter @storycapture/web dev` boots without errors
- `curl http://localhost:3000/api/trpc/health.ping` returns JSON with `ok: true`
- `npx prisma generate` succeeds with all 12 models
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Next.js 15 App Router boots, Prisma schema covers all Phase 4 data models, tRPC health endpoint responds, Tailwind v4 renders on the landing page.
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-companion-sharing/04-01-SUMMARY.md`
</output>
