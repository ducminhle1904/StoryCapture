---
phase: 17-record-engine-lifecycle-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/src/commands/encode.rs
  - crates/capture/src/xcap_backend.rs
  - crates/capture/src/error.rs
autonomous: true
requirements_addressed: [D-01, D-02, D-03]

must_haves:
  truths:
    - "App quit mid-record finalizes the MP4 (valid moov atom)"
    - "If capture.start_orchestrated() fails, the two spawn tasks are aborted before start_recording returns"
    - "XcapBackend::stop returns within 2s; on timeout returns CaptureError::StopTimedOut"
  artifacts:
    - path: apps/desktop/src-tauri/src/commands/encode.rs
      provides: "drain_recording_sessions(state) helper + orphan-safe start path"
    - path: apps/desktop/src-tauri/src/lib.rs
      provides: "drain_recording_sessions call on RunEvent::ExitRequested/Exit"
    - path: crates/capture/src/xcap_backend.rs
      provides: "Bounded stop via tokio::time::timeout + cancel_flag"
    - path: crates/capture/src/error.rs
      provides: "CaptureError::StopTimedOut variant"
  key_links:
    - from: "apps/desktop/src-tauri/src/lib.rs (RunEvent handler)"
      to: "apps/desktop/src-tauri/src/commands/encode.rs (drain_recording_sessions)"
      via: "direct fn call inside the existing ExitRequested/Exit match arm"
      pattern: "drain_recording_sessions"
---

<objective>
Wave 1 — CLEANUP cluster. Make shutdown and error-path teardown deterministic so app quit never corrupts an in-flight MP4, error paths do not leak spawned tasks, and XCap stop cannot hang indefinitely.

Purpose: Eliminates the #1 production risk (data loss on quit), closes two resource-leak paths.
Output: Three bounded-teardown paths and one new error variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-CONTEXT.md
@CLAUDE.md
@docs/CONVENTIONS.md
@apps/desktop/src-tauri/src/lib.rs
@apps/desktop/src-tauri/src/commands/encode.rs
@crates/capture/src/xcap_backend.rs
@crates/capture/src/error.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: D-01 exit drain + D-02 orphan-safe start</name>
  <files>
    apps/desktop/src-tauri/src/commands/encode.rs
    apps/desktop/src-tauri/src/lib.rs
  </files>
  <read_first>
    - apps/desktop/src-tauri/src/lib.rs lines 160-215 (author-preview drain pattern at 174-200 — copy structure)
    - apps/desktop/src-tauri/src/commands/encode.rs (full file — focus on RecordingRegistry 380-402, start_recording start/spawn sites 542-547 and 802-806, failure path 577-584, stop_recording_inner)
  </read_first>
  <behavior>
    - Test (unit): start_recording_inner with a mocked CaptureBackend whose start_orchestrated returns Err → assert shared Arc<AtomicUsize> alive-task counter returns to 0 within 100ms.
    - Test (integration-ish): invoke drain_recording_sessions on a registry containing one stubbed session → both encode_join and spawn handles are aborted within 5s.
  </behavior>
  <action>
    Implement two sibling changes in `encode.rs`:
    1. **D-02 orphan abort.** In `start_recording` (≈ line 422+), collect the two `tokio::spawn(...)` JoinHandles from capture-status forwarder (line ~542-547) and encoder-progress forwarder (line ~802-806) into a local `let mut spawn_abort_handles: Vec<tokio::task::AbortHandle> = Vec::new();` (push `.abort_handle()` for each). On every early-return error path after those spawns — specifically the capture start-failure path around 577-584 — iterate and call `.abort()` on each handle BEFORE returning `Err(...)`. Use a `scopeguard`-style closure or an explicit `drop_guard` struct implementing `Drop` that aborts on unwind. Prefer the explicit `Drop` guard — it also catches panics.
    2. **D-01 drain helper.** Add a pub(crate) function with exact signature:
       ```rust
       pub fn drain_recording_sessions(app_handle: &tauri::AppHandle)
       ```
       Structure (mirror `drain_author_preview_sessions` in lib.rs:174-200):
       - `app_handle.try_state::<AppState>()` → return if None
       - `state.recording_registry.try_lock()` (use `try_lock` — exit handler must never deadlock)
       - snapshot session IDs
       - for each: call `stop_recording_inner(&state, &session_id)` wrapped in `tokio::runtime::Handle::try_current()` + `block_on` with 5s timeout via `tokio::time::timeout`
       - on timeout: abort the session's `encode_join` JoinHandle, log `tracing::warn!(session_id, "drain timed out after 5s, aborted")`
       - after loop: `tracing::info!("drain_recording_sessions complete: N sessions drained")`
    Then in `lib.rs`, locate the existing `RunEvent::ExitRequested { .. } | RunEvent::Exit` match arm (≈ line 283-290) and add `drain_recording_sessions(app_handle);` AFTER `drain_author_preview_sessions(app_handle);` — same hook, ordering matters only insofar as both must run. Import the helper at the top of lib.rs: `use crate::commands::encode::drain_recording_sessions;`.
  </action>
  <verify>
    <automated>cargo nextest run -p storycapture-desktop encode::tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub fn drain_recording_sessions" apps/desktop/src-tauri/src/commands/encode.rs` == 1
    - `grep -c "drain_recording_sessions(app_handle)" apps/desktop/src-tauri/src/lib.rs` == 1
    - `grep -c "abort_handle\|AbortHandle" apps/desktop/src-tauri/src/commands/encode.rs` >= 2
    - `grep -c "impl Drop" apps/desktop/src-tauri/src/commands/encode.rs` shows a new guard struct OR explicit abort() calls on both failure paths
    - `cargo nextest run -p storycapture-desktop` exits 0
    - Manual QA: running `pnpm tauri dev`, starting record, quitting via menu, then `ffprobe output.mp4` shows a `moov` atom and non-zero duration
  </acceptance_criteria>
  <done>Quit-mid-record produces a valid MP4; capture-start failure leaves zero background tasks alive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: D-03 XCap bounded stop with cancel flag</name>
  <files>
    crates/capture/src/xcap_backend.rs
    crates/capture/src/error.rs
  </files>
  <read_first>
    - crates/capture/src/xcap_backend.rs (full file — stop() at 179-184, capture thread main loop, SendMonitor definition)
    - crates/capture/src/error.rs (CaptureError enum — follow existing variant style)
    - crates/capture/tests/ (existing test patterns)
  </read_first>
  <behavior>
    - Test (unit, synthetic): construct an XcapBackend whose capture thread is blocked/sleeping → stop() returns CaptureError::StopTimedOut within ~2.1s.
    - Test (unit): normal stop path still returns Ok(()) within the timeout.
  </behavior>
  <action>
    In `crates/capture/src/error.rs`, add variant:
    ```rust
    #[error("xcap stop timed out after {timeout_ms}ms")]
    StopTimedOut { timeout_ms: u64 },
    ```
    Keep ordering consistent with existing variants (alphabetical or logical grouping as the file already uses).
    In `xcap_backend.rs`:
    1. Add field to the backend struct: `cancel_flag: Arc<AtomicBool>` (initialize `AtomicBool::new(false)` on construction).
    2. In the capture thread main loop, check `self.cancel_flag.load(Ordering::Acquire)` at the top of each tick; break cleanly on true.
    3. Rewrite `stop()` at 179-184. New body:
       ```rust
       self.cancel_flag.store(true, Ordering::Release);
       let join_handle = self.handle.take().ok_or(CaptureError::NotRunning)?;
       match tokio::time::timeout(
           Duration::from_secs(2),
           tokio::task::spawn_blocking(move || join_handle.join())
       ).await {
           Ok(Ok(Ok(_))) => Ok(()),
           Ok(Ok(Err(e))) => Err(CaptureError::Internal(format!("thread join error: {e:?}"))),
           Ok(Err(e)) => Err(CaptureError::Internal(format!("spawn_blocking join error: {e:?}"))),
           Err(_) => {
               // Force-drop SendMonitor here if still held; log and return timeout.
               drop(self.monitor.take());
               tracing::warn!("xcap stop timed out, forced monitor drop");
               Err(CaptureError::StopTimedOut { timeout_ms: 2000 })
           }
       }
       ```
       Adapt to actual field names (`self.handle`, `self.monitor` may differ; use what exists in the file).
    4. Ensure orchestrator (`crates/capture/src/backend.rs` or equivalent) treats `StopTimedOut` as a non-fatal log-and-continue — if the orchestrator pattern-matches errors, add that arm; otherwise the default `Err` propagation is fine.
  </action>
  <verify>
    <automated>cargo nextest run -p capture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StopTimedOut" crates/capture/src/error.rs` == 1
    - `grep -c "tokio::time::timeout(Duration::from_secs(2)" crates/capture/src/xcap_backend.rs` >= 1
    - `grep -c "cancel_flag" crates/capture/src/xcap_backend.rs` >= 3 (field, store, load)
    - `cargo nextest run -p capture` exits 0
    - `cargo clippy -p capture --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>XCap stop is bounded by 2s; on timeout the new error variant is returned and logged; normal path is unchanged.</done>
</task>

</tasks>

<verification>
- `cargo nextest run -p storycapture-desktop -p capture` exits 0
- `cargo clippy --all-targets --all-features -- -D warnings` exits 0
- Manual D-01 QA: quit during record → ffprobe shows moov atom + playable duration
</verification>

<success_criteria>
Three teardown-safety holes closed: quit-mid-record finalizes, start-failure leaves zero orphans, XCap stop is bounded.
</success_criteria>

<commit_guidance>
- Split into two commits for clean blame:
  - `feat(17-02): drain recording sessions on app exit + abort orphan tasks on start failure`
  - `feat(17-02): bound XcapBackend::stop with 2s timeout + StopTimedOut error`
- NEVER include `Co-Authored-By:` trailer.
</commit_guidance>

<output>
Create `.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-02-SUMMARY.md`.
</output>
