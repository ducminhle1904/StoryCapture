---
phase: 17-record-engine-lifecycle-hardening
plan: 05
type: execute
wave: 3
depends_on: [17-01]
files_modified:
  - apps/desktop/src-tauri/src/commands/encode.rs
  - apps/desktop/src/features/recorder/recording-view.tsx
  - apps/desktop/src/ipc/encode.ts
autonomous: true
requirements_addressed: [D-13, D-14, D-15]

must_haves:
  truths:
    - "Audio negotiation failure emits RecordingEvent::AudioUnavailable with a reason string"
    - "Frontend shows a sonner toast + 'Audio unavailable' badge when AudioUnavailable fires"
    - "recording-view cleanup (unmount) calls stopRecording if sessionRef is set, nulls automation Channel handler, aborts in-flight mutations"
    - "Host emits Heartbeat every 2s while recording; FE surfaces 'out of sync' warning + Force stop on >5s gap"
  artifacts:
    - path: apps/desktop/src-tauri/src/commands/encode.rs
      provides: "AudioUnavailable emission in audio negotiation + 2s heartbeat ticker spawn"
    - path: apps/desktop/src/features/recorder/recording-view.tsx
      provides: "Toast + badge + unmount cleanup + heartbeat watchdog + Force stop"
  key_links:
    - from: "encode.rs audio negotiation (635-667)"
      to: "RecordingEvent::AudioUnavailable"
      via: "event channel emit on mic error"
      pattern: "AudioUnavailable"
    - from: "recording-view.tsx useEffect cleanup"
      to: "stopRecording + channel.onmessage = null"
      via: "return () => { ... }"
      pattern: "sessionRef"
---

<objective>
Wave 3 — UX-FEEDBACK wiring. Make invisible failures visible: surface audio errors, guarantee channel teardown on unmount, and add a host-driven heartbeat so the UI can detect and recover from desync.

Purpose: Closes the "silent failure" class of bugs between host and renderer.
Output: Three visible, recoverable UX paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-CONTEXT.md
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-01-PLAN.md
@CLAUDE.md
@docs/CONVENTIONS.md
@apps/desktop/src-tauri/src/commands/encode.rs
@apps/desktop/src/features/recorder/recording-view.tsx
@apps/desktop/src/ipc/encode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: D-13 AudioUnavailable emit + toast/badge</name>
  <files>
    apps/desktop/src-tauri/src/commands/encode.rs
    apps/desktop/src/features/recorder/recording-view.tsx
  </files>
  <read_first>
    - apps/desktop/src-tauri/src/commands/encode.rs lines 635-667 (audio/mic negotiation)
    - apps/desktop/src/features/recorder/recording-view.tsx (event-dispatch switch on RecordingEvent)
    - apps/desktop/src/ipc/encode.ts lines 85-94 (IPC wrapper)
  </read_first>
  <behavior>
    - Vitest: dispatch a mock RecordingEvent with type "audio-unavailable" and reason="Mic busy" expects toast.error("Audio unavailable: Mic busy") AND the badge text "Audio unavailable — recording video only" is rendered.
  </behavior>
  <action>
    Backend (encode.rs 635-667): at every Err(...) return inside the audio negotiation block, BEFORE returning/continuing, emit:
    let _ = session_event_tx.send(RecordingEvent::AudioUnavailable { reason: format!("{e}") }).await;
    tracing::warn!(reason = %e, "audio unavailable; recording video only");
    Then continue as video-only (do NOT abort the recording). If current code aborts on audio failure, change to log + emit + continue without audio track.
    Frontend (recording-view.tsx): in the event-dispatch switch add a case "audio-unavailable" that calls toast.error(`Audio unavailable: ${event.reason}`) and setAudioUnavailable(true). Add local state useState(false); reset on session start. Render a Badge with variant="warning" and text "Audio unavailable — recording video only" near the record status when true. Use existing Badge from packages/ui or shadcn Badge already in use; import toast from "sonner".
  </action>
  <verify>
    <automated>pnpm --filter desktop test recording-view && cargo check -p storycapture-desktop</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "AudioUnavailable" apps/desktop/src-tauri/src/commands/encode.rs is >= 1
    - grep -c "audio-unavailable" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - grep -c "Audio unavailable" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - pnpm --filter desktop test exits 0
    - pnpm --filter desktop typecheck exits 0
  </acceptance_criteria>
  <done>Mic failures no longer abort the recording silently; user sees toast + badge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: D-14 unmount cleanup</name>
  <files>
    apps/desktop/src/features/recorder/recording-view.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/recording-view.tsx lines 420-470 (useEffect with automation channel setup at 437-451)
  </read_first>
  <behavior>
    - Vitest: mount recording-view with sessionRef set, unmount, assert stopRecording IPC was invoked, automation channel.onmessage is null, AbortController for any active mutation was aborted.
  </behavior>
  <action>
    In the useEffect that sets up the recording (around 437-451), extend the returned cleanup to: (1) if automationChannel exists, set automationChannel.onmessage = null; (2) if sessionRef.current is non-null, capture it to sid, null the ref, call stopRecording(sid).catch(e => console.warn("stopRecording on unmount failed", e)); (3) call abortControllerRef.current?.abort(). Do NOT just null the ref. Add const abortControllerRef = useRef&lt;AbortController | null&gt;(null) at component top; set it to new AbortController() at the start of each mutation and pass signal to IPC where supported. If Tauri invoke does not accept AbortSignal directly, use TanStack Query mutation's built-in abort via queryClient.getMutationCache() per docs — consult TanStack Query v5 docs.
  </action>
  <verify>
    <automated>pnpm --filter desktop test features/recorder</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "onmessage = null" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - grep -c "sessionRef.current" apps/desktop/src/features/recorder/recording-view.tsx is >= 2
    - grep -c "stopRecording" apps/desktop/src/features/recorder/recording-view.tsx is >= 1 in the cleanup scope
    - grep -c "AbortController" apps/desktop/src/features/recorder/recording-view.tsx is >= 2
    - pnpm --filter desktop test exits 0
  </acceptance_criteria>
  <done>Unmount no longer leaks an in-flight session or a dangling channel handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.3: D-15 Heartbeat emit + watchdog</name>
  <files>
    apps/desktop/src-tauri/src/commands/encode.rs
    apps/desktop/src/features/recorder/recording-view.tsx
  </files>
  <read_first>
    - apps/desktop/src-tauri/src/commands/encode.rs (start_recording body, per-session spawns, stop_recording_inner)
    - apps/desktop/src/features/recorder/recording-view.tsx (event dispatch, status state)
  </read_first>
  <behavior>
    - Rust test: start_recording spawns a task that emits Heartbeat{seq: 0..=N} every 2s until stop; abort on stop sets stop_flag.
    - Vitest: mount recording-view, dispatch Heartbeat seq=0, wait 5.5s without further heartbeats, assert a "Recording state out of sync" banner with a Force stop button is shown.
  </behavior>
  <action>
    Backend (encode.rs): after session registry insert in start_recording, clone session_event_tx into tx and clone the per-session stop_flag (Arc&lt;AtomicBool&gt;; introduce one if missing). Spawn a tokio task: let hb_handle = tokio::spawn(async move { let mut interval = tokio::time::interval(Duration::from_secs(2)); let mut seq: u64 = 0; loop { interval.tick().await; if stop_flag.load(Ordering::Acquire) { break; } if tx.send(RecordingEvent::Heartbeat { seq }).await.is_err() { break; } seq = seq.wrapping_add(1); } }); Push hb_handle.abort_handle() into the per-session abort list from 17-02 Task 2.1 so drain_recording_sessions aborts it too. On stop_recording_inner, set stop_flag before joining.
    Frontend (recording-view.tsx): track last heartbeat with const [lastHeartbeat, setLastHeartbeat] = useState&lt;number | null&gt;(null) and const [desynced, setDesynced] = useState(false). On "heartbeat" event: setLastHeartbeat(Date.now()); setDesynced(false). Add useEffect watchdog gated on status === "recording": setInterval 1s that sets desynced=true when Date.now() - lastHeartbeat &gt; 5000. Render banner when desynced: text "Recording state out of sync" + a Button with onClick={forceStop} labeled "Force stop". forceStop calls stopRecording(sessionRef.current) and resets local state to idle regardless of IPC outcome.
  </action>
  <verify>
    <automated>cargo nextest run -p storycapture-desktop && pnpm --filter desktop test features/recorder</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Heartbeat { seq" apps/desktop/src-tauri/src/commands/encode.rs is >= 1
    - grep -c "tokio::time::interval(Duration::from_secs(2))" apps/desktop/src-tauri/src/commands/encode.rs is >= 1
    - grep -c "heartbeat" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - grep -c "out of sync" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - grep -c "Force stop" apps/desktop/src/features/recorder/recording-view.tsx is >= 1
    - cargo nextest run -p storycapture-desktop exits 0
    - pnpm --filter desktop test exits 0
  </acceptance_criteria>
  <done>Desync is detectable; user always has an escape hatch.</done>
</task>

</tasks>

<verification>
- cargo nextest run -p storycapture-desktop exits 0
- pnpm --filter desktop test exits 0
- pnpm --filter desktop typecheck exits 0
- biome check apps/desktop/src exits 0
- cargo clippy --all-targets --all-features -- -D warnings exits 0
</verification>

<success_criteria>
Audio failures visible; unmount always tears down the session; heartbeat + watchdog close desync windows.
</success_criteria>

<commit_guidance>
- Three commits (one per task):
  - feat(17-05): emit AudioUnavailable + show toast and badge on audio negotiation failure
  - fix(17-05): teardown session, channel, and in-flight mutations on recording-view unmount
  - feat(17-05): emit Heartbeat every 2s + frontend watchdog with Force stop
- NEVER include Co-Authored-By trailer.
</commit_guidance>

<output>
Create .planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-05-SUMMARY.md
</output>
