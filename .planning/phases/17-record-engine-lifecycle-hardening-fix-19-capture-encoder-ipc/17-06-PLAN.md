---
phase: 17-record-engine-lifecycle-hardening
plan: 06
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - crates/capture/src/sck_backend.rs
  - crates/capture/src/wgc_backend.rs
  - crates/capture/src/error.rs
  - crates/encoder/src/probe.rs
  - apps/desktop/src-tauri/src/commands/encode.rs
  - apps/desktop/src/features/recorder/TargetThumbnail.test.tsx
autonomous: true
requirements_addressed: [D-16, D-17, D-18, D-19]

must_haves:
  truths:
    - "CaptureConfig with pixel_format=Nv12 returns CaptureError::UnsupportedPixelFormat at start() on both SCK and WGC backends"
    - "probe::force_reprobe() bypasses and overwrites the cached OnceLock value"
    - "refresh_hw_encoders Tauri command invokes force_reprobe and returns Ok(())"
    - "SCK and WGC counter increments use Ordering::AcqRel instead of Relaxed where they are read cross-thread in stop()"
    - "TargetThumbnail.test.tsx contains zero @ts-ignore / @ts-expect-error"
  artifacts:
    - path: crates/capture/src/error.rs
      provides: "CaptureError::UnsupportedPixelFormat variant"
    - path: crates/encoder/src/probe.rs
      provides: "pub fn force_reprobe() helper + OnceLock overwrite"
    - path: apps/desktop/src-tauri/src/commands/encode.rs
      provides: "Real body of refresh_hw_encoders (stub from 17-01)"
  key_links:
    - from: "refresh_hw_encoders command"
      to: "encoder::probe::force_reprobe"
      via: "direct call inside command handler"
      pattern: "force_reprobe"
---

<objective>
Wave 4 — POLISH cluster. Honest pixel-format contract, re-probeable HW encoders, correct atomic ordering, zero ts-ignore.

Purpose: Contract cleanup and correctness. Low individual impact, high hygiene value.
Output: Four targeted fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-CONTEXT.md
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-01-PLAN.md
@CLAUDE.md
@docs/CONVENTIONS.md
@crates/capture/src/sck_backend.rs
@crates/capture/src/wgc_backend.rs
@crates/capture/src/error.rs
@crates/encoder/src/probe.rs
@apps/desktop/src-tauri/src/commands/encode.rs
@apps/desktop/src/features/recorder/TargetThumbnail.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 6.1: D-16 NV12 reject + D-18 atomic ordering</name>
  <files>
    crates/capture/src/sck_backend.rs
    crates/capture/src/wgc_backend.rs
    crates/capture/src/error.rs
  </files>
  <read_first>
    - crates/capture/src/error.rs (CaptureError enum, variant style)
    - crates/capture/src/sck_backend.rs (NV12 coerce at 244-247; counter increments at 305-312)
    - crates/capture/src/wgc_backend.rs (pixel-format handling; counter increments at 182-187)
  </read_first>
  <behavior>
    - Rust unit: constructing a backend and calling start() with pixel_format=Nv12 returns CaptureError::UnsupportedPixelFormat before any OS resource is acquired.
    - Rust unit: stop() reads counters that reflect all increments performed by the capture thread (no stale reads on aarch64).
  </behavior>
  <action>
    In crates/capture/src/error.rs add: UnsupportedPixelFormat { format: String } with thiserror message "unsupported pixel format: {format}". Keep alphabetic/logical ordering of variants consistent with existing style.
    In sck_backend.rs at 244-247: replace the silent coerce branch with an explicit check at start() entry: if matches!(config.pixel_format, PixelFormat::Nv12) { return Err(CaptureError::UnsupportedPixelFormat { format: "NV12".into() }); }. Remove the coerce comment and dead branch.
    In wgc_backend.rs: add the same guard at start() entry (mirror the SCK site). The orchestrator-level fallback does not catch this — the user explicitly asked for NV12, so fail loudly.
    D-18 ordering: in sck_backend.rs:305-312 and wgc_backend.rs:182-187, change Ordering::Relaxed to Ordering::AcqRel on every fetch_add that feeds a value later read in stop() / stats reporting. Grep the file after editing to confirm no Relaxed remains on the cross-thread counters. Reads in stop() should use Ordering::Acquire.
  </action>
  <verify>
    <automated>cargo nextest run -p capture && cargo clippy -p capture -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "UnsupportedPixelFormat" crates/capture/src/error.rs is 1
    - grep -c "UnsupportedPixelFormat" crates/capture/src/sck_backend.rs is >= 1
    - grep -c "UnsupportedPixelFormat" crates/capture/src/wgc_backend.rs is >= 1
    - grep -c "Ordering::AcqRel" crates/capture/src/sck_backend.rs is >= 1
    - grep -c "Ordering::AcqRel" crates/capture/src/wgc_backend.rs is >= 1
    - cargo nextest run -p capture exits 0
  </acceptance_criteria>
  <done>NV12 fails loudly; counters have acquire/release semantics on cross-thread reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.2: D-17 HW probe force_reprobe + command body</name>
  <files>
    crates/encoder/src/probe.rs
    apps/desktop/src-tauri/src/commands/encode.rs
  </files>
  <read_first>
    - crates/encoder/src/probe.rs (OnceLock/OnceCell cache at 66 + 144)
    - apps/desktop/src-tauri/src/commands/encode.rs (stub refresh_hw_encoders from 17-01)
  </read_first>
  <behavior>
    - Rust unit: probe::probe() returns cached result. probe::force_reprobe() returns a fresh result and overwrites the cache so the next probe() sees the new value. Use a mutable-for-test hook (cfg(test) setter) if OnceLock doesn't expose overwrite; prefer once_cell OnceCell with set/take OR use a parking_lot RwLock&lt;Option&lt;ProbeResult&gt;&gt; to allow overwrite.
  </behavior>
  <action>
    In crates/encoder/src/probe.rs: replace OnceLock/OnceCell with parking_lot::RwLock&lt;Option&lt;ProbeResult&gt;&gt;. Update probe() to read-lock, return clone; if None, drop read lock, write-lock, compute, store, return. Add pub fn force_reprobe() -> ProbeResult that write-locks, computes fresh, overwrites, returns. Keep existing public API stable (probe() signature unchanged).
    In encode.rs: replace the 17-01 stub body of refresh_hw_encoders with:
    pub async fn refresh_hw_encoders() -> Result<(), AppError> { tokio::task::spawn_blocking(|| encoder::probe::force_reprobe()).await.map_err(|e| AppError::Internal(format!("force_reprobe join: {e}")))?; Ok(()) }
    (Use whatever AppError::Internal / equivalent variant already exists; don't invent a new error type for this.)
  </action>
  <verify>
    <automated>cargo nextest run -p encoder -p storycapture-desktop</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pub fn force_reprobe" crates/encoder/src/probe.rs is 1
    - grep -c "force_reprobe" apps/desktop/src-tauri/src/commands/encode.rs is >= 1
    - grep -c "parking_lot::RwLock" crates/encoder/src/probe.rs returns >= 1
    - grep -c "OnceLock\|OnceCell" crates/encoder/src/probe.rs returns 0
    - cargo nextest run -p encoder test_probe_force_reprobe exits 0
    - cargo nextest run -p storycapture-desktop exits 0
  </acceptance_criteria>
  <done>HW probe can be refreshed via IPC; cache is overwritable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.3: D-19 remove @ts-ignore from TargetThumbnail.test.tsx</name>
  <files>
    apps/desktop/src/features/recorder/TargetThumbnail.test.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/TargetThumbnail.test.tsx (full file — ts-ignore at line 37)
  </read_first>
  <behavior>
    - Test file compiles under strict TS; the mock is properly typed.
  </behavior>
  <action>
    Remove the @ts-ignore at line 37. Replace the raw URL.revokeObjectURL assignment with vi.stubGlobal("URL", { ...URL, revokeObjectURL: vi.fn() }) inside a beforeEach, and call vi.unstubAllGlobals() in afterEach. Alternative acceptable form: const revokeSpy = vi.spyOn(URL, "revokeObjectURL").mockImplementation(() => {}); Pick whichever produces clean TS without @ts-ignore / @ts-expect-error. CLAUDE.md forbids both directives — this file must end with zero suppressions.
  </action>
  <verify>
    <automated>pnpm --filter desktop test TargetThumbnail && pnpm --filter desktop typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "@ts-ignore\|@ts-expect-error" apps/desktop/src/features/recorder/TargetThumbnail.test.tsx is 0
    - grep -c "vi.stubGlobal\|vi.spyOn(URL" apps/desktop/src/features/recorder/TargetThumbnail.test.tsx is >= 1
    - pnpm --filter desktop test TargetThumbnail exits 0
    - pnpm --filter desktop typecheck exits 0
  </acceptance_criteria>
  <done>No suppressions; test passes under strict TS.</done>
</task>

</tasks>

<verification>
- cargo nextest run -p capture -p encoder -p storycapture-desktop exits 0
- cargo clippy --all-targets --all-features -- -D warnings exits 0
- pnpm --filter desktop test exits 0
- pnpm --filter desktop typecheck exits 0
- biome check apps/desktop/src exits 0
- grep -rn "@ts-ignore\|@ts-expect-error" apps/desktop/src returns 0 matches
</verification>

<success_criteria>
Four polish fixes landed without regressions; contract is honest; suppressions removed.
</success_criteria>

<commit_guidance>
- Three commits:
  - feat(17-06): reject NV12 explicitly + tighten capture counter ordering to AcqRel
  - feat(17-06): force_reprobe for HW encoders + refresh_hw_encoders command body
  - test(17-06): remove @ts-ignore from TargetThumbnail.test.tsx
- NEVER include Co-Authored-By trailer.
</commit_guidance>

<output>
Create .planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-06-SUMMARY.md
</output>
