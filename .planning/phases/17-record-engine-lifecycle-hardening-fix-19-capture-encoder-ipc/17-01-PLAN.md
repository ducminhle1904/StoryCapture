---
phase: 17-record-engine-lifecycle-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src-tauri/src/commands/encode.rs
  - apps/desktop/src-tauri/src/ipc_spec.rs
  - packages/shared-types/src/ipc.ts
autonomous: true
requirements_addressed: [D-09, D-11, D-13, D-15, D-17]

must_haves:
  truths:
    - "RecordingEvent JSON wire schema gains AudioUnavailable and Heartbeat variants (additive)"
    - "StartRecordingArgs accepts optional first_frame_timeout_ms (serde default 3000)"
    - "EncodeConfig accepts optional keyframe_interval_sec"
    - "refresh_hw_encoders Tauri command is registered in ipc_spec.rs"
    - "packages/shared-types/src/ipc.ts is regenerated from tauri-specta and committed"
  artifacts:
    - path: apps/desktop/src-tauri/src/commands/encode.rs
      provides: "RecordingEvent::AudioUnavailable { reason } + Heartbeat { seq }; StartRecordingArgs.first_frame_timeout_ms; stub refresh_hw_encoders command"
    - path: apps/desktop/src-tauri/src/ipc_spec.rs
      provides: "refresh_hw_encoders in collect_commands!; new event/config types in .typ::<T>()"
    - path: packages/shared-types/src/ipc.ts
      provides: "Regenerated TS bindings"
  key_links:
    - from: "apps/desktop/src-tauri/src/commands/encode.rs"
      to: "packages/shared-types/src/ipc.ts"
      via: "tauri-specta export bindings build step"
      pattern: "AudioUnavailable|Heartbeat|first_frame_timeout_ms|keyframe_interval_sec|refresh_hw_encoders"
---

<objective>
Wave 0 — IPC contract additions. Introduce all additive IPC surface (event variants, config fields, commands) required by Waves 1-4 BEFORE any consumer code is written. This plan is a single atomic commit so the wire contract lands coherently.

Purpose: Waves 2 and 3 depend on these symbols; landing them first prevents downstream plans from racing on the shared generated `ipc.ts`.
Output: Additive Rust types, a stub `refresh_hw_encoders` command, and a regenerated `packages/shared-types/src/ipc.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-CONTEXT.md
@CLAUDE.md
@docs/ARCHITECTURE.md
@docs/CONVENTIONS.md
@apps/desktop/src-tauri/src/commands/encode.rs
@apps/desktop/src-tauri/src/ipc_spec.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Add additive RecordingEvent variants + config fields</name>
  <files>apps/desktop/src-tauri/src/commands/encode.rs</files>
  <read_first>
    - apps/desktop/src-tauri/src/commands/encode.rs (full file — understand RecordingEvent enum at 287-310, StartRecordingArgs, EncodeConfig wiring)
    - crates/encoder/src/config.rs (EncodeConfig definition for keyframe_interval_sec field)
    - apps/desktop/src-tauri/src/ipc_spec.rs (tauri-specta .typ::<T>() registration pattern)
  </read_first>
  <behavior>
    - RecordingEvent serializes AudioUnavailable as `{"type":"audio-unavailable","reason":"..."}`
    - RecordingEvent serializes Heartbeat as `{"type":"heartbeat","seq":N}`
    - StartRecordingArgs deserializes with missing first_frame_timeout_ms using default = Some(3000) (or None → caller default 3000)
    - EncodeConfig with keyframe_interval_sec = None produces identical FFmpeg argv as before this change
  </behavior>
  <action>
    In `RecordingEvent` enum (encode.rs:287-310), add two additive variants preserving `#[serde(tag="type", rename_all="kebab-case")]`:
    ```rust
    AudioUnavailable { reason: String },
    Heartbeat { seq: u64 },
    ```
    In `StartRecordingArgs` struct, add:
    ```rust
    #[serde(default)]
    pub first_frame_timeout_ms: Option<u64>,
    ```
    In `crates/encoder/src/config.rs` `EncodeConfig` struct, add:
    ```rust
    #[serde(default)]
    pub keyframe_interval_sec: Option<u32>,
    ```
    Do NOT consume these fields yet — Waves 2/3 wire them up. Keep all existing defaults/behaviors. Add a stub `#[tauri::command]` + `#[specta::specta]` async fn `refresh_hw_encoders() -> Result<(), AppError>` that returns `Ok(())` for now (Wave 4 D-17 fills the body). Add `#[derive(specta::Type)]` to EncodeConfig if missing so the field surfaces in generated TS.
    Per project conventions (docs/CONVENTIONS.md): no Co-Authored-By; concise comments only.
  </action>
  <verify>
    <automated>cargo check -p storycapture-desktop -p encoder 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AudioUnavailable" apps/desktop/src-tauri/src/commands/encode.rs` returns >= 1
    - `grep -c "Heartbeat { seq" apps/desktop/src-tauri/src/commands/encode.rs` returns >= 1
    - `grep -c "first_frame_timeout_ms: Option<u64>" apps/desktop/src-tauri/src/commands/encode.rs` returns 1
    - `grep -c "keyframe_interval_sec: Option<u32>" crates/encoder/src/config.rs` returns 1
    - `grep -c "fn refresh_hw_encoders" apps/desktop/src-tauri/src/commands/encode.rs` returns 1
    - `cargo check -p storycapture-desktop` exits 0
    - No `@ts-ignore` / `Co-Authored-By` strings introduced
  </acceptance_criteria>
  <done>Additive Rust types compile; no consumers wired yet; enum/struct changes are source-compatible with existing call sites.</done>
</task>

<task type="auto">
  <name>Task 1.2: Register in ipc_spec.rs and regenerate shared-types</name>
  <files>
    apps/desktop/src-tauri/src/ipc_spec.rs
    packages/shared-types/src/ipc.ts
  </files>
  <read_first>
    - apps/desktop/src-tauri/src/ipc_spec.rs (full file — collect_commands! + .typ::<T>() pattern)
    - packages/shared-types/src/ipc.ts (first 40 lines — confirm "AUTO-GENERATED" header)
    - docs/ARCHITECTURE.md section on IPC single source of truth
  </read_first>
  <action>
    In `ipc_spec.rs`:
    1. Add `refresh_hw_encoders` to the `collect_commands!` macro list alongside existing encode commands.
    2. Ensure `.typ::<RecordingEvent>()` and `.typ::<StartRecordingArgs>()` and `.typ::<EncodeConfig>()` are already chained; if any missing, add them.
    Then run the tauri-specta export. Per docs/ARCHITECTURE.md this runs as a build step; invoke explicitly:
    ```
    cargo test -p storycapture-desktop --features export-bindings export_bindings -- --nocapture
    ```
    or the project's canonical regen command (check `package.json` scripts / `justfile` / Cargo alias — look for `regen-types` / `gen-bindings`). Commit the regenerated `packages/shared-types/src/ipc.ts`. The file has an AUTO-GENERATED header — never hand-edit; if regen fails, fix the Rust side and re-run.
  </action>
  <verify>
    <automated>grep -c "AudioUnavailable\|Heartbeat\|first_frame_timeout_ms\|keyframe_interval_sec\|refreshHwEncoders\|refresh_hw_encoders" packages/shared-types/src/ipc.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refresh_hw_encoders\|refreshHwEncoders" apps/desktop/src-tauri/src/ipc_spec.rs` >= 1
    - `grep -c "AudioUnavailable" packages/shared-types/src/ipc.ts` >= 1
    - `grep -c "Heartbeat" packages/shared-types/src/ipc.ts` >= 1
    - `grep -c "first_frame_timeout_ms\|firstFrameTimeoutMs" packages/shared-types/src/ipc.ts` >= 1
    - `grep -c "keyframe_interval_sec\|keyframeIntervalSec" packages/shared-types/src/ipc.ts` >= 1
    - `pnpm --filter desktop typecheck` exits 0
    - AUTO-GENERATED header in `ipc.ts` preserved (not hand-edited)
  </acceptance_criteria>
  <done>ipc.ts regenerated, typecheck passes, downstream waves can import the new types.</done>
</task>

</tasks>

<verification>
- `cargo check -p storycapture-desktop -p encoder` exits 0
- `cargo clippy --all-targets --all-features -- -D warnings` exits 0 on touched crates
- `pnpm --filter desktop typecheck` exits 0
- `biome check apps/desktop/src packages/shared-types/src` exits 0
- `grep -c "Co-Authored-By" $(git diff --name-only HEAD)` returns 0
</verification>

<success_criteria>
All five additive IPC symbols (AudioUnavailable, Heartbeat, first_frame_timeout_ms, keyframe_interval_sec, refresh_hw_encoders) exist on both Rust and TS sides; no consumers wired yet; zero breaking changes.
</success_criteria>

<commit_guidance>
- Single commit: `feat(17-01): add additive IPC surface for record-engine hardening`
- Message body: list the 5 additive symbols and note they are consumer-agnostic.
- NEVER include `Co-Authored-By:` trailer (CLAUDE.md mandate).
</commit_guidance>

<output>
Create `.planning/phases/17-record-engine-lifecycle-hardening-fix-19-capture-encoder-ipc/17-01-SUMMARY.md`.
</output>
