---
phase: 13
plan: 04
type: execute
wave: 3
depends_on: [13-03]
files_modified:
  - apps/desktop/src/features/recorder/video-output/copy.ts
  - apps/desktop/src/features/recorder/video-output/video-output-section.tsx
  - apps/desktop/src/features/recorder/video-output/preset-select.tsx
  - apps/desktop/src/features/recorder/video-output/resolution-control.tsx
  - apps/desktop/src/features/recorder/video-output/fps-control.tsx
  - apps/desktop/src/features/recorder/video-output/fit-mode-control.tsx
  - apps/desktop/src/features/recorder/video-output/pad-color-control.tsx
  - apps/desktop/src/features/recorder/video-output/quality-preset-control.tsx
  - apps/desktop/src/features/recorder/video-output/bitrate-preview.tsx
  - apps/desktop/src/features/recorder/video-output/warnings.tsx
  - apps/desktop/src/features/recorder/video-output/output-summary-badge.tsx
  - apps/desktop/src/features/recorder/video-output/bitrate.ts
  - apps/desktop/src/features/recorder/video-output/bitrate.test.ts
  - apps/desktop/src/features/recorder/video-output/video-output-section.test.tsx
  - apps/desktop/src/features/recorder/recording-view.tsx
autonomous: true
requirements: [ENC-12, ENC-17, ENC-18, ENC-19]
must_haves:
  truths:
    - "User opens the Recorder view and sees a 'Đầu ra video' section with 5 knobs + preset select."
    - "Selecting a preset (Nhanh / Tiêu chuẩn / Chất lượng cao) updates all 5 knobs and the summary badge."
    - "Overriding any knob flips the preset label to 'Tùy chỉnh'."
    - "Selecting Resolution = 'Tùy chỉnh' reveals W×H inputs; submitting odd or out-of-range values shows the hard-error and disables the Record button."
    - "Selecting Pad color = 'Tùy chỉnh' reveals a native color picker + hex text input that stay in sync."
    - "The bitrate preview text updates live as knobs change."
    - "The summary badge next to the Record button always shows current values; clicking scrolls the section into view."
    - "Soft warnings appear inline (Lossless+4K+HW, Output>Capture) without blocking submit."
    - "Every interactive control disables when status is recording/paused/stopping."
    - "All copy is Vietnamese and centralized in copy.ts."
    - "When startRecording fires, the 5 optional fields (output_resolution, fit_mode, pad_color, quality_preset, scale_algo) are populated from the store."
  artifacts:
    - path: "apps/desktop/src/features/recorder/video-output/video-output-section.tsx"
      provides: "<VideoOutputSection /> root container"
    - path: "apps/desktop/src/features/recorder/video-output/output-summary-badge.tsx"
      provides: "<OutputSummaryBadge /> chip rendered next to Record CTA (D-13-02)"
    - path: "apps/desktop/src/features/recorder/video-output/copy.ts"
      provides: "Vietnamese label constants"
    - path: "apps/desktop/src/features/recorder/video-output/bitrate.ts"
      provides: "computeBitratePreview, resolveDims, validateCustomDims (pure)"
    - path: "apps/desktop/src/features/recorder/recording-view.tsx"
      provides: "Hosts <VideoOutputSection /> + <OutputSummaryBadge /> + threads 5 store knobs into startRecording call"
  key_links:
    - from: "video-output-section.tsx"
      to: "useOutputPrefsStore"
      via: "selector for recordingKnobs + activePreset"
      pattern: "useOutputPrefsStore"
    - from: "recording-view.tsx startRecording call site"
      to: "ipc/encode.ts StartRecordingArgs"
      via: "thread 5 store knobs"
      pattern: "output_resolution"
    - from: "output-summary-badge.tsx onActivate"
      to: "video-output-section DOM ref"
      via: "scrollIntoView"
      pattern: "scrollIntoView"
---

<objective>
Build the Recording-time Video Output section: 5 knobs + preset select + bitrate preview + warnings + summary badge, all wired to the shared store from Plan 13-03 and threaded into the existing `startRecording` IPC call. Vietnamese copy throughout. Delivers ENC-12, ENC-17, ENC-18, ENC-19.

Purpose: Operators today cannot change recording resolution/FPS/fit/pad/quality without editing code (Phase 12 hard-codes 1080p/30/Letterbox/Black/Med). D-13-01..02, D-13-08..12 lock the UX shape; this plan implements it.

Output:
- 11 new files under `apps/desktop/src/features/recorder/video-output/`
- 1 modified file: `recording-view.tsx`
- Vitest suite for bitrate formula + RTL tests for the section
- All UI strings in Vietnamese, centralized in `copy.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-CONTEXT.md
@.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-RESEARCH.md
@.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-PATTERNS.md
@.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-UI-SPEC.md
@.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-03-PLAN.md
@apps/desktop/src/features/recorder/recording-view.tsx
@apps/desktop/src/features/recorder/AudioDevicePicker.tsx
@apps/desktop/src/features/recorder/CursorToggle.tsx
@apps/desktop/src/features/recorder/ChromeHidingToggle.tsx
@apps/desktop/src/state/output-prefs.ts
@apps/desktop/src/components/ui/select.tsx

<interfaces>
From Plan 13-03 (apps/desktop/src/state/output-prefs.ts):
  - useOutputPrefsStore hook
  - RecordingKnobs, PresetName, PRESET_BUNDLES
  - selectors: state.recordingKnobs, state.activePreset
  - actions: state.setRecordingKnob<K>(k, v), state.applyPreset(name)

From Plan 13-02 (apps/desktop/src/components/ui/):
  - Accordion, ToggleGroup, RadioGroup, Slider, Input, Label, ColorField, NumberField

From recording-view.tsx (existing):
  - SettingsGroup wrapper component (lines 793-835)
  - status guard: status === "recording" || status === "paused" || status === "stopping"
  - startRecording call site: search `startRecording(`

UI-SPEC layout (13-UI-SPEC.md lines 130-235):
  - Section title left + Preset select right
  - 5 knob rows in `grid grid-cols-[120px_1fr] gap-4 items-center`
  - Custom reveal animations: 160ms motion height+opacity, gated via useReducedMotion

Vietnamese copy (UI-SPEC table lines 248-298):
  - Section title: "Đầu ra video"
  - Hard-validation copy: "Chiều rộng/cao phải là số chẵn và trong khoảng 16–7680 × 16–4320."

Bitrate formula (D-13-11):
  bitrateKbps = (w * h * 3 / 1000) * { low:0.75, med:1.0, high:1.25, lossless:1.5 }[quality]
  mbPerMin = bitrateKbps * 60 / 8 / 1024

Resolution → (w,h):
  P720: 1280×720, P1080: 1920×1080, P1440: 2560×1440, P4K: 3840×2160
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helpers — copy.ts (VN strings) + bitrate.ts (formula + tests)</name>
  <files>
    apps/desktop/src/features/recorder/video-output/copy.ts,
    apps/desktop/src/features/recorder/video-output/bitrate.ts,
    apps/desktop/src/features/recorder/video-output/bitrate.test.ts
  </files>
  <read_first>
    - .planning/phases/13-video-output-customization-knobs-recording-export-ui/13-UI-SPEC.md lines 240-300 (Vietnamese copy table)
    - .planning/phases/13-video-output-customization-knobs-recording-export-ui/13-CONTEXT.md lines 200-260 (specifics — bitrate formula + warning copy)
    - apps/desktop/src/state/output-prefs.ts (RecordingKnobs type, QualityPresetDto names)
    - packages/shared-types/src/ipc.ts (verify FitModeDto and PadColorDto exact serialized form, e.g. "letterbox" vs "Letterbox")
  </read_first>
  <behavior>
    - Test 1: `computeBitratePreview({ w:1920, h:1080, quality:"med" })` → mbps≈6.22 (±0.1), mbPerMin≈45 (±1).
    - Test 2: `computeBitratePreview({ w:3840, h:2160, quality:"lossless" })` → mbps≈37.32, mbPerMin≈273.
    - Test 3: `computeBitratePreview({ w:0, h:0, quality:"med" })` → `{ mbps:0, mbPerMin:0 }` (no NaN).
    - Test 4: `formatBitratePreview(6.22, 45)` → `"~6.2 Mbps • ~45 MB/phút"`.
    - Test 5: `resolveDims({ kind:"P1080" })` → `{ w:1920, h:1080 }`.
    - Test 6: `resolveDims({ kind:"Custom", w:1280, h:720 })` → `{ w:1280, h:720 }`.
    - Test 7: `resolveDims({ kind:"MatchSource" }, { w:1600, h:900 })` → `{ w:1600, h:900 }`.
    - Test 8: `validateCustomDims(1281, 720)` → `{ valid:false, reason:"odd-width" }`.
    - Test 9: `validateCustomDims(8000, 720)` → `{ valid:false, reason:"out-of-range" }`.
    - Test 10: `validateCustomDims(1920, 1080)` → `{ valid:true }`.
  </behavior>
  <action>
    Step A — Create `copy.ts` with all Vietnamese label constants per UI-SPEC table 248-298. Export named const only (no default). Group by surface (section / preset / knobs / warnings / badge / preview).

    Required constants (verify enum string forms against packages/shared-types/src/ipc.ts before finalizing):
    - `SECTION_TITLE = "Đầu ra video"`
    - `LABEL_PRESET = "Preset"`
    - `PRESET_OPTION_LABELS: Record<PresetName, string>` mapping to "Nhanh" / "Tiêu chuẩn" / "Chất lượng cao" / "Tùy chỉnh"
    - `LABEL_RESOLUTION = "Độ phân giải"`, `RESOLUTION_OPTION_LABELS` mapping P720/P1080/P1440/P4K/MatchSource/Custom
    - `LABEL_CUSTOM_W = "Rộng"`, `LABEL_CUSTOM_H = "Cao"`, `HELPER_CUSTOM_DIMS = "Chẵn, 16–7680 × 16–4320"`
    - `LABEL_FPS = "FPS"`
    - `LABEL_FIT = "Chế độ lấp khung"`, `FIT_OPTION_LABELS` mapping Letterbox/Pillarbox/Cắt/Kéo giãn
    - `LABEL_PAD = "Màu viền"`, `PAD_OPTION_LABELS` mapping Đen/Trắng/Tùy chỉnh
    - `LABEL_QUALITY = "Chất lượng"`, `QUALITY_OPTION_LABELS` mapping Thấp/Trung bình/Cao/Lossless
    - `WARN_HARD_CUSTOM_DIMS = "Chiều rộng/cao phải là số chẵn và trong khoảng 16–7680 × 16–4320."`
    - `WARN_SOFT_LOSSLESS_4K_HW = "Chất lượng Lossless ở 4K với HW encoder có thể vượt bitrate cap phần cứng và khiến render chậm. Cân nhắc giảm xuống Cao hoặc chuyển sang Software (libx264)."`
    - `WARN_SOFT_OUTPUT_GT_CAPTURE = "Nguồn ghi nhỏ hơn kích thước output — video sẽ giữ nguyên kích thước nguồn và thêm viền thay vì phóng to (không làm mờ text)."`
    - `BADGE_TOOLTIP = "Nhấn để xem chi tiết đầu ra video"`
    - `formatBitratePreview(mbps, mbPerMin) → "~X.X Mbps • ~N MB/phút"` (one-decimal Mbps via `.toFixed(1)`, integer MB via `Math.round`)

    Step B — Create `bitrate.ts` with pure functions per the `<behavior>` test contract:
    - `Q_MUL: Record<QualityPresetDto, number>` constant
    - `interface Dims { w:number; h:number }`
    - `resolveDims(res: OutputResolutionDto, capture?: Dims): Dims` — switch on res.kind covering P720/P1080/P1440/P4K/MatchSource/Custom
    - `computeBitratePreview({ w, h, quality }): { mbps, mbPerMin }` — guard against w<=0||h<=0 returning zeros
    - `validateCustomDims(w, h): { valid: true } | { valid: false, reason: "odd-width" | "odd-height" | "out-of-range" }` — order: range check first, then evenness

    Step C — Create `bitrate.test.ts` covering all 10 behaviors. Use `expect(x).toBeCloseTo(y, 1)` for mbps.

    Verify QualityPresetDto serialization values from packages/shared-types/src/ipc.ts before hardcoding "low"/"med"/"high"/"lossless".
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/features/recorder/video-output/bitrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist
    - All 10 tests in `bitrate.test.ts` pass
    - `grep -q 'SECTION_TITLE = "Đầu ra video"' apps/desktop/src/features/recorder/video-output/copy.ts` succeeds
    - `grep -q 'WARN_HARD_CUSTOM_DIMS' apps/desktop/src/features/recorder/video-output/copy.ts` succeeds
    - `grep -q 'computeBitratePreview' apps/desktop/src/features/recorder/video-output/bitrate.ts` succeeds
    - `grep -q 'validateCustomDims' apps/desktop/src/features/recorder/video-output/bitrate.ts` succeeds
    - `pnpm typecheck` passes (no `any`, no `@ts-ignore`)
  </acceptance_criteria>
  <done>Pure helpers + Vietnamese copy module shipped with full coverage. Components in Task 2 import from these.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2A: Build 5 single-knob controls + bitrate-preview + warnings (no integration tests yet)</name>
  <files>
    apps/desktop/src/features/recorder/video-output/preset-select.tsx,
    apps/desktop/src/features/recorder/video-output/resolution-control.tsx,
    apps/desktop/src/features/recorder/video-output/fps-control.tsx,
    apps/desktop/src/features/recorder/video-output/fit-mode-control.tsx,
    apps/desktop/src/features/recorder/video-output/pad-color-control.tsx,
    apps/desktop/src/features/recorder/video-output/quality-preset-control.tsx,
    apps/desktop/src/features/recorder/video-output/bitrate-preview.tsx,
    apps/desktop/src/features/recorder/video-output/warnings.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/AudioDevicePicker.tsx (full — Base UI Select + lazy query + discriminated-union round-trip lines 42-127)
    - apps/desktop/src/features/recorder/CursorToggle.tsx (small bespoke control with label rhythm)
    - apps/desktop/src/components/ui/select.tsx (header docstring + tokens reference)
    - apps/desktop/src/components/ui/{toggle-group,radio-group,input,label,color-field,number-field}.tsx (Plan 13-02 outputs — actual API)
    - .planning/phases/13-video-output-customization-knobs-recording-export-ui/13-UI-SPEC.md lines 130-235 (full layout + motion + state matrix)
    - .planning/phases/13-video-output-customization-knobs-recording-export-ui/13-PATTERNS.md lines 274-360 (discriminated-union round-trip, segmented, conditional reveal, motion gating)
    - apps/desktop/src/state/output-prefs.ts (selectors)
    - apps/desktop/src/features/recorder/video-output/{bitrate,copy}.ts (Task 1 output)
  </read_first>
  <action>
    Every component file: 4-line header docstring citing Phase 13 + relevant decision IDs. All token-only styling (no hex, no `bg-zinc-*`, no `text-gray-*`). Use `useOutputPrefsStore((s) => s.recordingKnobs.X)` selectors.

    Step A — `preset-select.tsx`: Base UI Select with 4 options. Display value = PRESET_OPTION_LABELS[activePreset]. onChange: if user picks Custom → no-op (Custom is set automatically by setRecordingKnob); else `applyPreset(name)`. Pattern: copy AudioDevicePicker.tsx lines 108-196 structure, swap query for static preset list.

    Step B — `resolution-control.tsx`: Use the `kind:payload` round-trip pattern from AudioDevicePicker.tsx:42-83. Select with options P720/P1080/P1440/P4K/MatchSource/Custom. When kind === "Custom", reveal a `<motion.div>` containing two `<NumberField>` (W, H) + helper text. On every W/H change call `validateCustomDims`. If invalid → set local invalid state AND publish via lifted state to the parent (Task 2B's <VideoOutputSection> reads it via the exported `useIsRecordingBlocked` hook). Custom-reveal animation: 160ms `motion.div` height+opacity, gated via `useReducedMotion()`.

    Step C — `fps-control.tsx`: Base UI RadioGroup with 3 options (24, 30, 60). Horizontal layout. onValueChange → `setRecordingKnob("fps", n)`.

    Step D — `fit-mode-control.tsx`: Base UI ToggleGroup `type="single"` with 4 options. Labels from FIT_OPTION_LABELS. onValueChange → `setRecordingKnob("fit", v)`.

    Step E — `pad-color-control.tsx`: Base UI ToggleGroup `type="single"` with 3 segments. When active === "Custom", reveal `<motion.div>` containing `<ColorField value={hexFromRgb(pad)} onChange={(hex) => setRecordingKnob("pad", { kind:"custom", ...rgbFromHex(hex) })} />`. Implement 5-line pure helpers `hexFromRgb({r,g,b})→string` and `rgbFromHex(hex)→{r,g,b}` inline.

    Step F — `quality-preset-control.tsx`: Base UI RadioGroup with 4 options. Labels from QUALITY_OPTION_LABELS.

    Step G — `bitrate-preview.tsx`: Pure presentational. Reads `recordingKnobs.resolution` + `.quality` + recorder capture dims (read from `useRecorderStore` — refer to apps/desktop/src/state/recorder.ts for the field name). Calls `resolveDims` + `computeBitratePreview` + `formatBitratePreview`. Mono font. Info-stripe left border per UI-SPEC line 82. Reserve 40px height to prevent layout shift.

    Step H — `warnings.tsx`: Pure derived from store + capture dims + hwEncoder. Receives `errors: Array<{id, msg}>` prop (lifted from <ResolutionControl />). Computes soft warnings inline: Lossless+4K+HW, Output>Capture. Render hard errors first (red stripe), then soft warnings (yellow stripe + TriangleAlert). `role="status" aria-live="polite"` on soft container; hard errors associate via `aria-describedby` from offending input.

    NO composition, NO summary badge, NO RTL tests in this task — those land in Task 2B.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist
    - `grep -L "@radix-ui" apps/desktop/src/features/recorder/video-output/{preset-select,resolution-control,fps-control,fit-mode-control,pad-color-control,quality-preset-control,bitrate-preview,warnings}.tsx | wc -l` returns 8 (no Radix)
    - Every component file has a 4-line header docstring referencing "Phase 13" or "D-13-XX"
    - At least 6 of the 8 files import from `@/state/output-prefs` or `@/features/recorder/video-output/copy`
    - `pnpm typecheck` clean
    - `pnpm lint` clean
  </acceptance_criteria>
  <done>8 single-purpose files (6 controls + bitrate preview + warnings) shipped with Vietnamese copy + token-only styling + accessibility wiring; typecheck + lint green; ready for Task 2B to compose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2B: Compose VideoOutputSection + OutputSummaryBadge + 8-test RTL suite</name>
  <files>
    apps/desktop/src/features/recorder/video-output/video-output-section.tsx,
    apps/desktop/src/features/recorder/video-output/output-summary-badge.tsx,
    apps/desktop/src/features/recorder/video-output/video-output-section.test.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/video-output/{preset-select,resolution-control,fps-control,fit-mode-control,pad-color-control,quality-preset-control,bitrate-preview,warnings}.tsx (Task 2A outputs)
    - apps/desktop/src/components/ui/label.tsx
    - apps/desktop/src/state/output-prefs.ts
    - .planning/phases/13-video-output-customization-knobs-recording-export-ui/13-UI-SPEC.md lines 130-235 (composition + summary badge layout)
  </read_first>
  <behavior>
    - Test 1 (RTL): VideoOutputSection renders with Standard preset → all 5 knob labels visible in Vietnamese.
    - Test 2 (RTL): Choose resolution = "Tùy chỉnh…" → W/H NumberFields appear; type 1281 in W → `WARN_HARD_CUSTOM_DIMS` text visible and W input has `aria-invalid="true"`.
    - Test 3 (RTL): Set quality = "lossless" + resolution = P4K (with mock store hwEncoder !== "software" && hwEncoder !== "auto") → `WARN_SOFT_LOSSLESS_4K_HW` text inside `[role="status"]`.
    - Test 4 (RTL): Click Pad color "Tùy chỉnh" → ColorField appears initialized to "#000000". Typing "#ff6b2d" updates store pad to `{ kind:"custom", r:255, g:107, b:45 }`.
    - Test 5 (RTL): With Standard preset, change FPS 30→60 → activePreset display flips to "Tùy chỉnh".
    - Test 6 (RTL): With Standard preset, change FPS 30→60 then Quality med→high → activePreset flips back to "Chất lượng cao" (matched High Quality bundle).
    - Test 7 (RTL): Render OutputSummaryBadge with Standard knobs → text contains "1080p • 30fps • Letterbox • Trung bình".
    - Test 8 (RTL): Click summary badge → `Element.prototype.scrollIntoView` (mocked) is called once.
  </behavior>
  <action>
    Step I — `video-output-section.tsx`: Composes everything from Task 2A.
      - Header row: SECTION_TITLE left, <PresetSelect /> right.
      - 5 knob rows in `grid grid-cols-[120px_1fr] gap-4 items-center` (single column on narrow).
      - Owns `customDimsError: ValidationResult | null` lifted from <ResolutionControl />.
      - Renders <BitratePreview /> + <Warnings /> below the grid.
      - Disable guard on every interactive control: `disabled = status === "recording" || status === "paused" || status === "stopping"` (read recorder status via existing recorder store hook).
      - Forwards a ref so `<OutputSummaryBadge />` can scroll to it.
      - Exports a separate `useIsRecordingBlocked()` hook returning true when any hard error is active.

    Step J — `output-summary-badge.tsx`: Reads recordingKnobs + activePreset, renders pill chip per UI-SPEC line 281-283:
      - Format: `${resLabel} • ${fps}fps • ${fitLabel} • ${qualityLabel}` + (pad.kind !== "black" ? ` • Viền ${padLabel}` : "")
      - Custom resolution → render literal `WxH` (e.g. "1280×720")
      - max-w-[220px] truncate
      - onClick: call consumer's `onActivate` prop
      - Tooltip: BADGE_TOOLTIP. ChevronRight icon (lucide-react).

    Step K — `video-output-section.test.tsx`: Cover all 8 behaviors with @testing-library/react + user-event + happy-dom (existing project setup). Reset `useOutputPrefsStore` and `useRecorderStore` in `beforeEach`. Mock `Element.prototype.scrollIntoView = vi.fn()` for Test 8.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/features/recorder/video-output/video-output-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist (section + badge + test)
    - All 8 RTL tests pass
    - `grep -L "@radix-ui" apps/desktop/src/features/recorder/video-output/video-output-section.tsx apps/desktop/src/features/recorder/video-output/output-summary-badge.tsx | wc -l` returns 2 (no Radix imports in composition layer)
    - `grep -c "var(--color-" apps/desktop/src/features/recorder/video-output/video-output-section.tsx` ≥ 1
    - 4-line header docstring present in both component files
    - `pnpm typecheck` clean
    - `pnpm lint` clean
  </acceptance_criteria>
  <done>VideoOutputSection composes the 8 Task-2A pieces; OutputSummaryBadge renders the chip; 8-test RTL suite green; ready to host in recording-view.tsx (Task 3).</done>
</task>


<task type="auto">
  <name>Task 3: Host VideoOutputSection + OutputSummaryBadge in recording-view.tsx + thread store knobs into startRecording</name>
  <files>
    apps/desktop/src/features/recorder/recording-view.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/recorder/recording-view.tsx (full — lines 760-870 for the Recording Setup panel + Record CTA region; search `startRecording(` for the IPC call site)
    - apps/desktop/src/features/recorder/video-output/video-output-section.tsx (Task 2 output — exports + ref API)
    - apps/desktop/src/features/recorder/video-output/output-summary-badge.tsx (Task 2 output)
    - apps/desktop/src/state/output-prefs.ts (selectors)
    - apps/desktop/src/ipc/encode.ts (StartRecordingArgs after Plan 13-03)
    - .planning/phases/12-fix-video-output-resolution-lock-letterbox/12-04-PLAN.md (verify whether Phase 12 expects existing width/height to be CAPTURE dims and the new output_resolution to drive OUTPUT dims — the answer determines whether to keep or replace the existing width/height args)
  </read_first>
  <action>
    Step A — Insert <VideoOutputSection /> after the existing ChromeHidingToggle SettingsGroup, BEFORE the Record CTA row:
    ```tsx
    const videoOutputSectionRef = useRef<HTMLDivElement>(null); // declared near other refs
    // ...
    <VideoOutputSection ref={videoOutputSectionRef} />
    ```

    Step B — Insert <OutputSummaryBadge /> next to the Record button in the Record CTA row:
    ```tsx
    <div className="flex items-center gap-3">
      <button onClick={handleRecord} disabled={recordDisabled || isOutputBlocked}>● Record</button>
      <OutputSummaryBadge
        onActivate={() => {
          videoOutputSectionRef.current?.scrollIntoView({
            behavior: prefersReducedMotion ? "auto" : "smooth",
            block: "center",
          });
        }}
      />
    </div>
    ```
    `prefersReducedMotion` from existing `useReducedMotion()` import (recording-view.tsx line 15).
    `isOutputBlocked = useIsRecordingBlocked()` from VideoOutputSection's exported hook.

    Step C — Thread store values into the existing `startRecording({...})` call (search for the call site).

    **Capture vs Output dimension contract (from `.planning/phases/12-fix-video-output-resolution-lock-letterbox/12-04-PLAN.md`):**
    Phase 12 plan 12-04 EXTENDED `StartRecordingArgs` with 5 optional fields (`output_resolution`, `fit_mode`, `pad_color`, `quality_preset`, `scale_algo`) WITHOUT removing the existing `width`, `height`, `fps` fields. The Phase 12 contract is explicit:
      - `width` / `height` continue to mean **CAPTURE dimensions** (the source surface size requested from the OS capture API). Backend default applies when the new optional fields are absent.
      - `output_resolution` (the new field) drives **OUTPUT dimensions** (the encoded video frame size); when absent, backend falls back to Phase 12 default `P1080`.
      - `fps` is a single field shared by capture and output (FPS split is deferred per Phase 12 D-12-09); Phase 13 reads `prefs.fps` and writes it into this single field — no separate output FPS field exists.

    Therefore Phase 13 MUST:
      - **Preserve** the existing `width`, `height` arguments — they remain the capture-source dims set by the existing recorder UI (do NOT remove them, do NOT overwrite them with output dims).
      - **Overwrite** `fps` with `prefs.fps` (single shared field; Phase 12 has no output-fps split).
      - **Add** the 5 new optional fields driven by the store.

    ```tsx
    const prefs = useOutputPrefsStore.getState().recordingKnobs;
    const exportPrefs = useOutputPrefsStore.getState().exportKnobs;
    await startRecording(
      {
        // existing fields preserved EXACTLY: project_folder, target, width, height, audio_device_id, include_cursor
        // — width/height remain CAPTURE dims (Phase 12 contract per 12-04-PLAN.md)
        fps: prefs.fps,
        // Phase 13 — OUTPUT knobs from useOutputPrefsStore
        output_resolution: prefs.resolution,
        fit_mode: prefs.fit,
        pad_color: prefs.pad,
        quality_preset: prefs.quality,
        scale_algo: exportPrefs.downscaleAlgo,
      },
      onEvent,
    );
    ```
    Use `useOutputPrefsStore.getState()` (one-shot read at click time) NOT a reactive hook — recording starts on user action.

    Step D — Wire Record-button disable to hard-error state via the `isOutputBlocked` selector from Step B.

    Per CLAUDE.md "no big change without plan approval" — this is a focused integration, not a refactor. Do NOT remove the existing `recorder.fps` state field; it remains in the store but is no longer read for the IPC call (cleanup is a future quick-task). Per CLAUDE.md concise comments: at most 1 short inline comment near the new fields ("// Phase 13 — output knobs from useOutputPrefsStore"). No essays.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm typecheck && pnpm lint && pnpm vitest run src/features/recorder</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "VideoOutputSection" apps/desktop/src/features/recorder/recording-view.tsx` succeeds
    - `grep -q "OutputSummaryBadge" apps/desktop/src/features/recorder/recording-view.tsx` succeeds
    - `grep -q "useOutputPrefsStore" apps/desktop/src/features/recorder/recording-view.tsx` succeeds
    - `grep -q "output_resolution\|fit_mode\|pad_color\|quality_preset" apps/desktop/src/features/recorder/recording-view.tsx` returns 4 matches (each appears at least once in the startRecording args)
    - `grep -q "useIsRecordingBlocked\|isOutputBlocked" apps/desktop/src/features/recorder/recording-view.tsx` succeeds
    - `pnpm typecheck` clean
    - `pnpm lint` clean
    - Existing recorder vitest suite still passes (no regressions)
  </acceptance_criteria>
  <done>recording-view.tsx hosts the new section + badge, threads 5 store knobs into startRecording, blocks Record button on hard errors, all existing tests still green.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/features/recorder/video-output` → all bitrate + section tests pass
- `pnpm typecheck` + `pnpm lint` clean
- Manual smoke (operator runs `pnpm tauri dev`):
  - Open Recorder → "Đầu ra video" section visible with Vietnamese labels
  - Choose preset "Nhanh" → resolution drops to 720p, summary badge updates
  - Set Resolution = Tùy chỉnh, type W=1281 → red error appears, Record button disabled
  - Toggle Pad = Tùy chỉnh → color picker + hex input appear, sync works
  - Click summary badge → section scrolls into view
</verification>

<success_criteria>
- Recording-time UI fully delivers ENC-12 (5 knobs), ENC-17 (bitrate preview), ENC-18 (warnings hard+soft), ENC-19 (summary badge)
- All Vietnamese copy in `copy.ts` only — no inline JSX strings
- Phase 12 backend receives the 5 optional fields end-to-end via the existing `start_recording` command
</success_criteria>

<output>
After completion, create `.planning/phases/13-video-output-customization-knobs-recording-export-ui/13-04-SUMMARY.md`
</output>
