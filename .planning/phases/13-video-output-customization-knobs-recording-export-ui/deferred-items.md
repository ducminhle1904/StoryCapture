# Phase 13 Deferred Items

## Pre-existing untracked

- `apps/desktop/src/features/recorder/video-output/bitrate.test.ts` — untracked test file that references a missing `./bitrate` module. Pre-dates Plan 13-05 (out of scope). Wave 2 recording view plan owns this; leaving untouched.
