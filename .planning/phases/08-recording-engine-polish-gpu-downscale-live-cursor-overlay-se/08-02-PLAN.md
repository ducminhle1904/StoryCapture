---
phase: 08
plan: 02
type: execute
wave: 3
depends_on: ["08-01", "08-04"]
files_modified:
  - crates/gpu_scale/Cargo.toml
  - crates/gpu_scale/src/lib.rs
  - crates/gpu_scale/src/macos.rs
  - crates/gpu_scale/src/composite.metal
  - crates/gpu_scale/benches/scale_bgra.rs
  - crates/gpu_scale/tests/macos_scale_roundtrip.rs
  - crates/encoder/src/pipeline.rs
  - crates/encoder/src/macos/vt_writer.rs
  - crates/encoder/src/config.rs
  - crates/capture/src/macos/raii.rs
autonomous: true
requirements:
  - PHASE-8.1  # GPU scale stack (Metal side)
  - PHASE-8.2  # VtWriter zero-copy integration (D-02)
  - PHASE-8.5  # GPU perf target (D-07) — Metal side
must_haves:
  truths:
    - "On macOS, `gpu_scale::probe_and_build(target_width)` returns `Ok(Some(MetalScaler))` when a default Metal device + MPS are available."
    - "`MetalScaler::scale_to_cvpixelbuffer` produces a NEW `CVPixelBuffer` of `target_width × scaled_height` (aspect-preserved, even dims) in `kCVPixelFormatType_32BGRA`."
    - "`MetalScaler::scale_to_bgra` produces packed BGRA with stride == width × 4."
    - "The VT fast path never calls `CVPixelBufferLockBaseAddress` on the input buffer after gpu_scale is active (no CPU readback)."
    - "AVAssetWriter's `AVVideoWidthKey` / `AVVideoHeightKey` match the post-scale dims (A4 resolved test-first)."
    - "`EncodeConfig.upstream_scale_applied` is set to `true` at pump/VtWriter init when a Metal scaler is built for an SCK-sourced recording."
    - "Criterion bench `scale_4k_to_1920` median ≤ 3 ms/frame on M-series hardware (D-07)."
    - "All Metal + MPS textures created with `bgra8Unorm` (NOT `_srgb`) — Pitfall 1 avoided."
  artifacts:
    - path: crates/gpu_scale/src/macos.rs
      provides: "MetalScaler with MPSImageBilinearScale + CVMetalTextureCache"
    - path: crates/gpu_scale/src/composite.metal
      provides: "Fused scale+cursor MSL compute kernel (placeholder body for 08-02; used in 08-05 fused path)"
    - path: crates/gpu_scale/tests/macos_scale_roundtrip.rs
      provides: "scale_bgra_roundtrip + scale_cvpb_roundtrip + avvideosize_matches tests"
    - path: crates/gpu_scale/benches/scale_bgra.rs
      provides: "criterion bench: 4K → 1920 wide; gate at 3ms median"
    - path: crates/encoder/src/macos/vt_writer.rs
      provides: "scaler hook pre-append; AVVideoWidthKey/HeightKey aligned to post-scale dims"
  key_links:
    - from: "crates/encoder/src/pipeline.rs"
      to: "crates/gpu_scale/src/macos.rs"
      via: "probe_gpu_scaler() -> MetalScaler"
      pattern: "probe_gpu_scaler"
    - from: "crates/encoder/src/macos/vt_writer.rs"
      to: "MetalScaler::scale_to_cvpixelbuffer"
      via: "pre-append substitution in Cmd::Append branch"
      pattern: "scale_to_cvpixelbuffer"
    - from: "MPS input texture"
      to: "MPS output texture"
      via: "bgra8Unorm end-to-end (no sRGB)"
      pattern: "bgra8Unorm"
---

<objective>
Implement `MetalScaler` (macOS GPU downscale using MPSImageBilinearScale) and integrate it into the `VtWriter` AVAssetWriter append loop so the zero-copy SCK → Metal → AVAssetWriter path performs the 1920-wide downscale on the GPU. Resolves Open Question 4 (AVVideoWidthKey/HeightKey) test-first before writing integration code.

**Purpose:** Take the encoder's CPU `scale='min(1920,iw)':-2` work off libswscale on macOS. Keeps the VT fast path GPU end-to-end.
**Output:** Working `MetalScaler` + VtWriter integration + probe wired into pump init + AVVideoSize updated to post-scale dims + criterion bench gating 3 ms/frame + roundtrip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-CONTEXT.md
@.planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-RESEARCH.md
@.planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-01-PLAN.md
@crates/gpu_scale/src/lib.rs
@crates/gpu_scale/src/macos.rs
@crates/encoder/src/pipeline.rs
@crates/encoder/src/macos/vt_writer.rs
@crates/encoder/src/config.rs
@crates/capture/src/macos/raii.rs
@crates/capture/src/macos/sck_backend.rs

<interfaces>
Input/output CVPixelBuffer path (from RESEARCH.md §Pattern 3 + §Example 1):

- Input: `CVPixelBuffer` in `kCVPixelFormatType_32BGRA` (already emitted by SCK — `sck_backend.rs:201`).
- Output: NEW `CVPixelBuffer` in same pixel format, post-scale dims, retained +1 (ownership transfers to caller).
- Bridging: `CVMetalTextureCache` + `CVMetalTextureCacheCreateTextureFromImage`.
- Pitfall 1: BOTH input and output MTLTextures use `.bgra8Unorm` (NOT `_srgb`).
- Pitfall 3: `commandBuffer.waitUntilCompleted()` before returning (keeps IOSurface alive).

MPS type locked by D-13 / Pitfall 4: use `MPSImageBilinearScale` (macOS 10.13+). Do NOT use Lanczos variant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify AVAssetWriter accepts post-scale dims (Open Question 4 / A4 spike)</name>
  <files>
    crates/encoder/src/macos/vt_writer.rs,
    crates/encoder/src/macos/tests/avvideosize_accepts_scaled_dims.rs
  </files>
  <read_first>
    crates/encoder/src/macos/vt_writer.rs,
    .planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-RESEARCH.md
  </read_first>
  <behavior>
    - A small integration test constructs an AVAssetWriter with `AVVideoWidthKey/HeightKey` set to 1920×(scaled height), creates a synthetic 1920-wide CVPixelBuffer (BGRA), appends it, calls `finishWriting`, and asserts success with no error.
    - A second variant sets width keys to 3840 but appends a 1920-wide buffer — documents (via test assertion) whether AVAssetWriter errors or silently accepts. Drives the config-update decision below.
  </behavior>
  <action>
    1. Create `crates/encoder/src/macos/tests/avvideosize_accepts_scaled_dims.rs` (or a `#[cfg(test)] mod` inside `vt_writer.rs` if that's the crate convention — executor inspects existing test layout first).
    2. Test 1 `avvideosize_matches_buffer_dims_accepts`: constructs a minimal AVAssetWriter pointed at a `tempfile::NamedTempFile` `.mov`/`.mp4` path, passes `AVVideoWidthKey=1920, AVVideoHeightKey=1080`, creates a synthetic 1920×1080 BGRA `CVPixelBuffer` filled with a solid color, appends it at pts 0, calls `markAsFinished` + `finishWritingWithCompletionHandler` blocking via a semaphore (or async-to-sync helper). Assert the writer `status` is `Completed` and `error` is nil.
    3. Test 2 `avvideosize_mismatch_behavior`: same harness, but widthKey=3840, buffer is 1920. Append and finalize. Snapshot the resulting `status` + `error.localizedDescription()` into a test-scoped string assertion so the behavior is DOCUMENTED; do NOT assert-fail if mismatch raises an error (we EXPECT it to either succeed or produce a documented error). The test exists to answer A4.
    4. Wire the test to only run on macOS (`#[cfg(target_os = "macos")]`) and skip when TCC is not available (gate behind `#[ignore]` with a reason string if AVAssetWriter init requires an output file path the sandbox blocks — mirror existing vt_writer tests).
    5. Run locally. If the test reveals that AVAssetWriter REQUIRES width/height keys to match appended buffers, Task 3 below MUST update `EncodeConfig.width`/`height` to post-scale values before VtWriter construction. If it accepts mismatch, document that as an observation but still prefer updating for correctness of downstream metadata (`ffprobe` etc.).

    Commit:
    ```
    test(encoder): verify AVAssetWriter dimension contract (Phase 8 A4)

    Adds two tests around AVVideoWidthKey/HeightKey vs appended
    CVPixelBuffer dims, answering Open Question 4 from 08-RESEARCH.md
    before wiring the gpu_scale MetalScaler into VtWriter. Second test
    documents (not asserts) the error shape on mismatch so 08-02 Task 3
    can set EncodeConfig.width/height correctly.

    Refs: A4 in 08-RESEARCH.md.
    ```
  </action>
  <verify>
    <automated>cargo test -p encoder --features gpu-scale avvideosize_matches_buffer_dims_accepts</automated>
  </verify>
  <acceptance_criteria>
    - `crates/encoder/src/macos/tests/avvideosize_accepts_scaled_dims.rs` (or equivalent module path) exists.
    - `grep -rn 'avvideosize_matches_buffer_dims_accepts' crates/encoder/src/macos/` matches (use `-r` — the path is a directory).
    - On macOS: `cargo test -p encoder avvideosize_matches_buffer_dims_accepts` exits 0.
    - Test output comment or `assert_eq!` line documents the observed behavior for the mismatch case (visible in test source).
  </acceptance_criteria>
  <done>
    AVAssetWriter dimension contract is resolved by a test, not assumed. Task 3 has a deterministic rule for whether to update EncodeConfig.width/height.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MetalScaler (MPSImageBilinearScale + CVMetalTextureCache)</name>
  <files>
    crates/gpu_scale/Cargo.toml,
    crates/gpu_scale/src/lib.rs,
    crates/gpu_scale/src/macos.rs,
    crates/gpu_scale/src/composite.metal,
    crates/gpu_scale/tests/macos_scale_roundtrip.rs,
    crates/gpu_scale/benches/scale_bgra.rs
  </files>
  <read_first>
    crates/gpu_scale/src/lib.rs,
    crates/gpu_scale/src/macos.rs,
    crates/encoder/src/macos/vt_writer.rs,
    crates/capture/src/macos/raii.rs,
    crates/capture/src/macos/sck_backend.rs,
    .planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-RESEARCH.md
  </read_first>
  <behavior>
    - `MetalScaler::new()` constructs `MTLDevice` (system default), `MTLCommandQueue`, `MPSImageBilinearScale`, `CVMetalTextureCache`; returns `GpuScaleError::DeviceInit(_)` when `MTLCreateSystemDefaultDevice` is nil (headless CI).
    - `scale_to_bgra(ScaleInput::CvPixelBuffer(p), 1920, None)` where p is a 3840×2160 BGRA CVPixelBuffer returns `ScaledBgra { bytes, width: 1920, height: 1080 }` with `bytes.len() == 1920 * 1080 * 4`.
    - `scale_to_cvpixelbuffer(ScaleInput::CvPixelBuffer(p), 1920, None)` returns a retained `CVPixelBuffer` in `kCVPixelFormatType_32BGRA` with the same dims; caller is responsible for one `CFRelease` (documented).
    - No `_srgb` anywhere: `MTLPixelFormat::BGRA8Unorm` (NOT `BGRA8Unorm_sRGB`). Roundtrip test asserts output pixel format via `CVPixelBufferGetPixelFormatType`.
    - `probe_and_build(target_width)` now returns `Ok(Some(Box::new(MetalScaler::new()?)))` on macOS when construction succeeds; `Ok(None)` when `MTLCreateSystemDefaultDevice` is nil.
    - Criterion bench `scale_4k_to_1920` on a synthetic 3840×2160 BGRA CVPixelBuffer: median ≤ 3 ms/frame. Bench fails the build when median exceeds 3.0 ms.
  </behavior>
  <action>
    1. Replace the stub body of `crates/gpu_scale/src/macos.rs` with the `MetalScaler` struct per RESEARCH.md §Example 1, adjusted for the actual API surfaces exposed by `objc2-metal` 0.3.x and `objc2-metal-performance-shaders` 0.3.x. Key steps (CLAUDE.md "no workarounds" — if an API doesn't match docs, consult the docs.rs source, do not paper over):
       - Hold `device: Retained<ProtocolObject<dyn MTLDevice>>`, `queue: Retained<ProtocolObject<dyn MTLCommandQueue>>`, `mps: Retained<MPSImageBilinearScale>`, `cv_cache: CvMetalTextureCache` (a safe wrapper you write in the same file around `CVMetalTextureCacheRef`).
       - `new()` does `MTLCreateSystemDefaultDevice()` → `newCommandQueueWithLabel` → `MPSImageBilinearScale::initWithDevice` → `CVMetalTextureCacheCreate`. Any `nil`/error → `GpuScaleError::DeviceInit(desc)`.
       - For `ScaleInput::CvPixelBuffer(p)`:
         * Create input `MTLTexture` via `CVMetalTextureCacheCreateTextureFromImage` (format `MTLPixelFormat::BGRA8Unorm`, width/height from `CVPixelBufferGetWidth/Height`).
         * Compute `dst_h = ((src_h * target_w) / src_w) & !1` (preserve aspect, force even).
         * For `scale_to_bgra`: create a `MTLTexture` output (via `MTLDevice::newTextureWithDescriptor`, `bgra8Unorm`, `MTLTextureUsage::ShaderWrite | ShaderRead`); dispatch MPS; `commandBuffer.waitUntilCompleted` (Pitfall 3); then create a staging `Vec<u8>` sized `target_w * dst_h * 4` and `output.getBytes:bytesPerRow:fromRegion:mipmapLevel:` into it.
         * For `scale_to_cvpixelbuffer`: create an output `CVPixelBuffer` via `CVPixelBufferCreate` with `kCVPixelFormatType_32BGRA` + `kCVPixelBufferIOSurfacePropertiesKey` (empty dict) + `kCVPixelBufferMetalCompatibilityKey=true`; wrap it as an MTLTexture via the cache; dispatch MPS; `waitUntilCompleted`; return the CVPixelBuffer pointer with +1 retain (caller takes ownership).
       - For `ScaleInput::Bgra { bytes, width, height, stride }`: upload to a transient MTLTexture via `replaceRegion:mipmapLevel:withBytes:bytesPerRow:`, then same as the CV path for `scale_to_bgra`. For `scale_to_cvpixelbuffer`, same but with a fresh output CVPixelBuffer.
       - Cursor overlay (08-02 phase): if `cursor.is_some()`, return `GpuScaleError::Unsupported("fused cursor composite lands in 08-05")`. The scale path must work without cursor overlay in this plan.

    2. Create `crates/gpu_scale/src/composite.metal` containing ONLY a header comment + an empty `// plan 08-05 lands the fused scale+cursor kernel here` placeholder. File exists so 08-05's `include_str!` doesn't change file tree; content is bodiless for now.

    3. In `crates/gpu_scale/src/lib.rs`:
       - Replace the stub `probe_and_build` body on macOS:
         ```rust
         #[cfg(target_os = "macos")]
         {
             match macos::MetalScaler::new(target_width) {
                 Ok(scaler) => return Ok(Some(Box::new(scaler))),
                 Err(GpuScaleError::DeviceInit(reason)) => {
                     tracing::info!(target: "storycapture::gpu_scale",
                         %reason, "metal device unavailable; CPU fallback");
                     return Ok(None);
                 }
                 Err(e) => return Err(e),
             }
         }
         ```
       - `MetalScaler::new(target_width)` stores `target_width` for later bounds checks.
       - Keep the existing `#[cfg(target_os = "windows")]` branch returning `Ok(None)` (08-03 replaces it).
       - Linux: still `Ok(None)`.

    4. Tests: `crates/gpu_scale/tests/macos_scale_roundtrip.rs`, `#[cfg(target_os = "macos")]`:
       - `scale_bgra_roundtrip`: 640×480 checkerboard-gradient BGRA as `ScaleInput::Bgra` → 320-wide → assert `ScaledBgra.width == 320`, `height == 240`, `bytes.len() == 320 * 240 * 4`, and at least one pixel is non-zero (sanity).
       - `scale_cvpb_roundtrip`: Build a 640×480 BGRA CVPixelBuffer, scale to 320-wide via `scale_to_cvpixelbuffer`, assert `CVPixelBufferGetWidth == 320`, `Height == 240`, `PixelFormatType == kCVPixelFormatType_32BGRA`. Release the buffer once at end of test.
       - `no_srgb_texture_format`: Construct the scaler, dispatch `scale_to_bgra`, and assert via a debug counter field on `MetalScaler` (add `#[cfg(test)] pub(crate) last_output_pixel_format: u64`) that the output texture's `pixelFormat` is `MTLPixelFormat::BGRA8Unorm` (numeric value 80), NOT `BGRA8Unorm_sRGB` (value 81). Guards Pitfall 1.
       - `scaler_rejects_cursor_for_08_02`: `scale_to_bgra(..., target, Some(overlay))` returns `Err(GpuScaleError::Unsupported(_))` — locks the 08-02 contract until 08-05 delivers fused composite.

    5. Bench: `crates/gpu_scale/benches/scale_bgra.rs` using `criterion`:
       - Set up a 3840×2160 BGRA CVPixelBuffer filled with a gradient.
       - Bench group `scale_4k_to_1920`; measure `scaler.scale_to_cvpixelbuffer(cvpb_input, 1920, None)` per iteration.
       - After the bench block, inspect the generated `target/criterion/scale_4k_to_1920/new/estimates.json` and fail the bench binary with exit code 1 if `mean.point_estimate > 3_000_000.0` (3 ms in ns). Implement this as a post-bench assertion inside `main` (criterion's `cargo bench` doesn't have a native threshold API — add a small parser). This is D-07's build-time gate.

    6. Update `crates/gpu_scale/Cargo.toml` `[[bench]]` to now be real (source file exists); ensure `harness = false` and `[dev-dependencies]` includes `serde_json` if the estimates.json parsing uses it (prefer using a trivial regex over adding serde_json — no extra deps if avoidable).

    Commit:
    ```
    feat(gpu_scale): MetalScaler with MPSImageBilinearScale

    Implements the macOS GpuScaler impl using MPSImageBilinearScale for
    bilinear downscale. Supports CVPixelBuffer input (via
    CVMetalTextureCache, zero IOSurface copy) and packed BGRA input
    (via replaceRegion). Output path:
    - scale_to_bgra: GPU scale + getBytes readback to Vec<u8>
    - scale_to_cvpixelbuffer: GPU scale into a fresh CVPixelBuffer
      backed by IOSurface (retained +1; caller owns release)

    All textures pinned to MTLPixelFormat::BGRA8Unorm (never _sRGB) to
    avoid gamma-drift (Pitfall 1). waitUntilCompleted() enforced before
    return to keep IOSurface alive (Pitfall 3). Fused cursor-composite
    path returns Unsupported for now; lands in 08-05.

    Adds four roundtrip tests + a criterion bench with a 3ms build-time
    gate (D-07).

    Refs: D-01, D-02, D-06, D-07 in 08-CONTEXT.md.
    ```
  </action>
  <verify>
    <automated>cargo test -p gpu_scale --tests && cargo bench -p gpu_scale -- scale_4k_to_1920</automated>
  </verify>
  <acceptance_criteria>
    - `crates/gpu_scale/src/macos.rs` contains `impl GpuScaler for MetalScaler`.
    - `grep -c 'BGRA8Unorm_sRGB\|bgra8Unorm_srgb' crates/gpu_scale/src/macos.rs` returns 0.
    - `grep -c 'waitUntilCompleted\|wait_until_completed' crates/gpu_scale/src/macos.rs` returns ≥ 1.
    - `crates/gpu_scale/src/composite.metal` exists (even if bodiless).
    - `crates/gpu_scale/benches/scale_bgra.rs` exists.
    - `crates/gpu_scale/tests/macos_scale_roundtrip.rs` exists with tests `scale_bgra_roundtrip`, `scale_cvpb_roundtrip`, `no_srgb_texture_format`, `scaler_rejects_cursor_for_08_02`.
    - `cargo test -p gpu_scale --tests` exits 0 on macOS.
    - `cargo bench -p gpu_scale -- scale_4k_to_1920` exits 0 AND bench median reported ≤ 3 ms (D-07 gate).
    - `cargo build -p gpu_scale` on Windows/Linux still exits 0 (macos.rs is cfg-gated).
    - Commit message contains neither `Co-Authored-By` nor `Co-authored-by`.
  </acceptance_criteria>
  <done>
    MetalScaler ships, meets the 3 ms/frame budget at 4K60 → 1920 wide, and is reachable via `probe_and_build` on macOS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate MetalScaler into VtWriter append loop + update EncodeConfig dims</name>
  <files>
    crates/encoder/src/pipeline.rs,
    crates/encoder/src/macos/vt_writer.rs,
    crates/encoder/src/config.rs,
    crates/capture/src/macos/raii.rs
  </files>
  <read_first>
    crates/encoder/src/pipeline.rs,
    crates/encoder/src/macos/vt_writer.rs,
    crates/encoder/src/config.rs,
    crates/capture/src/macos/raii.rs,
    crates/gpu_scale/src/lib.rs,
    crates/gpu_scale/src/macos.rs,
    .planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-RESEARCH.md
  </read_first>
  <behavior>
    - On macOS with `gpu-scale` feature: the encoder pump probes `gpu_scale::probe_and_build(target_width=1920)` once before entering the frame loop, stores the `Option<Box<dyn GpuScaler>>` in pump-local state, and when Some:
      * Updates `EncodeConfig.width` and `EncodeConfig.height` to post-scale values (1920 × aspect-even-height).
      * Sets `EncodeConfig.upstream_scale_applied = true`.
    - `VtWriter::Cmd::Append` handler: when a scaler is available, calls `scaler.scale_to_cvpixelbuffer(ScaleInput::CvPixelBuffer(buf.as_ptr()), target_w, None)`, wraps the returned CVPixelBuffer via a NEW `CVPixelBufferHandle::from_raw_no_retain(...)` constructor (add this to `crates/capture/src/macos/raii.rs`), then passes it to `appendPixelBuffer_withPresentationTime`.
    - If `scaler.scale_to_cvpixelbuffer` returns `Err`: emit `RecordingEvent::GpuScaleFailed { reason: format!("{e}") }` ONCE (guard with a `failed_once: bool` flag on pump state), set the scaler to `None`, fall back to the original append-with-input-buffer path for this and all subsequent frames. Must NOT panic or abort the recording.
    - No `CVPixelBufferLockBaseAddress` is called on the input `buffer` in the scaler path. Assert via a test (Task 2 covered the scaler side; this task covers the integration).
  </behavior>
  <action>
    1. **Add `CVPixelBufferHandle::from_raw_no_retain`** to `crates/capture/src/macos/raii.rs`. Existing `CVPixelBufferHandle::retain(ptr)` does +1; the scaler returns an already-+1 pointer. The new constructor skips the extra retain:
       ```rust
       impl CVPixelBufferHandle {
           /// Wrap a CVPixelBuffer pointer that is already retained (+1).
           /// Ownership transfers — drop will CFRelease exactly once.
           ///
           /// # Safety
           /// `ptr` must be a non-null CVPixelBufferRef with refcount ≥ 1
           /// that the caller is transferring ownership of.
           pub unsafe fn from_raw_no_retain(ptr: *mut std::ffi::c_void) -> Option<Self> {
               if ptr.is_null() { None } else { Some(Self { inner: ptr }) }
           }
       }
       ```
       If the `inner` field is private and named differently, adapt accordingly; do NOT change the existing `retain` constructor.

    2. **Pump integration** in `crates/encoder/src/pipeline.rs`:
       - At the start of the pump (before the `while let Some(frame) = frames.recv().await` loop), compute `target_width = 1920u32;` and call `probe_gpu_scaler(target_width)` (from 08-01 Task 2).
       - Bind: `let mut gpu_scaler: Option<Box<dyn gpu_scale::GpuScaler>> = /* probe */;` guarded by `#[cfg(all(target_os = "macos", feature = "gpu-scale"))]`.
       - When `Some`:
         * Compute post-scale `(w, h)` from the first frame's dims (you may need to peek the first frame or use the known capture dims from `CaptureConfig`). If this is stateful, wait for the first frame, derive dims, update `cfg.width`/`cfg.height` BEFORE spawning VtWriter.
         * Set `cfg.upstream_scale_applied = true` before VtWriter init (because VtWriter reads `cfg` at construction time for `AVVideoWidthKey/HeightKey`).
       - When `None`: proceed exactly as today.

    3. **VtWriter hook** in `crates/encoder/src/macos/vt_writer.rs::Cmd::Append` handler (around line 265-290 per RESEARCH.md):
       - Wrap the append logic so that when `Some(scaler)` is threaded in via VtWriter construction:
         ```rust
         let pb_to_append: CVPixelBufferHandle = match self.gpu_scaler.as_mut() {
             Some(gs) => {
                 match gs.scale_to_cvpixelbuffer(
                     gpu_scale::ScaleInput::CvPixelBuffer(buffer.as_ptr()),
                     self.target_width,
                     None, // cursor fused in 08-05
                 ) {
                     Ok(scaled) => unsafe {
                         CVPixelBufferHandle::from_raw_no_retain(scaled.raw)
                             .expect("gpu_scale returned null CVPixelBuffer")
                     },
                     Err(e) => {
                         if !self.gpu_scale_failed_once {
                             let _ = self.event_tx.send(
                                 RecordingEvent::GpuScaleFailed {
                                     reason: format!("{e}"),
                                 },
                             );
                             self.gpu_scale_failed_once = true;
                         }
                         self.gpu_scaler = None;
                         buffer
                     }
                 }
             }
             None => buffer,
         };
         // Then: adaptor.appendPixelBuffer_withPresentationTime(&pb_to_append, cm_pts)
         ```
       - Add the new fields to `VtWriter`'s struct: `gpu_scaler: Option<Box<dyn gpu_scale::GpuScaler + Send>>`, `target_width: u32`, `gpu_scale_failed_once: bool`, and an `event_tx` channel handle if not already present (reuse the existing `RecordingEvent` sender; if unavailable inside VtWriter, pass it in via the constructor — do NOT invent a new channel).

    4. **Integration tests** (macOS only):
       - `vtwriter_gpu_scale_drives_avvideowidth_1920`: With `probe_and_build` returning Some, construct an encoder pipeline end-to-end on a 3840×2160 synthetic capture source (one frame suffices); assert the resulting `.mp4`'s ffprobe `width` == 1920 and `height` is the even-aspect value. Requires `ffmpeg`/`ffprobe` in PATH; skip with `#[ignore]` + reason if not present (follow existing `ffprobe_drift.rs` convention in `crates/encoder/tests/`).
       - `vtwriter_no_cvpb_lock_on_input`: Instrument `MetalScaler` (via a test-only atomic counter) to detect calls that would require `CVPixelBufferLockBaseAddress` on the INPUT buffer; assert count == 0 after processing 5 frames.
       - `gpu_scale_error_falls_back_to_cpu`: Inject a failing scaler (create a tiny test-only `FailingScaler` impl that returns `GpuScaleError::Dispatch("test".into())`), drive the pump through 2 frames, assert one `RecordingEvent::GpuScaleFailed` event arrives and the recording completes successfully with the original (unscaled) frames appended.

    Commit:
    ```
    feat(encoder): VtWriter uses MetalScaler pre-append (macOS zero-copy)

    The encoder pump now probes gpu_scale once before the frame loop. On
    macOS with the gpu-scale feature, a MetalScaler is built and threaded
    into VtWriter. VtWriter substitutes the input CVPixelBuffer with a
    fresh scaled one via MPSImageBilinearScale; the output CVPixelBuffer
    is passed directly into AVAssetWriterInputPixelBufferAdaptor.
    appendPixelBuffer, preserving the zero-copy IOSurface path end-to-end.

    EncodeConfig.width/height are updated to post-scale dims BEFORE
    VtWriter init so AVVideoWidthKey / AVVideoHeightKey match appended
    buffers (A4 resolved in Task 1). upstream_scale_applied = true
    removes `scale='min(1920,iw)':-2` from the CPU FFmpeg filter chain
    (08-01 groundwork).

    On scaler.scale_to_cvpixelbuffer error: single RecordingEvent::
    GpuScaleFailed telemetry, scaler reset to None, pump continues on
    the CPU path without interrupting the recording (CLAUDE.md no-
    workaround compliance: fallback is the designed behavior, not a
    silent failure).

    Adds CVPixelBufferHandle::from_raw_no_retain for +1-transfer wrapping.

    Refs: D-02, D-06, D-07, D-15 in 08-CONTEXT.md.
    ```
  </action>
  <verify>
    <automated>cargo test -p encoder --features gpu-scale vtwriter_gpu_scale_drives_avvideowidth_1920 vtwriter_no_cvpb_lock_on_input gpu_scale_error_falls_back_to_cpu</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'from_raw_no_retain' crates/capture/src/macos/raii.rs` matches.
    - `grep -n 'scale_to_cvpixelbuffer' crates/encoder/src/macos/vt_writer.rs` matches ≥ 1.
    - `grep -n 'upstream_scale_applied = true' crates/encoder/src/pipeline.rs` matches ≥ 1.
    - `grep -n 'GpuScaleFailed' crates/encoder/src/macos/vt_writer.rs` matches ≥ 1.
    - `grep -c 'CVPixelBufferLockBaseAddress' crates/encoder/src/macos/vt_writer.rs` returns 0 NEW occurrences in the scaler branch (existing unrelated locks OK).
    - `cargo test -p encoder --features gpu-scale vtwriter_gpu_scale_drives_avvideowidth_1920` exits 0 on macOS.
    - `cargo test -p encoder --features gpu-scale vtwriter_no_cvpb_lock_on_input` exits 0 on macOS.
    - `cargo test -p encoder --features gpu-scale gpu_scale_error_falls_back_to_cpu` exits 0 on macOS.
    - `cargo build --workspace --no-default-features` still exits 0 (Linux CI path preserved).
    - Commit message contains neither `Co-Authored-By` nor `Co-authored-by`.
  </acceptance_criteria>
  <done>
    Macos zero-copy VT path scales on Metal, produces scaled MP4s, falls back cleanly on dispatch failure, 3 ms/frame budget met.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p gpu_scale --tests` green on macOS.
- `cargo test -p encoder --features gpu-scale` green on macOS (including 3 integration tests).
- `cargo bench -p gpu_scale -- scale_4k_to_1920` median ≤ 3 ms.
- `cargo build --workspace` + `cargo build --workspace --no-default-features` green.
- Manual macOS operator walkthrough (added to VERIFICATION.md, not blocking code-complete per Phase 5/6 precedent): 60s 4K60 SCK-window record, verify MP4 width == 1920 via ffprobe, RAM ≤ 800 MB.
</verification>

<success_criteria>
- D-02 honored (VT fast path stays GPU end-to-end).
- D-06 honored (output is CVPixelBuffer on VT path; 08-03 delivers packed BGRA on FFmpeg path).
- D-07 honored (criterion bench gated at 3 ms).
- A4 resolved test-first (Task 1).
- Pitfall 1 (sRGB drift) and Pitfall 3 (IOSurface lifetime) addressed with code-level assertions / waitUntilCompleted.
- D-15 honored: `GpuScaleFailed` telemetry emitted once on error, recording continues.
</success_criteria>

<output>
After completion: `.planning/phases/08-recording-engine-polish-gpu-downscale-live-cursor-overlay-se/08-02-SUMMARY.md`.
</output>
