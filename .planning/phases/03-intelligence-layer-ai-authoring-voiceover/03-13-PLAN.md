---
phase: 03-intelligence-layer-ai-authoring-voiceover
plan: 13
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/intelligence/src/lsp/mod.rs
  - crates/intelligence/src/lsp/server.rs
  - crates/intelligence/src/lsp/diagnostics.rs
  - crates/intelligence/src/lsp/document.rs
  - crates/intelligence/tests/lsp_server_tests.rs
autonomous: true
requirements: [AI-06]
must_haves:
  truths:
    - "`tower-lsp::LanguageServer` impl over `crates/story-parser` pest grammar (D-16)"
    - "`did_open` / `did_change` triggers pest parse and `publish_diagnostics`"
    - "Hover returns verb catalog doc for identifier at cursor"
    - "Completion returns verb list + arg names filtered by prefix"
    - "Diagnostics include grammar errors AND undefined-verb / arg-arity semantic errors (D-17)"
  artifacts:
    - path: "crates/intelligence/src/lsp/server.rs"
      provides: "StoryLanguageServer impl of LanguageServer"
      contains: "impl LanguageServer for StoryLanguageServer"
    - path: "crates/intelligence/src/lsp/diagnostics.rs"
      provides: "pest ParseError → lsp_types::Diagnostic mapping + semantic checks"
      contains: "fn semantic_diagnostics"
  key_links:
    - from: "lsp/server.rs"
      to: "story_parser::parse_story"
      via: "shared pest grammar (D-16)"
      pattern: "story_parser::"
---

<objective>
Implement the in-process `tower-lsp` language server for `.story` files (D-16). Shares `crates/story-parser` directly — no stdio, no sidecar. This plan covers the core LSP surface: document tracking, grammar parse on change, diagnostic publish, hover, completion. The IPC bridge (NOT stdio) lands in Plan 14.

Purpose: ROADMAP Success Criteria #3 backbone (LSP hover + autocomplete + diagnostics).
Output: Compilable LanguageServer impl with in-memory test harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-intelligence-layer-ai-authoring-voiceover/03-AI-SPEC.md
@.planning/phases/03-intelligence-layer-ai-authoring-voiceover/03-CONTEXT.md

<interfaces>
```rust
use tower_lsp::{Client, LanguageServer, jsonrpc::Result as LspResult};
use tower_lsp::lsp_types::*;

pub struct StoryLanguageServer {
    client: Client,
    docs: DashMap<Url, Rope>,   // rope for incremental edits
}

#[tower_lsp::async_trait]
impl LanguageServer for StoryLanguageServer {
    async fn initialize(&self, _: InitializeParams) -> LspResult<InitializeResult> { ... }
    async fn did_open(&self, p: DidOpenTextDocumentParams) { ... }
    async fn did_change(&self, p: DidChangeTextDocumentParams) { ... }
    async fn did_close(&self, p: DidCloseTextDocumentParams) { ... }
    async fn hover(&self, p: HoverParams) -> LspResult<Option<Hover>> { ... }
    async fn completion(&self, p: CompletionParams) -> LspResult<Option<CompletionResponse>> { ... }
}
```

Capabilities declared in `initialize`:
- `text_document_sync: Some(TextDocumentSyncCapability::Kind(TextDocumentSyncKind::INCREMENTAL))`
- `hover_provider: Some(HoverProviderCapability::Simple(true))`
- `completion_provider: Some(CompletionOptions { trigger_characters: Some(vec![" ".into(), "\n".into()]), ..default() })`
- `diagnostic_provider: Some(DiagnosticServerCapabilities::Options(DiagnosticOptions { ... }))`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StoryLanguageServer impl with did_open/did_change + diagnostics publish</name>
  <files>crates/intelligence/src/lsp/mod.rs, crates/intelligence/src/lsp/server.rs, crates/intelligence/src/lsp/diagnostics.rs, crates/intelligence/src/lsp/document.rs</files>
  <read_first>
    - crates/story-parser (Phase 1 crate — parse_story signature + ParseError shape + span info per DSL-04 REQUIREMENTS)
    - .planning/phases/03-intelligence-layer-ai-authoring-voiceover/03-AI-SPEC.md §3 core imports for LSP (lines 231–241) + pitfall #3 (stdio warning)
    - .planning/phases/03-intelligence-layer-ai-authoring-voiceover/03-CONTEXT.md D-17 (diagnostic scope)
    - tower-lsp 0.20 docs (WebFetch https://docs.rs/tower-lsp/0.20 if unclear)
  </read_first>
  <behavior>
    - Test 1: `did_open` inserts doc in `docs`; subsequent `hover` at position in doc returns verb catalog content for `click` keyword.
    - Test 2: `did_change` with INCREMENTAL edits applies to rope; re-parse runs; mocked `Client::publish_diagnostics` receives updated Diagnostic[].
    - Test 3: `did_close` removes doc from map; hover on stale URL returns None.
    - Test 4: pest parse failure at line 3 col 5 produces `Diagnostic { range: {start:{line:2,character:4},...}, severity: DiagnosticSeverity::ERROR, ... }`.
    - Test 5: undefined verb `teleport` produces a semantic diagnostic with severity WARNING and message `"Unknown verb 'teleport'. Did you mean 'type'? ..."` (Levenshtein suggestion from DSL-05 inherited from Phase 1 parser).
    - Test 6: completion at cursor after `cl` returns `click` as a CompletionItem with `kind: CompletionItemKind::KEYWORD`.
  </behavior>
  <action>
Add to `crates/intelligence/Cargo.toml` deps: `dashmap = "6"`, `ropey = "1.6"`.

Create `crates/intelligence/src/lsp/mod.rs`:
```rust
pub mod server;
pub mod diagnostics;
pub mod document;
pub use server::StoryLanguageServer;
```

Create `crates/intelligence/src/lsp/document.rs` — Rope wrapper + utility for applying `TextDocumentContentChangeEvent` edits to a `ropey::Rope`. Include `byte_offset_to_position` and `position_to_byte_offset` helpers (needed for hover/completion).

Create `crates/intelligence/src/lsp/diagnostics.rs`:
- `fn grammar_diagnostics(source: &str) -> Vec<Diagnostic>` — runs `story_parser::parse_story(source)`; on error, maps span → `Range` → `Diagnostic { severity: ERROR, ... }`.
- `fn semantic_diagnostics(source: &str) -> Vec<Diagnostic>` — checks undefined verbs (WARNING severity, `code: "unknown-verb"`), missing required args per verb (WARNING), uses `VERB_CATALOG` from `crates/intelligence/src/nl/verb_whitelist.rs`.
- `fn diagnose(source: &str) -> Vec<Diagnostic>` — concatenation.

Create `crates/intelligence/src/lsp/server.rs`:
- `StoryLanguageServer { client, docs: DashMap<Url, Rope> }`.
- Implement the six methods from `<interfaces>`.
- `did_change`: apply edits to rope, call `diagnose(&rope.to_string())`, `self.client.publish_diagnostics(uri, diags, None).await`.
- `hover`: find identifier at position via rope; match against `VERB_CATALOG_MD` entries; return `Hover { contents: HoverContents::Markup(MarkupContent { kind: Markdown, value: doc }), range: Some(ident_range) }`.
- `completion`: gather verb list; filter by prefix if present; return `CompletionResponse::Array(items)`.

Test harness in `lsp_server_tests.rs`: wrap `StoryLanguageServer` with a fake `Client` (build via `LspService::build(StoryLanguageServer::new)` → gives you a `MessageStream`; pump request/responses through it OR inject a mock client trait). Use tower-lsp's `LspService::new(|client| StoryLanguageServer::new(client))` to construct; test via direct method calls on the server impl OR via the service layer.
  </action>
  <verify>
    <automated>cargo test -p intelligence --test lsp_server_tests</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests green
    - `grep -c "publish_diagnostics" crates/intelligence/src/lsp/server.rs` ≥ 1
    - `grep -c "CompletionResponse\\|CompletionItem" crates/intelligence/src/lsp/server.rs` ≥ 1
    - `grep -c "story_parser" crates/intelligence/src/lsp/diagnostics.rs` ≥ 1
    - `grep -c "Did you mean\\|did_you_mean" crates/intelligence/src/lsp/diagnostics.rs` ≥ 1 (Levenshtein suggestion)
  </acceptance_criteria>
  <done>LanguageServer impl tested in-process.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-edited `.story` text → pest parser | Parser must handle any byte sequence safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13-01 | Denial of Service | Pathological grammar input | mitigate | pest parser is O(n) per rule; document size capped (typically <20KB per AI-SPEC); panic-mode recovery from DSL-06 caps multi-error fan-out |
| T-03-13-02 | Tampering | Multi-byte UTF-8 in Rope offsets | mitigate | Use `ropey::Rope::byte_to_char` + `char_to_byte` — handles multi-byte correctly; never slice by byte offsets directly |
| T-03-13-03 | Information Disclosure | Diagnostic text echoing secrets from source | accept | `.story` files are user-authored and user-visible; diagnostics only quote source spans visible to the same user. No new disclosure. |
</threat_model>

<verification>
- All 6 tests exit 0
- Manual smoke: create a test doc, fuzz edits, observe diagnostic updates
</verification>

<success_criteria>
- LanguageServer impl complete
- Shared grammar import works (no drift vs. parser crate)
- Diagnostics span-accurate
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-layer-ai-authoring-voiceover/03-13-SUMMARY.md`.
</output>
