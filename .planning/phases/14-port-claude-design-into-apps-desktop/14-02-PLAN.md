---
phase: 14-port-claude-design-into-apps-desktop
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src/lib/platform.ts
  - apps/desktop/src/components/sc-shell/sc-title-bar.tsx
  - apps/desktop/src/components/sc-shell/sc-side-nav.tsx
  - apps/desktop/src/components/sc-shell/sc-shell.tsx
  - apps/desktop/src/components/sc-shell/index.ts
  - apps/desktop/src/components/title-bar.tsx
  - apps/desktop/src/components/sidebar.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/main.tsx
  - apps/desktop/src/router.tsx
autonomous: false
requirements: [D-03, D-06a]
must_haves:
  truths:
    - "Main window has decorations: false and renders custom chrome on macOS and Windows"
    - "macOS shows traffic-light cluster; clicks minimize/maximize/close the window"
    - "Windows shows caption buttons; clicks minimize/maximize/close the window"
    - "data-tauri-drag-region on outer titlebar lets user drag the window"
    - "Side nav links to Dashboard/Editor/Post-Production/Recorder/Settings"
    - "Every route renders inside the new ScShell layout"
    - "ScSideNav carries a TRANSIENT theme-toggle button (bottom of nav) until Wave 5 when Settings→Appearance lands and removes it"
  artifacts:
    - path: "apps/desktop/src-tauri/tauri.conf.json"
      provides: "decorations: false + titleBarStyle: Overlay + hiddenTitle: true"
    - path: "apps/desktop/src/components/sc-shell/sc-title-bar.tsx"
      provides: "Platform-aware titlebar component"
    - path: "apps/desktop/src/components/sc-shell/sc-shell.tsx"
      provides: "Single layout that wraps all non-overlay routes"
  key_links:
    - from: "apps/desktop/src/main.tsx"
      to: "html[data-platform]"
      via: "@tauri-apps/plugin-os platform() before render"
      pattern: "platform\\(\\)"
    - from: "sc-title-bar.tsx"
      to: "@tauri-apps/api/window getCurrentWindow"
      via: "onClick handlers"
      pattern: "getCurrentWindow"
---

<objective>
Wave 2 — Chrome. Flip Tauri decorations off, port chrome.jsx into ScTitleBar + ScSideNav + ScShell, wire platform detection at boot, connect window-control buttons to @tauri-apps/api/window. Delete old title-bar.tsx and sidebar.tsx in the same commit that introduces the new shell (D-10 big-bang).

Purpose: Every route renders inside the new shell starting this wave.
Output: App shell with custom chrome on macOS + Windows, side-nav links to all existing routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-RESEARCH.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-PATTERNS.md
@.planning/design/storycapture-claude-design/project/components/chrome.jsx
@apps/desktop/src/components/title-bar.tsx
@apps/desktop/src/components/sidebar.tsx
@apps/desktop/src/router.tsx
@apps/desktop/src-tauri/tauri.conf.json
@apps/desktop/src/main.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tauri config flip + platform boot probe</name>
  <read_first>
    - apps/desktop/src-tauri/tauri.conf.json main window block (lines 14-27)
    - apps/desktop/src/main.tsx current bootstrap (lines 19-28)
    - RESEARCH.md §Code Example 1 (exact config) + §Pattern 2 (platform probe)
    - RESEARCH.md Pitfall 2 + Pitfall 3 (Windows resize, macOS traffic lights)
  </read_first>
  <action>
    1) Modify apps/desktop/src-tauri/tauri.conf.json main window ONLY (label="main"):
       Set `"decorations": false`, `"titleBarStyle": "Overlay"`, `"hiddenTitle": true`. Keep width/height/minWidth/minHeight/resizable/acceptFirstMouse/macOSPrivateApi unchanged. Do NOT touch region-overlay window.
    2) Create apps/desktop/src/lib/platform.ts:
       ```ts
       import { platform as osPlatform } from "@tauri-apps/plugin-os";
       export type AppPlatform = "mac" | "win";
       export async function detectPlatform(): Promise<AppPlatform> {
         const p = await osPlatform();
         return p === "macos" ? "mac" : "win";
       }
       export function applyPlatform(p: AppPlatform) {
         document.documentElement.dataset.platform = p;
       }
       ```
    3) Modify apps/desktop/src/main.tsx bootstrap — insert platform probe BEFORE `initOutputPrefs`:
       ```ts
       const plat = await detectPlatform();
       applyPlatform(plat);
       await initOutputPrefs();
       ```
       Import `detectPlatform, applyPlatform` from "./lib/platform".
    Per D-03 (custom chrome + platform-driven via plugin-os).
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && grep -q '"decorations": false' apps/desktop/src-tauri/tauri.conf.json && grep -q '"hiddenTitle": true' apps/desktop/src-tauri/tauri.conf.json</automated>
  </verify>
  <done>
    - tauri.conf.json main window has decorations:false, titleBarStyle:Overlay, hiddenTitle:true
    - apps/desktop/src/lib/platform.ts exists and exports detectPlatform + applyPlatform
    - main.tsx calls detectPlatform+applyPlatform before initOutputPrefs
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScTitleBar + ScSideNav + ScShell (port chrome.jsx)</name>
  <read_first>
    - .planning/design/storycapture-claude-design/project/components/chrome.jsx (authoritative visual + behavior source)
    - apps/desktop/src/components/title-bar.tsx (current AppLayout/FullscreenLayout — being replaced)
    - apps/desktop/src/components/sidebar.tsx (current sidebar — being replaced; lines 1-50 for NAV_ITEMS shape, lines 105-175 for active-state)
    - apps/desktop/src/router.tsx (route paths for nav wiring: /, /editor, /post-production, /recorder, /settings)
    - RESEARCH.md §Example 2 (window controls), §Pattern 4 (data-tauri-drag-region)
    - RESEARCH.md Pitfalls 3, 4
    - PATTERNS.md §sc-title-bar + §sc-side-nav
  </read_first>
  <behavior>
    - ScTitleBar with platform="mac" renders 3 circular buttons (red/yellow/green), each with onClick that calls the correct window API
    - ScTitleBar with platform="win" renders 3 rectangular caption buttons (min/max/close)
    - Clicking minimize calls getCurrentWindow().minimize()
    - Clicking max toggles via getCurrentWindow().toggleMaximize()
    - Clicking close calls getCurrentWindow().close()
    - Outer <div> has data-tauri-drag-region attribute
    - Interactive children do NOT have data-tauri-drag-region
    - ScSideNav renders links for each route; active route has aria-current="page"
    - ScShell composes ScTitleBar + ScSideNav + <Outlet/>
  </behavior>
  <action>
    1) Create apps/desktop/src/components/sc-shell/sc-title-bar.tsx. Import `getCurrentWindow` from "@tauri-apps/api/window" at module top (not in handler closures — but only call inside handlers, never at module top-level per RESEARCH Anti-Patterns). Use component-local derivation: `const win = getCurrentWindow()` inside component body, handlers `onMin = () => void win.minimize()`, `onMax = () => void win.toggleMaximize()`, `onClose = () => void win.close()`. Read platform from `document.documentElement.dataset.platform` lazily via a small hook `usePlatform()` in lib/platform.ts, OR accept `platform?: "mac" | "win"` prop (prefer prop for testability; default to dataset read). Render:
       - Outer: `<div data-tauri-drag-region className="sc-titlebar" data-platform={platform}>`
       - macOS: 3 `<button aria-label="close|minimize|zoom">` in traffic-light cluster on left
       - Windows: 3 `<button aria-label="minimize|maximize|close">` on right
       - Center: brand + subtitle (non-interactive; inherits drag via parent)
       Classes drawn from claude-design/app.css `.sc-titlebar`, `.sc-traffic`, `.sc-caption`.
    2) Create apps/desktop/src/components/sc-shell/sc-side-nav.tsx. Port sidebar.tsx NAV_ITEMS array verbatim (Dashboard /, Editor /editor, Post-Production /post-production, Recorder /recorder, Settings /settings) — preserve contextual matchPattern logic. Use lucide-react icons (Home, FileText, Film, Video, Settings). Apply `.sc-sidenav` + `.sc-sidenav-item` + `.sc-sidenav-item.active` classes. Use `NavLink` from react-router-dom for active state; set `aria-current="page"` when active.
       **TRANSIENT THEME TOGGLE** (Wave 2 ↔ Wave 5 bridge): At the bottom of ScSideNav (below nav items), render a small icon button that calls the existing `toggleTheme()` from `apps/desktop/src/lib/theme.ts` (current localStorage-backed impl — do NOT rewrite in this wave). Use lucide-react `Sun`/`Moon` icons, `aria-label="Toggle theme"`. Immediately above the button, add the marker comment EXACTLY as follows (grep anchor):
       ```tsx
       // TRANSIENT: theme-toggle lives in ScSideNav until Wave 5 Settings→Appearance lands — remove in 14-05 Task 2
       <button aria-label="Toggle theme" className="sc-sidenav-theme-toggle" onClick={() => toggleTheme()}>
         {theme === "dark" ? <Sun size={14} /> : <Moon size={14} />}
       </button>
       ```
       This is the ONLY user-facing theme switch during Waves 2–4 (the old sidebar toggle is deleted with sidebar.tsx in this wave).
    3) Create apps/desktop/src/components/sc-shell/sc-shell.tsx:
       ```tsx
       import { Outlet } from "react-router-dom";
       export function ScShell() {
         return (
           <div className="sc-app">
             <ScTitleBar />
             <div className="sc-body">
               <ScSideNav />
               <main id="main-content" className="sc-main"><Outlet /></main>
             </div>
           </div>
         );
       }
       ```
    4) Create apps/desktop/src/components/sc-shell/index.ts barrel.
    5) Modify apps/desktop/src/router.tsx: replace existing AppLayout wrapper with `element: <ScShell />`. Keep all child route paths.
    6) DELETE apps/desktop/src/components/title-bar.tsx and apps/desktop/src/components/sidebar.tsx (D-10 same-commit deletion). Grep for any remaining imports of those modules and redirect them (likely zero since router is the only consumer).
    7) Tests:
       - apps/desktop/src/components/sc-shell/__tests__/sc-title-bar.test.tsx: mock @tauri-apps/api/window (`vi.mock("@tauri-apps/api/window", () => ({ getCurrentWindow: () => ({ minimize: vi.fn(), toggleMaximize: vi.fn(), close: vi.fn() }) }))`). Render `<ScTitleBar platform="mac" />`, click each traffic light, assert corresponding mock called. Repeat for `platform="win"`.
       - apps/desktop/src/components/sc-shell/__tests__/sc-side-nav.test.tsx: render inside MemoryRouter at `/editor`, assert the Editor NavLink has `aria-current="page"`.
    Per D-03 + D-06a + RESEARCH Pitfalls 3-4.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop test -- components/sc-shell</automated>
  </verify>
  <done>
    - sc-title-bar.tsx + sc-side-nav.tsx + sc-shell.tsx exist
    - title-bar.tsx + sidebar.tsx deleted
    - Router renders ScShell as root layout
    - Tests prove window-control clicks invoke correct @tauri-apps/api/window methods
    - Tests prove side-nav active state
    - Transient theme-toggle button present in sc-side-nav.tsx: `rg "TRANSIENT: theme-toggle lives in ScSideNav" apps/desktop/src/components/sc-shell/sc-side-nav.tsx` returns 1 match, AND `rg "sc-sidenav-theme-toggle" apps/desktop/src/components/sc-shell/sc-side-nav.tsx` returns ≥1 match
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Windows + macOS chrome QA</name>
  <what-built>
    Custom Tauri titlebar (traffic lights on macOS, caption buttons on Windows), side-nav, data-tauri-drag-region on outer titlebar.
  </what-built>
  <how-to-verify>
    On macOS:
    1. `pnpm tauri:dev` from repo root
    2. App opens without native decorations
    3. Traffic lights render top-left in StoryCapture red/yellow/green style
    4. Click red → window closes; reopen; click yellow → window minimizes; click green → window maximizes
    5. Drag empty area of titlebar → window moves
    6. Resize window by dragging bottom-right corner — verify resize works
    On Windows (separate QA pass — RESEARCH Pitfall 2):
    1. Build or run on Windows
    2. App opens without native decorations
    3. Caption buttons (min/max/close) render top-right
    4. Each works
    5. Drag titlebar area → window moves
    6. **Resize from every edge + corner** — RESEARCH Pitfall 2 warns Windows may lose resize. If broken, add `tauri-plugin-decorum` or a 4px CSS inset resize border as documented in Pitfall 2.
  </how-to-verify>
  <acceptance_criteria>
    - macOS: traffic-light clicks close/minimize/zoom the window (NOT just visual)
    - Windows: caption-button clicks close/minimize/maximize the window
    - Drag works on both OSes (data-tauri-drag-region correct)
    - **Windows resize works from every edge** — if it does not, Pitfall 2 mitigation is required before marking this task done
    - Side-nav routes navigate correctly when clicked
  </acceptance_criteria>
  <resume-signal>Type "approved" once macOS + Windows QA pass. If Windows resize broken, describe the broken edges/corners and the planner will revise the plan to add resize-border mitigation.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @storycapture/desktop test` green
- Manual macOS + Windows smoke passes Task 3 checklist
- `rg "title-bar\.tsx|sidebar\.tsx" apps/desktop/src` returns only the NEW sc-shell files (no references to deleted old ones)
</verification>

<success_criteria>
Custom chrome shipping on both OSes. All existing routes render inside ScShell. Behavior preserved (D-09) — no feature removed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-port-claude-design-into-apps-desktop/14-02-SUMMARY.md`.
</output>
