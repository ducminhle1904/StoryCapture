---
phase: 14-port-claude-design-into-apps-desktop
plan: 04
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - apps/desktop/src/components/command-palette/command-palette.tsx
  - apps/desktop/src/components/command-palette/index.ts
  - apps/desktop/src/components/recording-indicator.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/features/post-production/export-modal/export-modal.tsx
autonomous: true
requirements: [D-06b, D-06c, D-06d, D-07]
must_haves:
  truths:
    - "Cmd/Ctrl+K opens CommandPalette; Esc closes; arrow keys navigate; Enter routes"
    - "Sonner toasts render with sc-* CSS variables (--normal-bg etc.) pulling from --sc-surface / --sc-text"
    - "RecordingIndicator shows only while recorder Zustand state is active; hides otherwise"
    - "Export modal preserves all Phase 13 behavior (ENC-12..ENC-19): AdvancedOutputOptions, EncoderOptionsDto, useOutputPrefsStore, exportRun, exportValidateConfig, HW_UI_TO_DTO, buildEncoderOptions, AiDisclosureModal"
    - "Export modal looks like claude-design/project/screens/export.jsx"
    - "Overlay mount point is apps/desktop/src/App.tsx (legacy chrome retained — D-03/D-06a dropped)"
  artifacts:
    - path: "apps/desktop/src/components/command-palette/command-palette.tsx"
      provides: "cmdk+Base UI Dialog command palette"
    - path: "apps/desktop/src/components/recording-indicator.tsx"
      provides: "Floating recording badge driven by recorder store"
    - path: "apps/desktop/src/features/post-production/export-modal/export-modal.tsx"
      provides: "Restyled export modal (Phase 13 wiring intact)"
  key_links:
    - from: "App.tsx Toaster"
      to: "--sc-* CSS variables"
      via: "style prop with --normal-bg: var(--sc-surface)"
      pattern: "--normal-bg.*--sc-"
    - from: "App.tsx"
      to: "<CommandPalette /> + <RecordingIndicator />"
      via: "mounted as siblings to <RouterProvider/>"
      pattern: "CommandPalette|RecordingIndicator"
    - from: "export-modal.tsx"
      to: "useOutputPrefsStore + exportRun + AdvancedOutputOptions"
      via: "preserved imports"
      pattern: "useOutputPrefsStore|AdvancedOutputOptions|exportRun"
---

<objective>
Wave 4 — Overlays + Export. Port CommandPalette (D-06b), RecordingIndicator (D-06d), skin Sonner via CSS vars to replicate ToastStack (D-06c), restyle the Phase 13 export modal (D-07) — all while preserving every bit of Phase 13 ENC-12..ENC-19 wiring. Overlays mount into the existing `apps/desktop/src/App.tsx` (D-03/D-06a dropped — no ScShell exists).

Purpose: Overlays shipping + Export modal matching mocks.
Output: Full visual overlay suite + restyled export modal with 100% Phase 13 behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-RESEARCH.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-PATTERNS.md
@.planning/design/storycapture-claude-design/project/components/overlays.jsx
@.planning/design/storycapture-claude-design/project/screens/export.jsx
@apps/desktop/src/App.tsx
@apps/desktop/src/features/post-production/export-modal/export-modal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CommandPalette (cmdk + Base UI Dialog + motion)</name>
  <read_first>
    - .planning/design/storycapture-claude-design/project/components/overlays.jsx (CommandPalette visual reference)
    - apps/desktop/src/router.tsx (list of routes for palette entries: /, /editor, /post-production, /recorder, /settings)
    - apps/desktop/src/App.tsx (mount target — sibling to `<RouterProvider/>` and `<Toaster/>`)
    - apps/desktop/src/features/post-production/export-modal/export-modal.tsx (Base UI Dialog pattern analog)
    - RESEARCH.md §Don't Hand-Roll (use cmdk + Base UI Dialog, not custom)
    - RESEARCH.md §Assumption A3 + Pitfall 7 (mod+k is clean)
    - PATTERNS.md §command-palette.tsx
  </read_first>
  <behavior>
    - Hotkey mod+k toggles palette open
    - Esc closes
    - Arrow up/down navigates items
    - Enter activates selected item
    - Each item, when activated, navigates via useNavigate to the route path
    - When closed, palette unmounts (AnimatePresence)
    - Focus trap inside palette while open
  </behavior>
  <action>
    1) Create apps/desktop/src/components/command-palette/command-palette.tsx:
       - Compose Base UI Dialog.Root (focus trap + esc close + scroll lock) as outer shell.
       - Inside Dialog.Popup, render cmdk `<Command>` with `<Command.Input placeholder="Search…" />`, `<Command.List>`, `<Command.Group heading="Navigate">` containing `<Command.Item onSelect={() => { navigate(path); close(); }}>` entries for each route (Dashboard/Editor/Post-Production/Recorder/Settings), `<Command.Empty>No results.</Command.Empty>`.
       - Apply `.sc-palette` class on outer wrapper; styling from claude-design/app.css CommandPalette rules.
       - Animate with motion/react `<AnimatePresence>` and `<motion.div>` inside Dialog.Popup: fade-in `{opacity:0, y:4} → {opacity:1, y:0}` at 180ms per RESEARCH Claude's Discretion.
       - Register mod+k via react-hotkeys-hook `useHotkeys("mod+k", (e) => { e.preventDefault(); setOpen(o => !o); })`, scope to window.
       - Export a `<CommandPalette />` component with no props; own internal open state via a Zustand mini-store `commandPaletteStore` or internal useState. Since there is no ScShell (D-03/D-06a dropped), the palette uses a self-contained hook + internal state — no shell-level provider needed.
    2) Mount `<CommandPalette />` inside apps/desktop/src/App.tsx as a sibling to `<RouterProvider />` and `<Toaster />` so it is available on every route (legacy chrome — AppLayout / title-bar / sidebar — stays unchanged).
    3) Tests at apps/desktop/src/components/command-palette/__tests__/command-palette.test.tsx:
       - render inside MemoryRouter, press `{Meta>}k`, assert `<Command.Input>` is in DOM
       - press `{Escape}`, assert palette closed
       - type "edit", press `{Enter}`, assert navigation to /editor (mock useNavigate)
    Per D-06b, RESEARCH Don't Hand-Roll.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop test -- components/command-palette</automated>
  </verify>
  <done>
    - command-palette.tsx exists using cmdk + Base UI Dialog + motion
    - Mounted in apps/desktop/src/App.tsx (not any sc-shell file — D-03/D-06a dropped)
    - All 3 behavior tests pass
    - No collision with existing mod+k hotkey (grep `useHotkeys\("mod\+k"` returns only this file)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RecordingIndicator + Sonner CSS-var skin</name>
  <read_first>
    - .planning/design/storycapture-claude-design/project/components/overlays.jsx (RecordingIndicator + ToastStack reference)
    - apps/desktop/src/App.tsx (current `<Toaster>` position+theme — mount target for RecordingIndicator too)
    - apps/desktop/src/features/recorder/** — find the recorder Zustand store selector that exposes `isRecording` (likely `useRecorderStore` or equivalent). Grep `isRecording` to locate.
    - apps/desktop/src/components/status-bar.tsx (existing status pill analog per PATTERNS)
    - RESEARCH.md §Pattern 3 (sonner CSS-var skin) + Pitfall 8 (theme prop conflict)
    - PATTERNS.md §recording-indicator.tsx + §App.tsx
  </read_first>
  <action>
    1) Create apps/desktop/src/components/recording-indicator.tsx:
       - Subscribe to the recorder store's `isRecording` flag (grep to confirm actual hook name; likely `useRecorderStore(s => s.isRecording)`).
       - When true, render `<motion.div className="sc-rec-indicator" initial={{opacity:0, y:8}} animate={{opacity:1, y:0}} exit={{opacity:0, y:8}}>` containing red pulsing dot + "Recording" text + elapsed time (if store exposes it; otherwise just "Recording").
       - Wrap in `<AnimatePresence>` so the indicator exits cleanly.
       - Position bottom-right (via .sc-rec-indicator class; fallback inline style `position:fixed; bottom:16px; right:16px; z-index:50`).
       - Use ScBadge for the status chip composition if the visual matches; otherwise raw `.sc-rec-indicator` class from claude-design app.css.
    2) Mount `<RecordingIndicator />` in apps/desktop/src/App.tsx as a sibling to `<RouterProvider />`, `<Toaster />`, and `<CommandPalette />` (legacy chrome stays unchanged — D-03/D-06a dropped).
    3) Modify apps/desktop/src/App.tsx `<Toaster>`:
       ```tsx
       // NOTE: tweaks-store lands in Wave 5; for Wave 4 hard-code theme="dark"
       <Toaster
         position="bottom-left"
         theme="dark"
         style={{
           "--normal-bg": "var(--sc-surface)",
           "--normal-text": "var(--sc-text)",
           "--normal-border": "var(--sc-border-2)",
           "--border-radius": "var(--sc-r-lg)",
           "--toast-animation-duration": "200ms",
         } as React.CSSProperties}
       />
       ```
       Since tweaks-store lands in Wave 5, for Wave 4 hard-code `theme="dark"`; Wave 5 will replace with `useTweaksStore(s => s.theme)`.
    4) Remove `richColors` prop (Sonner's default dark palette overrides our skin per RESEARCH Pitfall 8).
    Per D-06c, D-06d, RESEARCH Patterns + Pitfalls.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- components/recording-indicator 2>&1 | tail -20 || true</automated>
  </verify>
  <done>
    - recording-indicator.tsx mounted in apps/desktop/src/App.tsx; reads recorder store
    - App.tsx Toaster applies 5 CSS vars for sc-* skin
    - Visual smoke: trigger any toast from app; verify background is dark `--sc-surface`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Export modal visual restyle (Phase 13 wiring preserved)</name>
  <read_first>
    - apps/desktop/src/features/post-production/export-modal/export-modal.tsx (FULL file — this is the target)
    - apps/desktop/src/features/post-production/export-modal/advanced-output-options.tsx
    - apps/desktop/src/features/post-production/export-modal/format-checkboxes.tsx
    - apps/desktop/src/features/post-production/export-modal/resolution-picker.tsx
    - apps/desktop/src/state/output-prefs.ts (DO NOT MODIFY — read only to understand)
    - apps/desktop/src/ipc/export.ts (DO NOT MODIFY)
    - .planning/design/storycapture-claude-design/project/screens/export.jsx (visual reference)
    - PATTERNS.md §export-modal.tsx RESTYLE (lists the exact imports to preserve verbatim)
    - CONTEXT.md D-07 (ENC-12..ENC-19 must continue to pass)
  </read_first>
  <action>
    1) In apps/desktop/src/features/post-production/export-modal/export-modal.tsx:
       - Preserve VERBATIM every import in PATTERNS §export-modal.tsx: `Dialog` from `@base-ui-components/react/dialog`, `EncoderOptionsDto`, `HardwareEncoderDto`, `invoke`, `toast`, `Accordion*`, `AiDisclosureModal`, `useVoiceoverStore`, `ExportOutput`, `exportRun`, `exportValidateConfig`, `ExportKnobs`, `useOutputPrefsStore`, `AdvancedOutputOptions`, `FormatCheckboxes`, `ResolutionPicker`. Also preserve `HW_UI_TO_DTO` and `buildEncoderOptions` helpers (grep to confirm location).
       - Swap `<Button>` instances → `<ScButton variant="primary|ghost|default">` per the claude-design export.jsx button styling.
       - Retoken Dialog.Popup surfaces from `--color-surface-*` to `--sc-surface*`, borders to `--sc-border-2`.
       - Apply `.sc-dialog` / `.sc-dialog-header` / `.sc-dialog-body` / `.sc-dialog-footer` classes from claude-design app.css if present; otherwise use inline classes matching the mock visual.
       - Keep the AdvancedOutputOptions disclosure (it's a child component — DO NOT modify that file; just retoken its trigger row inside this modal).
    2) **Do NOT modify** advanced-output-options.tsx, format-checkboxes.tsx, resolution-picker.tsx, output-prefs.ts, ipc/export.ts. These contain Phase 13 logic (ENC-12..ENC-19). If a child component's chrome looks wrong, accept it this wave — a follow-up sub-phase can restyle children.
    3) Verify the Phase 13 test suite passes verbatim after restyle: `pnpm --filter @storycapture/desktop test -- features/post-production/export-modal` and `features/post-production/__tests__/export-modal.test.tsx`.
    Per D-07, D-09.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop test -- features/post-production 2>&1 | tail -60</automated>
  </verify>
  <done>
    - export-modal.tsx retokens outer Dialog surfaces to sc-*
    - Every import in PATTERNS §export-modal.tsx preserved verbatim (grep confirms)
    - advanced-output-options.tsx / format-checkboxes.tsx / resolution-picker.tsx / output-prefs.ts unchanged (git diff shows no touch)
    - ENC-12..ENC-19 test suite (export-modal.test.tsx) green
  </done>
</task>

</tasks>

<verification>
- Full `pnpm --filter @storycapture/desktop test` green
- Manual: mod+k opens palette; navigate to Editor works
- Manual: start recording → floating indicator appears; stop → disappears
- Manual: open export modal → visual matches claude-design/screens/export.jsx; all Advanced options still work; exporting a clip still produces a file (ENC-12..ENC-19 regression check)
- `rg "sc-shell" apps/desktop/src` returns zero (no ScShell — D-03/D-06a dropped)
</verification>

<success_criteria>
Overlays shipping. Export modal restyled with zero behavior loss. Phase 13 tests all green.
</success_criteria>

<output>
After completion, create `.planning/phases/14-port-claude-design-into-apps-desktop/14-04-SUMMARY.md`.
</output>
