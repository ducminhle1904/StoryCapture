---
phase: 14-port-claude-design-into-apps-desktop
plan: 03
type: execute
wave: 3
depends_on: ["14-01"]
files_modified:
  - apps/desktop/src/routes/dashboard.tsx
  - apps/desktop/src/routes/editor.tsx
  - apps/desktop/src/routes/post-production.tsx
  - apps/desktop/src/features/post-production/editor-shell.tsx
  - apps/desktop/src/routes/settings.tsx
autonomous: true
requirements: [D-05a, D-05b, D-05c, D-05d, D-09]
must_haves:
  truths:
    - "Dashboard renders project grid in new sc-* dark-first styling"
    - "Editor SHELL (toolbar, panels, splitpane dividers) uses sc-* tokens while CodeMirror/LSP/DryRun/SelectorValidator children remain untouched"
    - "Post-Production editor-shell renders with sc-* tokens and preserves all 6 Zustand slices + WebGPU preview"
    - "Settings route renders AccountsPage in new styling"
    - "Every IPC call, hotkey, Zustand selector, motion transition preserved (D-09)"
    - "Legacy chrome (AppLayout, title-bar.tsx, sidebar.tsx) remains untouched this wave."
  artifacts:
    - path: "apps/desktop/src/routes/dashboard.tsx"
      provides: "Restyled Dashboard route"
    - path: "apps/desktop/src/routes/editor.tsx"
      provides: "Restyled Editor shell with child components unchanged"
    - path: "apps/desktop/src/features/post-production/editor-shell.tsx"
      provides: "Restyled post-production editor shell"
    - path: "apps/desktop/src/routes/settings.tsx"
      provides: "Restyled Settings route (Appearance added in Wave 5)"
  key_links:
    - from: "dashboard.tsx"
      to: "useProjects + useDashboardStore + navigate"
      via: "preserved imports"
      pattern: "useProjects|useDashboardStore"
    - from: "editor.tsx"
      to: "StoryEditor + PreviewPanel + SceneListPanel + TimelinePanel + TtsClipInspector"
      via: "preserved child component tree"
      pattern: "StoryEditor|PreviewPanel|SceneListPanel"
---

<objective>
Wave 3 — Screens. Port the 4 mocked routes (Dashboard, Editor, Post-production, Settings) to the new dark-first sc-* styling while preserving every behavior (D-09). Each task restyles the route's CONTENT only — the legacy `AppLayout` / `FullscreenLayout` / `title-bar.tsx` / `sidebar.tsx` shell stays in place (D-03/D-06a dropped). Every Zustand slice, IPC call, hotkey, LSP bridge, WebGPU lifecycle, motion transition survives.

Purpose: Mocked screens visually match the Claude Design bundle, inside the existing app chrome.
Output: 4 restyled routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-PATTERNS.md
@.planning/design/storycapture-claude-design/project/screens/dashboard.jsx
@.planning/design/storycapture-claude-design/project/screens/editor.jsx
@.planning/design/storycapture-claude-design/project/screens/postprod.jsx
@.planning/design/storycapture-claude-design/project/screens/settings.jsx
@apps/desktop/src/routes/dashboard.tsx
@apps/desktop/src/routes/editor.tsx
@apps/desktop/src/routes/post-production.tsx
@apps/desktop/src/routes/settings.tsx
@apps/desktop/src/features/post-production/editor-shell.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port Dashboard + Settings routes</name>
  <read_first>
    - apps/desktop/src/routes/dashboard.tsx (preserve: useProjects, useDashboardStore, navigate, ProjectGrid, ProjectFilters, NewProjectDialog, PageContentTransition; preserve `<AppLayout>` wrapper as-is)
    - apps/desktop/src/routes/settings.tsx (preserve: AccountsPage; preserve `<AppLayout>` wrapper as-is)
    - .planning/design/storycapture-claude-design/project/screens/dashboard.jsx
    - .planning/design/storycapture-claude-design/project/screens/settings.jsx
    - PATTERNS.md §routes/dashboard.tsx REWRITE + §routes/settings.tsx REWRITE + §Route shell shared pattern
    - RESEARCH Pitfall 6 (prototype artifact strip checklist)
  </read_first>
  <action>
    1) Rewrite the CONTENTS of apps/desktop/src/routes/dashboard.tsx (leave the `<AppLayout>` / `<FullscreenLayout>` wrapper untouched — D-03/D-06a dropped):
       - Preserve ALL imports and hook calls verbatim: `useProjects`, `useDashboardStore` (searchQuery, sortMode), `navigate`, `openProject = (id) => navigate(`/editor/${id}`)`, `setDialogOpen` state, `<ProjectGrid>`, `<ProjectFilters>`, `<NewProjectDialog>`, `<PageContentTransition>` wrapper.
       - Replace the inner content shell per PATTERNS §Route shell:
         `<main id="main-content" className="flex h-full flex-col">` → keep
         `<header className="... border-[var(--sc-border-2)] bg-[var(--sc-surface)] ...">` → retoken
         Replace `brand-button` / shadcn `<Button>` for "New Project" → `<ScButton variant="primary" icon={<Plus size={14}/>}>New Project</ScButton>`.
       - Wrap project cards container in `<ScCard>` where claude-design dashboard.jsx shows a card container.
       - Strip any prototype artifacts if accidentally ported (checklist from RESEARCH Pitfall 6).
       - Wave 1 primitives (Sc*) are consumed via `@storycapture/ui`.
       Per D-05a, D-09.
    2) Rewrite the CONTENTS of apps/desktop/src/routes/settings.tsx (leave the `<AppLayout>` wrapper untouched):
       - Preserve `<AccountsPage />` wiring.
       - Retoken header/body to sc-*.
       - Leave space for an "Appearance" section that Wave 5 will add; do NOT add it here (Wave 5 owns the tweaks-store wiring).
       Per D-05d, D-09.
    3) Do NOT touch routes/recorder.tsx or routes/index.tsx this wave (Wave 5 does recorder cosmetic pass; index is router config).
    4) Do NOT modify the legacy chrome files: apps/desktop/src/App.tsx, apps/desktop/src/components/title-bar.tsx, apps/desktop/src/components/sidebar.tsx. They stay as-is through this wave.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- routes/dashboard routes/settings 2>&1 | tail -30 || true</automated>
  </verify>
  <done>
    - dashboard.tsx imports unchanged from prior version except ScButton/ScCard additions
    - useProjects, useDashboardStore, openProject preserved verbatim (grep)
    - settings.tsx still wraps AccountsPage
    - No --color-* classes in either file (all retoken'd to --sc-*)
    - `rg "window\.parent\.postMessage|localStorage\.(get|set)Item.*sc-" apps/desktop/src/routes/{dashboard,settings}.tsx` returns empty
    - `<AppLayout>` / `<FullscreenLayout>` wrappers unchanged in both files (git diff shows only inner content edits)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle Editor route (shell only, children untouched)</name>
  <read_first>
    - apps/desktop/src/routes/editor.tsx (860+ lines — SHELL ONLY restyle per Open Q 3)
    - .planning/design/storycapture-claude-design/project/screens/editor.jsx (visual reference for shell)
    - PATTERNS.md §routes/editor.tsx RESTYLE
    - CONTEXT.md D-09 (preserve all behavior) + RESEARCH Open Q 3
  </read_first>
  <action>
    1) Open apps/desktop/src/routes/editor.tsx. Identify the SHELL-level elements INSIDE the route (do NOT touch the outer `<AppLayout>` / `<FullscreenLayout>` wrapper — D-03/D-06a dropped, legacy chrome stays): outer content `<main>`, toolbar div, split-pane container (`<PanelGroup>`), panel header strips, `<PanelResizeHandle>`, panel wrappers around `<StoryEditor>`, `<PreviewPanel>`, `<SceneListPanel>`, `<TimelinePanel>`, `<TtsClipInspector>`, `<TtsScriptEditor>`, `<VoiceCatalogDialog>`, DryRun, SelectorValidator.
    2) Retoken ONLY those inner shell wrappers:
       - Replace `--color-surface-*`, `--color-border-*`, `--color-fg-*`, `--color-accent-*` refs with `--sc-surface*`, `--sc-border*`, `--sc-text*`, `--sc-accent-500`.
       - Replace any shadcn `<Button>` in the toolbar (save/run/pick/dryrun buttons) with `<ScButton>` (from `@storycapture/ui`) using the matching variant from claude-design/app.css.
       - Replace PanelResizeHandle visual styling to match claude-design's split divider (thin line using `--sc-border-2`).
    3) **Do NOT modify** StoryEditor, PreviewPanel, SceneListPanel, TimelinePanel, TtsClipInspector, TtsScriptEditor, VoiceCatalogDialog, or any dialog child. CodeMirror theme wiring is owned by StoryEditor; a later phase can retoken it.
    4) Preserve all hotkeys (`useHotkeys`), IPC calls (`invoke`, `parseStory`), motion transitions (`motion`, `useReducedMotion`), and every hook at top of file.
    5) Strip any prototype artifacts if any were accidentally introduced.
    Per D-05b, D-09, RESEARCH Open Q 3 recommendation.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- routes/editor features/editor 2>&1 | tail -30 || true</automated>
  </verify>
  <done>
    - editor.tsx SHELL uses sc-* tokens (grep `--sc-` in shell sections, zero `--color-` remaining in apps/desktop/src/routes/editor.tsx)
    - All child component imports preserved (`grep "StoryEditor\|PreviewPanel\|SceneListPanel\|TimelinePanel\|TtsClipInspector\|TtsScriptEditor"` returns matches)
    - All hotkeys preserved (`grep "useHotkeys"` count unchanged vs pre-wave)
    - `<AppLayout>` / `<FullscreenLayout>` wrapper unchanged
    - Existing editor tests still pass
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle Post-Production editor-shell (children untouched)</name>
  <read_first>
    - apps/desktop/src/routes/post-production.tsx (17-line thin wrapper — likely NO change)
    - apps/desktop/src/features/post-production/editor-shell.tsx (REAL target)
    - apps/desktop/src/features/post-production/state/ (6-slice Zustand store — DO NOT TOUCH)
    - .planning/design/storycapture-claude-design/project/screens/postprod.jsx
    - PATTERNS.md §routes/post-production.tsx RESTYLE
    - CONTEXT.md D-09 + Assumption A6 (mock is subset; controls mock omits are RETAINED)
  </read_first>
  <action>
    1) Leave apps/desktop/src/routes/post-production.tsx untouched — it's a thin `<EditorShell storyId=… />` wrapper (and its outer `<AppLayout>` stays as-is).
    2) In apps/desktop/src/features/post-production/editor-shell.tsx, restyle the internal SHELL: toolbar, track headers, timeline ruler, inspector panel headers, sound drawer header, export queue widget wrapper. Retoken `--color-*` → `--sc-*` and swap shadcn `<Button>` instances in the top toolbar for `<ScButton>` (from `@storycapture/ui`) where they fit semantically.
    3) **Do NOT touch** any of the 6 Zustand slices (export-slice, panels-slice, queue-slice, selection-slice, timeline-slice, undo-slice), WebGPU preview engine, render queue, timeline scrubber logic, undo-redo ring buffer, export modal (Wave 4 owns export-modal restyle).
    4) Preserve hotkeys registered via `useHotkeys` (mod+z, mod+y, space, arrows, comma/period, shift+comma/period per RESEARCH A3).
    5) If claude-design postprod.jsx shows controls the current shell does not implement → DEFER (list them in the SUMMARY.md "Deferred / not ported"). If current shell has controls the mock omits → RETAIN (list in SUMMARY.md "Retained beyond mock").
    Per D-05c + D-09.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- features/post-production 2>&1 | tail -40 || true</automated>
  </verify>
  <done>
    - editor-shell.tsx SHELL sections use `--sc-*` tokens
    - No change to state/ directory
    - Existing post-production tests still green (especially export-slice, undo ring, timeline)
    - SUMMARY.md enumerates any Deferred mock-only controls + Retained extra controls
  </done>
</task>

</tasks>

<verification>
- Full `pnpm --filter @storycapture/desktop test` green
- `pnpm tauri:dev` → navigate through Dashboard → Editor → Post-Production → Settings; all render in sc-* dark styling; all interactive behaviors (project open, editor type+save, timeline scrub, settings read) work
- `rg "var\(--color-" apps/desktop/src/routes apps/desktop/src/features/post-production/editor-shell.tsx` returns zero
- Legacy chrome untouched: `git diff apps/desktop/src/App.tsx apps/desktop/src/components/title-bar.tsx apps/desktop/src/components/sidebar.tsx` shows no changes from this wave.
</verification>

<success_criteria>
All 4 mocked routes render in the new sc-* dark-first styling inside the existing (unchanged) app chrome. No behavior removed. All Phase 13 wiring (output-prefs, encoder options) intact for Wave 4.
</success_criteria>

<output>
After completion, create `.planning/phases/14-port-claude-design-into-apps-desktop/14-03-SUMMARY.md`.
Include sections "Deferred mock-only controls" and "Retained beyond mock" per task 3.
</output>
