---
phase: 14-port-claude-design-into-apps-desktop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src/styles.css
  - packages/ui/src/claude-design/primitives/sc-button.tsx
  - packages/ui/src/claude-design/primitives/sc-input.tsx
  - packages/ui/src/claude-design/primitives/sc-badge.tsx
  - packages/ui/src/claude-design/primitives/sc-switch.tsx
  - packages/ui/src/claude-design/primitives/sc-card.tsx
  - packages/ui/src/claude-design/primitives/sc-kbd.tsx
  - packages/ui/src/claude-design/primitives/sc-slider.tsx
  - packages/ui/src/claude-design/primitives/sc-select.tsx
  - packages/ui/src/claude-design/primitives/sc-segmented.tsx
  - packages/ui/src/claude-design/primitives/index.ts
  - packages/ui/src/claude-design/index.ts
  - packages/ui/src/index.ts
  - packages/ui/src/tokens.css
  - apps/desktop/src/routes/_design-system/tokens.tsx
  - apps/desktop/src/routes/_design-system/components.tsx
  - apps/desktop/src/router.tsx
autonomous: true
requirements: [D-01, D-04, D-06f, D-11]
must_haves:
  truths:
    - "sc-* tokens load as the single canonical token system in apps/desktop"
    - "Inter + JetBrains Mono are the only font families imported"
    - "Sc* React primitives exist and are exported from @storycapture/ui"
    - "/_design-system route renders token and component showcases"
  artifacts:
    - path: "packages/ui/src/claude-design/primitives/sc-button.tsx"
      provides: "ScButton primitive with variant/size props"
    - path: "apps/desktop/src/styles.css"
      provides: "Single @import graph for sc-* tokens + variable fonts"
    - path: "apps/desktop/src/routes/_design-system/tokens.tsx"
      provides: "Hidden showcase route"
  key_links:
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/claude-design/primitives/*"
      via: "re-export barrel"
      pattern: "export.*from.*claude-design"
---

<objective>
Wave 1 — Foundation. Retire Cursor-warm tokens (D-01), swap fonts to Inter + JetBrains Mono variable (D-11), ship 9 Sc* React primitives (D-04), wire hidden /_design-system showcase (D-06f). No routes change visually yet; primitives just become available.

Purpose: Every downstream wave consumes sc-* tokens and Sc* primitives. This wave is the breaking-change foundation.
Output: Working app with new token system loaded, Sc* primitives importable, old tokens.css retired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-RESEARCH.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-PATTERNS.md
@packages/ui/src/claude-design/tokens.css
@packages/ui/src/claude-design/app.css
@.planning/design/storycapture-claude-design/project/components/primitives.jsx
@apps/desktop/src/components/ui/button.tsx
@apps/desktop/src/components/ui/input.tsx
@apps/desktop/src/components/ui/slider.tsx
@apps/desktop/src/components/ui/select.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Font swap + token wire-up + tokens.css retirement</name>
  <read_first>
    - apps/desktop/package.json (current fontsource deps at lines 24-28)
    - apps/desktop/src/styles.css (current @import graph)
    - packages/ui/src/claude-design/tokens.css (already staged, :root --sc-* vars)
    - packages/ui/src/claude-design/app.css (already staged, .sc-btn/.sc-input/etc rules)
    - packages/ui/src/tokens.css (TO BE DELETED — this file)
    - PATTERNS.md §apps/desktop/package.json MODIFY + §apps/desktop/src/styles.css MODIFY
  </read_first>
  <action>
    1) In apps/desktop/package.json remove: `@fontsource-variable/outfit`, `@fontsource/inter`, `@fontsource/jetbrains-mono`, `@fontsource/lora`. Add: `"@fontsource-variable/inter": "^5.2.0"`, `"@fontsource-variable/jetbrains-mono": "^5.1.0"`.
    2) Run `pnpm install` from repo root to lock.
    3) Rewrite apps/desktop/src/styles.css so the @import graph is exactly (replace old fontsource + tokens block):
       ```
       @import "@fontsource-variable/inter";
       @import "@fontsource-variable/jetbrains-mono";
       @import "@storycapture/ui/claude-design/tokens.css";
       @import "@storycapture/ui/claude-design/app.css";
       ```
       Keep any existing Tailwind `@import "tailwindcss"` + app-level CSS below. Remove any import of `@storycapture/ui/tokens.css`.
    4) Grep for stale `--color-*` refs: `rg "var\(--color-" apps/desktop/src` — every hit must be resolved by mapping to an `--sc-*` equivalent per packages/ui/src/claude-design/tokens.css. Do not leave transitional stubs (CONTEXT.md Deferred Ideas notes a stub MAY be kept, but D-10 Wave 1 says retire cleanly; keep stub only if grep returns >50 hits, otherwise migrate).
    5) Delete packages/ui/src/tokens.css.
    6) Update packages/ui/src/index.ts to remove any re-export of the deleted tokens.css.
    7) Ensure packages/ui exports `claude-design/tokens.css` and `claude-design/app.css` as package subpath exports. Inspect packages/ui/package.json `exports` field and add/confirm:
       ```json
       "./claude-design/tokens.css": "./src/claude-design/tokens.css",
       "./claude-design/app.css": "./src/claude-design/app.css"
       ```
    Per D-01 (retire Cursor-warm tokens), D-11 (Inter + JetBrains Mono only).
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop build && rg "var\(--color-" apps/desktop/src | wc -l | awk '{if ($1==0) exit 0; else exit 1}' && rg "fontsource/lora|fontsource-variable/outfit|@fontsource/inter[^-]|@fontsource/jetbrains-mono" apps/desktop | wc -l | awk '{if ($1==0) exit 0; else exit 1}'</automated>
  </verify>
  <done>
    - packages/ui/src/tokens.css is deleted
    - apps/desktop/package.json deps include @fontsource-variable/inter + @fontsource-variable/jetbrains-mono only
    - apps/desktop/src/styles.css imports claude-design tokens.css + app.css
    - No `--color-*` references remain in apps/desktop/src
    - `pnpm --filter @storycapture/desktop build` succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship 9 Sc* primitives + barrels</name>
  <read_first>
    - apps/desktop/src/components/ui/button.tsx (CVA + forwardRef analog)
    - apps/desktop/src/components/ui/input.tsx (Base UI Input wrap)
    - apps/desktop/src/components/ui/slider.tsx (Base UI Slider wrap)
    - apps/desktop/src/components/ui/select.tsx (Base UI Select wrap)
    - apps/desktop/src/components/ui/toggle-group.tsx (segmented control analog)
    - .planning/design/storycapture-claude-design/project/components/primitives.jsx (visual reference)
    - packages/ui/src/claude-design/app.css (authoritative .sc-* class rules)
    - PATTERNS.md §Shared Patterns: "Base UI + CVA primitive composition"
    - RESEARCH.md §Pattern 1 (ScButton example with forwardRef + clsx)
  </read_first>
  <behavior>
    - ScButton renders `<button class="sc-btn ${variant} ${size}">` with variant ∈ {default, primary, ghost, danger, success}, size ∈ {sm, md, lg, icon}; forwards ref; accepts `icon` and `kbd` props
    - ScInput wraps Base UI Input with `.sc-input` class; forwards ref
    - ScBadge renders span `.sc-badge` with tone ∈ {neutral, info, success, warn, danger}
    - ScSwitch wraps Base UI Switch with `.sc-switch` class; accepts `checked`, `onCheckedChange`
    - ScCard renders `<div class="sc-card">{children}</div>` with optional `title` + `action` slots
    - ScKbd renders `<kbd class="sc-kbd">{children}</kbd>`
    - ScSlider wraps Base UI Slider.Root/Control/Track/Indicator/Thumb with `.sc-slider-*` classes
    - ScSelect wraps Base UI Select with `.sc-select` trigger + popup
    - ScSegmented wraps Base UI ToggleGroup with `.sc-segmented` classes
    - Every primitive has forwardRef + displayName
  </behavior>
  <action>
    1) Create packages/ui/src/lib/cn.ts exporting `cn = (...inputs) => twMerge(clsx(inputs))` using `clsx` + `tailwind-merge` (add these as deps to packages/ui/package.json if missing). Export from packages/ui/src/index.ts.
    2) Create 9 primitive files under packages/ui/src/claude-design/primitives/:
       - sc-button.tsx: CVA-style shape from RESEARCH §Pattern 1, classes drawn from claude-design/app.css `.sc-btn`, `.sc-btn.primary`, `.sc-btn.ghost`, `.sc-btn.danger`, `.sc-btn.success`, `.sc-btn.sm`, `.sc-btn.lg`, `.sc-btn.icon`. Props: variant, size, icon: ReactNode, kbd?: string.
       - sc-input.tsx: `import { Input as BaseInput } from "@base-ui-components/react/input"` + forwardRef + `.sc-input` class. Preserve all BaseInput props.
       - sc-badge.tsx: `<span class="sc-badge ${tone}">` — tone ∈ neutral|info|success|warn|danger (classes from app.css).
       - sc-switch.tsx: `import { Switch } from "@base-ui-components/react/switch"` wrap; `.sc-switch` class on Root; Thumb with `.sc-switch-thumb`.
       - sc-card.tsx: `<div class="sc-card">` with optional `<div class="sc-card-header">`.
       - sc-kbd.tsx: simple `<kbd class="sc-kbd">`.
       - sc-slider.tsx: analog = apps/desktop/src/components/ui/slider.tsx, retokened: Base UI Slider.Root/Control/Track/Indicator/Thumb with classes from `.sc-slider-*` in app.css.
       - sc-select.tsx: analog = apps/desktop/src/components/ui/select.tsx, decomposed into ScSelect/ScSelectTrigger/ScSelectValue/ScSelectContent/ScSelectItem, each retokened to `.sc-select*` classes.
       - sc-segmented.tsx: Base UI ToggleGroup Root+Item wrap with `.sc-segmented` + `.sc-segmented-item` classes.
    3) For each primitive write a test file at packages/ui/src/claude-design/primitives/__tests__/{name}.test.tsx using @testing-library/react that renders the primitive + asserts the key class is present + ref forwards to the underlying HTML element. Run under the existing vitest config for packages/ui (create apps/desktop-style vitest.config.ts for packages/ui if one does not yet exist — environment: happy-dom is fine here, a11y checks are Wave 5).
    4) Create packages/ui/src/claude-design/primitives/index.ts as a barrel exporting every Sc* named export.
    5) Create packages/ui/src/claude-design/index.ts re-exporting `./primitives`.
    6) Update packages/ui/src/index.ts to re-export `./claude-design` (new) while preserving existing exports.
    Per D-04 (React wrappers, not raw classNames in ported JSX).
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/ui test && pnpm --filter @storycapture/desktop typecheck</automated>
  </verify>
  <done>
    - 9 Sc* primitive files exist under packages/ui/src/claude-design/primitives/
    - Each has forwardRef + displayName + at least one render+class+ref assertion test passing
    - `import { ScButton, ScInput, ScBadge, ScSwitch, ScCard, ScKbd, ScSlider, ScSelect, ScSegmented } from "@storycapture/ui"` resolves
    - No raw --color-* tokens in primitive CSS
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: /_design-system showcase routes + router wiring</name>
  <read_first>
    - apps/desktop/src/router.tsx (current route table)
    - apps/desktop/src/routes/settings.tsx (simple content route analog)
    - .planning/design/storycapture-claude-design/project/screens/tokens.jsx
    - .planning/design/storycapture-claude-design/project/screens/components.jsx
    - CONTEXT.md D-06f (hidden route; reachable by URL only, not linked)
  </read_first>
  <action>
    1) Create apps/desktop/src/routes/_design-system/tokens.tsx — port screens/tokens.jsx visuals. Strip prototype artifacts per RESEARCH Pitfall 6 checklist: no `window.parent.postMessage`, no `localStorage` reads, no CDN `<script>`, no Babel globals, no `window.SC_TWEAKS` reads. Convert .jsx → .tsx. Render each --sc-* color token + radius + spacing in a grid using Sc* primitives where applicable.
    2) Create apps/desktop/src/routes/_design-system/components.tsx — port screens/components.jsx. Render every Sc* primitive with each variant + state.
    3) Modify apps/desktop/src/router.tsx: add two routes `/_design-system/tokens` → tokens.tsx, `/_design-system/components` → components.tsx. Do NOT link from sidebar. No sidebar changes.
    4) Both routes use the existing layout for now (chrome is Wave 2); wrap in a plain `<main id="main-content" className="h-full overflow-auto p-8">`.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- routes/_design-system 2>&1 | tail -20 || true</automated>
  </verify>
  <done>
    - Navigating to `/_design-system/tokens` in dev renders without errors
    - Navigating to `/_design-system/components` renders every Sc* primitive
    - No sidebar/nav entry links to these routes
    - No prototype artifacts (postMessage, localStorage, CDN scripts) present
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @storycapture/desktop build` succeeds with no `--color-*` in apps/desktop/src
- `pnpm --filter @storycapture/ui test` green for all primitives
- `pnpm tauri:dev` boots and renders dashboard (looks unchanged pre-Wave 3 but dark theme from sc-* tokens visible)
- `rg "@fontsource/lora|@fontsource-variable/outfit|@fontsource/inter[^-]|@fontsource/jetbrains-mono" apps/desktop` returns nothing
- `rg "packages/ui/src/tokens\.css" .` returns nothing
</verification>

<success_criteria>
Foundation laid: new tokens + fonts + primitives available. Existing routes still render (may look slightly different due to sc-* tokens being used where old --color-* vars were migrated, but no route body restyle this wave). No chrome change yet.
</success_criteria>

<output>
After completion, create `.planning/phases/14-port-claude-design-into-apps-desktop/14-01-SUMMARY.md`.
</output>
