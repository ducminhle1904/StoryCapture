---
phase: 14-port-claude-design-into-apps-desktop
plan: 05
type: execute
wave: 5
depends_on: ["14-04"]
files_modified:
  - apps/desktop/src/stores/tweaks-store.ts
  - apps/desktop/src/lib/theme.ts
  - apps/desktop/src/main.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/components/tweaks-panel.tsx
  - apps/desktop/src/components/sc-shell/sc-shell.tsx
  - apps/desktop/src/routes/settings.tsx
  - apps/desktop/src/routes/recorder.tsx
  - apps/desktop/vitest.config.ts
  - apps/desktop/src/test-setup.ts
  - apps/desktop/package.json
  - apps/desktop/src/components/sc-shell/__tests__/sc-title-bar.a11y.test.tsx
  - apps/desktop/src/routes/__tests__/theme.a11y.test.tsx
autonomous: false
requirements: [D-02, D-05e, D-06e, D-08]
must_haves:
  truths:
    - "tauri-plugin-store persists {theme, accentHue, density, radius} via tweaks.json"
    - "Boot-time initTweaksStore hydrates dataset attributes + --sc-accent-h on <html>"
    - "Settings → Appearance exposes theme toggle (dark/light) + accent hue slider"
    - "TweaksPanel mounts ONLY when import.meta.env.DEV; toggled by Cmd/Ctrl+Shift+."
    - "Dev-only TweaksPanel exposes theme+accentHue+density+radius controls"
    - "Recorder route uses sc-* tokens and sits inside ScShell (cosmetic-only, no layout redesign)"
    - "vitest-axe reports zero violations for representative screens on both dark and light themes"
  artifacts:
    - path: "apps/desktop/src/stores/tweaks-store.ts"
      provides: "Zustand store + plugin-store persistence + applyTweaks side-effect"
    - path: "apps/desktop/src/components/tweaks-panel.tsx"
      provides: "Dev-only overlay gated by import.meta.env.DEV + Cmd/Ctrl+Shift+."
    - path: "apps/desktop/vitest.config.ts"
      provides: "jsdom env override for a11y tests"
  key_links:
    - from: "main.tsx"
      to: "initTweaksStore → plugin-store → applyTweaks"
      via: "await initTweaksStore() before render"
      pattern: "initTweaksStore"
    - from: "settings.tsx Appearance"
      to: "useTweaksStore.setTweak"
      via: "setTweak('theme', value) + setTweak('accentHue', value)"
      pattern: "useTweaksStore|setTweak"
---

<objective>
Wave 5 — Tweaks + Polish. Wire the TweaksPanel (dev-only), migrate theme persistence to tauri-plugin-store, expose theme + accent hue in Settings → Appearance, recorder cosmetic pass (tokens + chrome), and ship WCAG 2.1 AA verification via vitest-axe under a jsdom env.

Purpose: Finish behavior gap (tweaks persistence + user-facing theme toggle) + prove WCAG AA on both themes.
Output: Dev toggle persists; end-user theme toggle works; a11y suite green on both themes; recorder unified with rest of app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-RESEARCH.md
@.planning/phases/14-port-claude-design-into-apps-desktop/14-PATTERNS.md
@.planning/design/storycapture-claude-design/project/components/primitives.jsx
@apps/desktop/src/lib/output-prefs-persist.ts
@apps/desktop/src/state/output-prefs.ts
@apps/desktop/src/lib/theme.ts
@apps/desktop/vitest.config.ts
@apps/desktop/src/routes/recorder.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tweaks-store + theme.ts rewrite + main.tsx hydration</name>
  <read_first>
    - apps/desktop/src/lib/output-prefs-persist.ts (Phase 13 hydrate pattern, lines 1-95)
    - apps/desktop/src/state/output-prefs.ts (Zustand + plugin-store shape)
    - apps/desktop/src/lib/theme.ts (current localStorage impl, 40 lines — being rewritten)
    - RESEARCH.md §Code Example 3 (tweaks store skeleton)
    - PATTERNS.md §tweaks-store.ts + §lib/theme.ts REWRITE + §main.tsx MODIFY
    - CONTEXT.md D-02 + D-08 (dark default; density/radius dev-only; theme+accentHue user-facing)
  </read_first>
  <behavior>
    - Shape: { theme: "dark"|"light", accentHue: number(0-360), density: "comfortable"|"compact", radius: "sharp"|"md"|"lg" }
    - Defaults: { theme: "dark", accentHue: 78, density: "comfortable", radius: "md" }
    - setTweak(k, v) updates store + applies side-effect to <html> + persists to plugin-store
    - applyTweaks writes data-theme, data-density, data-radius dataset attrs + --sc-accent-h style prop
    - initTweaksStore loads from tauri-plugin-store on boot, falls back to DEFAULTS, applies side-effect
    - theme.ts re-exports getTheme/setTheme/toggleTheme as thin wrappers over useTweaksStore for existing call sites
  </behavior>
  <action>
    1) Create apps/desktop/src/stores/tweaks-store.ts per RESEARCH §Code Example 3 skeleton (full implementation). Store file name for tauri-plugin-store: `"tweaks.json"`. Key: `"tweaks"`. Mirror output-prefs-persist.ts's `getStore()` lazy-singleton pattern.
    2) Rewrite apps/desktop/src/lib/theme.ts so every exported function wraps tweaks-store:
       - `getTheme() = useTweaksStore.getState().theme`
       - `setTheme(t) = useTweaksStore.getState().setTweak("theme", t)`
       - `toggleTheme() = { const t = getTheme()==="dark"?"light":"dark"; setTheme(t); return t; }`
       - Remove `applyPersistedTheme` export (no longer needed; tweaks-store.initTweaksStore handles it). Search for all callers: `rg "applyPersistedTheme" apps/desktop/src` — if any remain, swap to `initTweaksStore` or delete.
       - Keep localStorage fallback behind a try/catch inside setTweak itself ONLY if needed — RESEARCH Anti-Pattern says "don't remove silently" but also recommends plugin-store. Behaviour: plugin-store is primary; if plugin-store call rejects, setTweak still applies the dataset attr (graceful degradation).
    3) Modify apps/desktop/src/main.tsx:
       ```tsx
       import { detectPlatform, applyPlatform } from "./lib/platform";
       import { initTweaksStore } from "./stores/tweaks-store";
       async function bootstrap() {
         applyPlatform(await detectPlatform());
         await initTweaksStore();
         await initOutputPrefs();
         root.render(...);
       }
       ```
       Remove any `applyPersistedTheme()` call.
    4) Modify apps/desktop/src/App.tsx — replace hard-coded `theme="dark"` in `<Toaster>` with `const theme = useTweaksStore(s => s.theme);` then `theme={theme}`. Keep the 5 CSS vars from Wave 4.
    5) Tests at apps/desktop/src/stores/__tests__/tweaks-store.test.ts:
       - Mock `@tauri-apps/plugin-store` `Store.load` + get/set/save.
       - initTweaksStore with empty store → applies DEFAULTS, html dataset has theme="dark" density="comfortable" radius="md", --sc-accent-h is "78".
       - setTweak("theme", "light") → html.dataset.theme === "light", store.set called with new shape.
       - setTweak("accentHue", 200) → html.style.getPropertyValue("--sc-accent-h") === "200".
    Per D-02, D-08, RESEARCH Pattern 2.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop test -- stores/tweaks-store lib/theme 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tweaks-store.ts exists with 4-field shape + Zustand + plugin-store persistence
    - theme.ts wraps tweaks-store; no localStorage direct refs remain in apps/desktop/src/lib/theme.ts
    - main.tsx calls initTweaksStore before initOutputPrefs
    - App.tsx Toaster reads live theme from store
    - Tweaks tests green
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TweaksPanel (dev-only) + Settings → Appearance + Recorder cosmetic pass</name>
  <read_first>
    - .planning/design/storycapture-claude-design/project/components/primitives.jsx (TweaksPanel reference)
    - apps/desktop/src/routes/settings.tsx (from Wave 3 — add Appearance section here)
    - apps/desktop/src/routes/recorder.tsx (cosmetic pass only; preserve recorder state + IPC)
    - CONTEXT.md D-08 (keybind Cmd/Ctrl+Shift+. — confirmed no collision per RESEARCH A3) + D-05e (recorder inherits tokens + chrome, no redesign)
    - RESEARCH.md Claude's Discretion (Cmd/Ctrl+Shift+.)
  </read_first>
  <action>
    1) Create apps/desktop/src/components/tweaks-panel.tsx:
       - Gate ENTIRE component with `if (!import.meta.env.DEV) return null;` at top of render.
       - Register hotkey `useHotkeys("mod+shift+period", (e) => { e.preventDefault(); setOpen(o => !o); })`.
       - Render inside Base UI Dialog.Root (focus trap, esc close) when open.
       - Inside Dialog.Popup:
         - Theme: ScSegmented with options [{value:"dark",label:"Dark"},{value:"light",label:"Light"}] bound to `theme` from useTweaksStore; onChange calls setTweak("theme", v).
         - Accent hue: ScSlider min=0 max=360 step=1 bound to accentHue.
         - Density: ScSegmented [comfortable|compact] (DEV-only — not exposed in Settings per D-08).
         - Radius: ScSegmented [sharp|md|lg] (DEV-only).
       - Classes: `.sc-tweaks` from claude-design app.css.
       - Mount in sc-shell.tsx (sibling to CommandPalette + RecordingIndicator).
    2) Extend apps/desktop/src/routes/settings.tsx — add "Appearance" section ABOVE AccountsPage (or wherever the Settings flow dictates):
       - `<ScCard title="Appearance">` containing:
         - Theme toggle: ScSegmented [Dark|Light] bound to `useTweaksStore(s => s.theme)` → `setTweak("theme", v)`.
         - Accent hue: ScSlider 0-360 bound to `accentHue`.
       - Do NOT expose density or radius here (D-08: those are dev-only).
    3) Cosmetic pass on apps/desktop/src/routes/recorder.tsx:
       - Ensure route body is wrapped in the same `<main id="main-content" className="flex h-full flex-col">` shell pattern as dashboard/settings from Wave 3.
       - Retoken the page header, any toolbar buttons, and sidebar usage to sc-*. Swap shadcn `<Button>` in header/toolbar to `<ScButton>` only where visually needed.
       - **Do NOT redesign** the recorder body (preview pane, capture-target picker, HUD). Leave child components untouched per D-05e.
       - Preserve every hook, IPC, Zustand selector, motion transition.
    4) Test: apps/desktop/src/components/__tests__/tweaks-panel.test.tsx:
       - With `vi.stubGlobal("import.meta.env", { DEV: false })`, render `<TweaksPanel />` → assert nothing rendered.
       - With DEV:true, press `{Meta>}{Shift>}.`, assert Dialog.Popup appears with theme segmented present. (Or mount inside a test harness that doesn't require the hotkey path: expose an `open` prop for testability and test via prop + also test hotkey path separately.)
    4) REMOVE the TRANSIENT theme-toggle button from apps/desktop/src/components/sc-shell/sc-side-nav.tsx (introduced in 14-02 Task 2). Delete both the marker comment (`// TRANSIENT: theme-toggle lives in ScSideNav until Wave 5...`) and the `<button className="sc-sidenav-theme-toggle">` element + its Sun/Moon icon import usage. Also drop `.sc-sidenav-theme-toggle` CSS rule if it was added locally. Grep verification: `rg "sc-sidenav-theme-toggle|TRANSIENT: theme-toggle lives in ScSideNav" apps/desktop/src` MUST return 0 matches after this task. The user-facing theme switch now lives solely in Settings → Appearance.
    Per D-06e, D-05e, D-08 + 14-02 transient-toggle handoff.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop typecheck && pnpm --filter @storycapture/desktop test -- components/tweaks-panel routes/settings routes/recorder 2>&1 | tail -30 || true</automated>
  </verify>
  <done>
    - tweaks-panel.tsx mounted in sc-shell with DEV + hotkey gates
    - settings.tsx has Appearance section with theme + accent-hue controls (density/radius NOT shown)
    - recorder.tsx cosmetic tokens applied; no child-component changes (git diff shows only className/button-swap changes)
    - Transient theme-toggle button REMOVED from ScSideNav: `rg "sc-sidenav-theme-toggle|TRANSIENT: theme-toggle lives in ScSideNav" apps/desktop/src` returns 0 matches
    - All tests green
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WCAG 2.1 AA verification via vitest-axe + jsdom env</name>
  <read_first>
    - RESEARCH.md §Pitfall 1 (happy-dom incompatible with axe-core) + §Example 4 (jsdom env override)
    - RESEARCH.md §Wave 0 Gaps (vitest.config + test-setup changes needed)
    - apps/desktop/vitest.config.ts (current happy-dom config)
    - apps/desktop/src/test-setup.ts (current matcher extensions)
    - CONTEXT.md D-02 (both themes must pass WCAG AA)
  </read_first>
  <behavior>
    - vitest-axe + jsdom installed
    - At least 2 a11y test files pass with zero violations on both themes
    - CI command for a11y subset documented
  </behavior>
  <action>
    1) Install devDeps: `pnpm --filter @storycapture/desktop add -D vitest-axe jsdom`.
    2) Modify apps/desktop/vitest.config.ts — keep `environment: "happy-dom"` as default; the jsdom override is per-file via `// @vitest-environment jsdom` pragma (simpler than config projects). If the config uses `environmentMatchGlobs`, add `environmentMatchGlobs: [["**/*.a11y.test.tsx", "jsdom"]]` for clarity.
    3) Modify apps/desktop/src/test-setup.ts — guard matcher extension so it only adds `toHaveNoViolations` when the env is jsdom (or unconditionally — toHaveNoViolations is scoped to the matcher; safe to always extend):
       ```ts
       import { toHaveNoViolations } from "vitest-axe";
       import { expect } from "vitest";
       expect.extend({ toHaveNoViolations });
       ```
    4) Create apps/desktop/src/components/sc-shell/__tests__/sc-title-bar.a11y.test.tsx per RESEARCH §Example 4. Verify:
       - `// @vitest-environment jsdom` pragma at top.
       - Render `<ScTitleBar platform="mac" />` on data-theme=dark → axe clean.
       - Render on data-theme=light (set `document.documentElement.dataset.theme = "light"`) → axe clean.
    5) Create apps/desktop/src/routes/__tests__/theme.a11y.test.tsx:
       - jsdom env
       - Render Dashboard route (via MemoryRouter + mocked useProjects returning 3 fake projects) on both themes → axe clean on both.
       - Render Settings route Appearance section on both themes → axe clean.
    6) **Extended a11y coverage (D-02: "all ported screens"):** Create each of the following files, all with the `// @vitest-environment jsdom` pragma at top, and each test renders the component twice — once with `document.documentElement.dataset.theme = "dark"` and once with `"light"` — asserting `expect(await axe(container)).toHaveNoViolations()` on both:
       - apps/desktop/src/__tests__/a11y/editor.a11y.test.tsx — render a minimal Editor route shell (MemoryRouter at /editor; mock CodeMirror + LSP dependencies to inert stubs; verify toolbar + panel chrome only — per RESEARCH Open Q3, shell-only is correct for this phase).
       - apps/desktop/src/__tests__/a11y/post-production.a11y.test.tsx — render `features/post-production/editor-shell.tsx` at its minimum render path (mock the 6-slice store with a fixture; WebGPU/Canvas stubbed via `vi.stubGlobal`).
       - apps/desktop/src/__tests__/a11y/export-modal.a11y.test.tsx — render `features/post-production/export-modal/export-modal.tsx` in its open state (mock ENC-12..ENC-19 IPC via mockIPC → resolved defaults).
       - apps/desktop/src/__tests__/a11y/command-palette.a11y.test.tsx — render the command-palette component (`apps/desktop/src/components/command-palette/*`) in its open state.
       - apps/desktop/src/__tests__/a11y/recording-indicator.a11y.test.tsx — render `apps/desktop/src/components/recording-indicator.tsx` in its recording state (mock recorder store).
       - apps/desktop/src/__tests__/a11y/recorder.a11y.test.tsx — render the Recorder route (`apps/desktop/src/routes/recorder.tsx`) — chrome + tokens only assertion per D-05e; stub recorder state deps.
       Each test file MUST grep-verify as follows (used in the <done> block):
       - `rg -l "@vitest-environment jsdom" apps/desktop/src/__tests__/a11y/` lists all 6 files.
       - `rg "dataset.theme = "light"" apps/desktop/src/__tests__/a11y/ | wc -l` returns ≥6 (each file flips to light at least once).
       - `rg "toHaveNoViolations" apps/desktop/src/__tests__/a11y/ | wc -l` returns ≥12 (dark + light assertion per file).
    7) Add npm script shortcut in apps/desktop/package.json: `"test:a11y": "vitest run --testNamePattern='a11y'"`.
    Per D-02 ("WCAG AA on all ported screens"), RESEARCH Pitfall 1.
  </action>
  <verify>
    <automated>pnpm --filter @storycapture/desktop test:a11y 2>&1 | tail -40</automated>
  </verify>
  <done>
    - vitest-axe + jsdom installed
    - ≥8 a11y test files pass with zero violations: sc-title-bar, theme (dashboard+settings), editor, post-production, export-modal, command-palette, recording-indicator, recorder
    - Every a11y test file tests BOTH dark and light themes via `document.documentElement.dataset.theme` flip
    - Grep verifications green:
      - `rg -l "@vitest-environment jsdom" apps/desktop/src/__tests__/a11y/` → 6 files
      - `rg "toHaveNoViolations" apps/desktop/src/__tests__/a11y/ | wc -l` → ≥12
    - test:a11y script exists and is green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 14 final QA — both themes, both OSes, full regression</name>
  <what-built>
    End-to-end Claude Design visual re-skin: custom chrome, sc-* tokens, Sc* primitives, 4 ported routes, overlays (palette/toasts/recording indicator), restyled export modal, dev-only TweaksPanel, user-facing theme + accent hue in Settings, WCAG AA verified on both themes.
  </what-built>
  <how-to-verify>
    **Pre-check:** `pnpm --filter @storycapture/desktop test` — full suite green (including Phase 13 ENC-12..ENC-19 export tests).

    **macOS smoke (dark + light):**
    1. `pnpm tauri:dev`
    2. Dark theme loads by default. Dashboard renders with sc-* styling.
    3. Cmd+K opens palette; navigate to Editor; editor shell uses sc-* tokens; CodeMirror still works; DSL parses; DryRun and SelectorValidator still functional.
    4. Go to /post-production/<any-story-id>: timeline scrubs, presets apply, undo/redo works.
    5. Open Export modal: all Phase 13 Advanced options present; export MP4 actually produces a file.
    6. Settings → Appearance: flip theme to Light. Every route re-renders in light mode; text is readable.
    7. Flip accent hue slider: accent color updates live across UI.
    8. Cmd+Shift+. opens TweaksPanel; density + radius toggles work; persist survives reload.
    9. Start a recording: RecordingIndicator floats bottom-right; pulsing red dot; disappears on stop.

    **Windows smoke (dark + light):** repeat the 9 steps above. Confirm caption buttons work and window resize still functional (Pitfall 2 re-check).

    **a11y smoke (automated):** `pnpm --filter @storycapture/desktop test:a11y` green.

    **Manual axe DevTools sweep (both themes):** For each screen below, run `pnpm tauri:dev`, open axe DevTools (browser extension), scan the screen on BOTH dark and light themes (flip via Settings → Appearance or TweaksPanel), record zero Serious/Critical violations for each:
    - Dashboard (dark + light)
    - Editor (dark + light)
    - Post-Production (dark + light)
    - Settings → Appearance (dark + light)
    - Export modal (dark + light)
    - Command palette (dark + light)
    - Recording indicator (dark + light)
    - Recorder route (dark + light)

    **Doc-sync grep (I1 — conditional):** Run `rg -n "packages/ui/src/tokens.css|Cursor-warm|cursor-warm" CLAUDE.md` — if it returns ANY match, edit CLAUDE.md to replace the reference with `packages/ui/src/claude-design/tokens.css` (single-line edit, keep the surrounding sentence lean per CLAUDE.md doc-sync rules). If the grep returns 0 hits, no edit required; just record "CLAUDE.md clean" in the summary.

    **Regression smoke:** `pnpm --filter @storycapture/desktop test` full suite green.
  </how-to-verify>
  <acceptance_criteria>
    - Both macOS and Windows pass the 9-step smoke on both themes
    - ENC-12..ENC-19 export regression tests green
    - No behavior regressions (no missing hotkeys, no broken IPC, no removed controls that mocks didn't show — D-09)
    - WCAG AA a11y suite green on both themes (automated — 8 test files)
    - Manual axe DevTools sweep complete for all 8 screens × 2 themes (zero Serious/Critical)
    - CLAUDE.md doc-sync grep run; if any retired-token reference found, it is updated to `packages/ui/src/claude-design/tokens.css`
    - rg "var\(--color-" apps/desktop/src returns 0
    - rg "@fontsource/lora|@fontsource-variable/outfit" apps/desktop returns 0
    - packages/ui/src/tokens.css deleted
  </acceptance_criteria>
  <resume-signal>Type "approved" once both OSes + both themes + full regression green. If any regression found, describe it and planner will enter revision mode.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @storycapture/desktop test:a11y` green
- Full `pnpm --filter @storycapture/desktop test` green (including export-modal Phase 13 suite)
- Manual macOS + Windows + both-themes smoke per Task 4 checklist
- docs/CONVENTIONS.md updated with sc-* primitive authoring pattern (CLAUDE.md mandate: keep agent docs in sync)
- docs/ARCHITECTURE.md updated if claude-design package boundaries shifted (likely a short note about packages/ui/src/claude-design as the token + primitive home)
</verification>

<success_criteria>
Phase 14 complete: visual re-skin shipped; both themes pass WCAG AA; every behavior preserved; recorder + index inherit tokens + chrome without redesign; dev + user tweaks wired via plugin-store. Docs updated.
</success_criteria>

<output>
After completion, create `.planning/phases/14-port-claude-design-into-apps-desktop/14-05-SUMMARY.md`.
Also update docs/CONVENTIONS.md with a short "Claude Design primitives" section and docs/ARCHITECTURE.md with the packages/ui/src/claude-design/ note per CLAUDE.md doc-sync mandate.
</output>
