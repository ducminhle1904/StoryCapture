---
phase: 16
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - package.json
  - apps/desktop/package.json
  - apps/web/package.json
  - packages/ui/package.json
  - packages/shared-types/package.json
  - packages/config/package.json
  - packages/story-dsl/package.json
  - biome.json
  - pnpm-lock.yaml
autonomous: true
requirements: []
tags: [dependency-upgrade, js, majors, phase-d]
must_haves:
  truths:
    - "tailwind-merge at 3.x (desktop + ui lockstep)"
    - "sonner 2.x, cmdk latest, react-resizable-panels 4.x, react-hotkeys-hook 5.x, lucide-react 1.x"
    - "zod 4.x (web) — compatible with tRPC 11 inputs"
    - "jose 6.x (web) — JWT verify green"
    - "pino 10.x + pino-pretty 13.x (web)"
    - "resend 6.x (web)"
    - "vite 8.x + @vitejs/plugin-react 6.x (desktop + ui)"
    - "typescript 6.x repo-wide across 6 manifests"
    - "biome 2.x with migrated biome.json"
  artifacts:
    - path: "apps/desktop/package.json"
      provides: "tailwind-merge 3, sonner 2, react-resizable-panels 4, react-hotkeys-hook 5, lucide-react 1, vite 8, @vitejs/plugin-react 6, typescript 6"
    - path: "apps/web/package.json"
      provides: "zod 4, jose 6, pino 10, pino-pretty 13, resend 6, typescript 6"
    - path: "biome.json"
      provides: "Migrated schema + rule IDs (Biome 2)"
  key_links:
    - from: "apps/web/package.json (zod 4)"
      to: "@trpc/server 11.16 input validators"
      via: "zod.input() usage in tRPC routers"
      pattern: "from \"zod\""
---

<objective>
Phase D — JS major bumps. Each step is its own atomic commit with pass/fail verification before the next.

Purpose: Cross committed-major lines for JS ecosystem where PRD authorized (tailwind-merge, sonner, react-resizable-panels, react-hotkeys-hook, lucide-react, zod, jose, pino, resend, vite, typescript, Biome). Root-cause migration only — NO `@ts-ignore`, NO `any`-casts, NO rule disables.
Output: 9 atomic commits (D13–D21), each green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/deps-upgrade-plan.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-CONTEXT.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md
@CLAUDE.md
@docs/CONVENTIONS.md
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| `jose` JWT verification | Auth tokens cross desktop ↔ web boundary; regression would silently break auth |
| `zod` tRPC input validation | Untrusted client input crosses API boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Spoofing | `jose` 6 JWT verify | mitigate | Confirm `jwtVerify` default algos remain `[RS256,ES256]` per OWASP; run desktop↔web auth smoke test post-bump |
| T-16-05 | Tampering | `zod` 4 strict-mode default | mitigate | Verify existing `z.object()` routers still reject unknown fields after bump (or explicitly relax with `.passthrough()` at root-cause site, NEVER via a blanket disable) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task D13: tailwind-merge 2 → 3 (desktop + ui lockstep)</name>
  <files>apps/desktop/package.json, packages/ui/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (Manifest 2 + 7)
    - apps/desktop/package.json
    - packages/ui/package.json
    - Grep: `grep -rn "tailwind-merge\|twMerge" apps/desktop/src/ packages/ui/src/`
  </read_first>
  <action>
    Edit both manifests in ONE commit: `tailwind-merge: ^3.5.0` (was `^2.5.0`). v3 targets Tailwind v4 — the pairing is intended (repo already on Tailwind 4). Pnpm install, then run `turbo run typecheck` — `twMerge` API is stable across v2→v3; if any edge cases surface, fix at root cause. Full gate. Commit:

    `refactor(16-D13-tailwind-merge): bump tailwind-merge 2 → 3 (desktop + ui lockstep)`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck && turbo run build && pnpm --filter desktop test</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"tailwind-merge": "\^3.5.0"' apps/desktop/package.json packages/ui/package.json` — returns 2 lines
    - Run `turbo run typecheck` — exits 0
    - Run `turbo run build` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D13-tailwind-merge): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task D14: UI library majors (sonner, cmdk, react-resizable-panels, react-hotkeys-hook, lucide-react)</name>
  <files>apps/desktop/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (Manifest 2)
    - apps/desktop/package.json
    - Grep: `grep -rn "from \"sonner\"\|from \"cmdk\"\|react-resizable-panels\|react-hotkeys-hook\|lucide-react" apps/desktop/src/`
    - .planning/phases/14-port-claude-design-into-apps-desktop/*-SUMMARY.md (sonner styling at Wave 4)
  </read_first>
  <action>
    FIVE separate commits, gate green between each (per PRD D14 — per-package verification):

    **D14a — sonner 1 → 2** (`apps/desktop/package.json`): `sonner: ^2.0.7`. Sonner 2 changed `Toaster` API; re-skin per Phase 14-04 conventions (`--normal-bg`/`--normal-text`/`--normal-border`/`--border-radius`/`--toast-animation-duration`). Commit `refactor(16-D14a-sonner): bump sonner 1 → 2 + re-skin per sc-* tokens`.

    **D14b — cmdk 1.0 → 1.1.1** (`apps/desktop/package.json`): `cmdk: ^1.1.1` (this is minor within v1, but included per PRD D14 group). Commit `chore(16-D14b-cmdk): bump cmdk 1.0 → 1.1.1`.

    **D14c — react-resizable-panels 2 → 4** (`apps/desktop/package.json`): `react-resizable-panels: ^4.10.0`. v3 + v4 have API changes (panel group refs, resize handle props). Fix at root cause. Commit `refactor(16-D14c-react-resizable-panels): bump react-resizable-panels 2 → 4`.

    **D14d — react-hotkeys-hook 4 → 5** (`apps/desktop/package.json`): `react-hotkeys-hook: ^5.2.4`. v5 API changes (`useHotkeys` options). Migrate call sites. Commit `refactor(16-D14d-react-hotkeys-hook): bump react-hotkeys-hook 4 → 5`.

    **D14e — lucide-react 0.460 → 1.x** (`apps/desktop/package.json`): `lucide-react: ^1.8.0`. v1 GA — icon import paths may have changed. Fix at root cause. Commit `refactor(16-D14e-lucide-react): bump lucide-react 0.460 → 1.8`.

    Each sub-commit runs full gate + `pnpm --filter desktop test`.
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck && turbo run build && pnpm --filter desktop test</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"sonner": "\^2.0.7"' apps/desktop/package.json` — returns the bumped line
    - Run `grep -n '"react-resizable-panels": "\^4.10.0"' apps/desktop/package.json` — returns the bumped line
    - Run `grep -n '"react-hotkeys-hook": "\^5.2.4"' apps/desktop/package.json` — returns the bumped line
    - Run `grep -n '"lucide-react": "\^1.8.0"' apps/desktop/package.json` — returns the bumped line
    - Run `git log --oneline | grep -cE '16-D14[a-e]'` — returns 5
    - Run `pnpm --filter desktop test` — exits 0 (or matches baseline)
  </acceptance_criteria>
  <done>5 sub-commits landed; gate green between each.</done>
</task>

<task type="auto">
  <name>Task D15: zod 3 → 4 (web) — coordinate with tRPC 11 inputs</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 11)
    - apps/web/package.json
    - Grep: `grep -rn "from \"zod\"\|z\\.object\\|\\.parse\\(" apps/web/`
  </read_first>
  <action>
    At execution time, verify tRPC 11 supports Zod 4 (check `@trpc/server` 11.16 changelog for Zod 4 compat). If tRPC 11.16 does NOT yet support Zod 4, SKIP D15 and escalate to user (per RESEARCH assumption A11, CONTEXT D-B4 deferral pattern).

    If compatible: Edit `apps/web/package.json`: `zod: ^4.3.6`. Migrate per Zod 4 breaking changes (error-map API, `.object()` strict-mode default). Fix at root cause — for any router where passthrough is intended, use `.passthrough()` at the explicit call site (NEVER add a blanket type-assertion). Full gate + `turbo run test --filter=web`. Commit:

    `refactor(16-D15-zod): bump zod 3 → 4 in apps/web + migrate tRPC input validators`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"zod": "\^4' apps/web/package.json` — returns the bumped line
    - Run `turbo run typecheck --filter=web` — exits 0
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D15-zod): …` landed OR SKIP commit with written escalation if tRPC compat missing.</done>
</task>

<task type="auto">
  <name>Task D16: jose 5 → 6 (web) — JWT verification</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Risk Surface Inventory row D16, § Security Domain V6)
    - Grep: `grep -rn "from \"jose\"\|jwtVerify\|createRemoteJWKSet" apps/web/`
  </read_first>
  <action>
    Edit `apps/web/package.json`: `jose: ^6.2.2`. jose 6 is ESM-only + some sig changes. Migrate `jwtVerify` / `createRemoteJWKSet` call sites at root cause. Per T-16-04 threat: verify default algos after bump still include RS256/ES256. Full gate + web tests + (if auth smoke test available) desktop↔web auth flow. Commit:

    `refactor(16-D16-jose): bump jose 5 → 6 in apps/web + migrate JWT verify call sites`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"jose": "\^6.2.2"' apps/web/package.json` — returns the bumped line
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D16-jose): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task D17: pino 9 → 10 + pino-pretty 11 → 13 (web)</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json
    - Grep: `grep -rn "from \"pino\"\|pino(" apps/web/`
  </read_first>
  <action>
    Edit `apps/web/package.json` in ONE commit:
    - `pino: ^10.3.1` (was 9)
    - `pino-pretty: ^13.1.3` (devDep, was 11)

    Fix logger config API drift at root cause. Full gate + web tests. Commit:

    `refactor(16-D17-pino): bump pino 9 → 10 + pino-pretty 11 → 13 in apps/web`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"pino": "\^10.3.1"' apps/web/package.json` — returns the bumped line
    - Run `grep -n '"pino-pretty": "\^13.1.3"' apps/web/package.json` — returns the bumped line
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D17-pino): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task D18: resend 4 → 6 (web)</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json
    - Grep: `grep -rn "from \"resend\"\|new Resend" apps/web/`
  </read_first>
  <action>
    Edit `apps/web/package.json`: `resend: ^6.12.2`. v5 + v6 had breaking changes in client constructor + email API. Migrate at root cause. Full gate. Commit:

    `refactor(16-D18-resend): bump resend 4 → 6 in apps/web`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"resend": "\^6.12.2"' apps/web/package.json` — returns the bumped line
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D18-resend): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task D19: vite 6 → 8 + @vitejs/plugin-react 4 → 6 (desktop + ui lockstep)</name>
  <files>apps/desktop/package.json, packages/ui/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 7)
    - apps/desktop/package.json
    - packages/ui/package.json
    - apps/desktop/vite.config.ts
  </read_first>
  <action>
    Verify `@tailwindcss/vite` 4.2.x peerDependencies includes Vite 8 (run `pnpm view @tailwindcss/vite peerDependencies` at exec time). If it doesn't, STOP and escalate.

    Edit in ONE commit:
    - `apps/desktop/package.json`: `vite: ^8.0.9`, `@vitejs/plugin-react: ^6.0.1`
    - `packages/ui/package.json`: `@vitejs/plugin-react: ^6.0.1`

    Fix any Vite 7+8 plugin API drift in `vite.config.ts`. Clear `.turbo` build cache + `node_modules/.vite`. Full gate + `pnpm --filter desktop dev` smoke (or skip dev-server smoke if non-interactive). Commit:

    `refactor(16-D19-vite): bump vite 6 → 8 + @vitejs/plugin-react 4 → 6 (desktop + ui)`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck && turbo run build && pnpm --filter desktop test</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"vite": "\^8.0.9"' apps/desktop/package.json` — returns the bumped line
    - Run `grep -n '"@vitejs/plugin-react": "\^6.0.1"' apps/desktop/package.json packages/ui/package.json` — returns 2 lines
    - Run `turbo run build` — exits 0
    - Run `pnpm --filter desktop test` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D19-vite): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task D20: TypeScript 5 → 6 repo-wide (6 manifests)</name>
  <files>package.json, apps/desktop/package.json, apps/web/package.json, packages/ui/package.json, packages/shared-types/package.json, packages/story-dsl/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 8)
    - All 6 manifests containing `typescript` devDep
    - Root `tsconfig.json` + workspace tsconfigs
  </read_first>
  <action>
    Edit ALL 6 manifests in ONE commit: `typescript: ^6.0.3` (was ^5.7.x). After `pnpm install`, run `turbo run typecheck` — expect errors across workspaces from tightened TS 6 strictness. Fix root-cause-first (NO `as any`, NO `@ts-ignore`, NO `// @ts-expect-error` unless paired with a concrete narrow explanation comment ≤1 line). If a specific error requires a larger refactor out of phase scope, STOP and escalate. Full gate + all test commands. Commit:

    `refactor(16-D20-typescript): bump typescript 5 → 6.0.3 repo-wide`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck && turbo run build && pnpm --filter desktop test && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -rn '"typescript": "\^6' package.json apps/desktop/package.json apps/web/package.json packages/ui/package.json packages/shared-types/package.json packages/story-dsl/package.json` — returns 6 lines
    - Run `grep -rn '"typescript": "\^5' package.json apps/ packages/` — returns 0 lines
    - Run `turbo run typecheck` — exits 0
    - Run `turbo run build` — exits 0
    - Run `grep -rn '@ts-ignore\|as any' apps/desktop/src apps/web/src packages/ui/src | wc -l` — must not exceed the pre-bump count (no new suppressions introduced)
  </acceptance_criteria>
  <done>Commit `refactor(16-D20-typescript): …` landed; gate green with no new `@ts-ignore`/`any`-casts.</done>
</task>

<task type="auto">
  <name>Task D21: Biome 1 → 2 + biome migrate</name>
  <files>package.json, biome.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pattern: Biome 1 → 2 migration (D21), § Don't Hand-Roll row "Biome 1 → 2")
    - package.json (root)
    - biome.json
  </read_first>
  <action>
    Edit root `package.json`: `@biomejs/biome: ^2.4.12` (was ^1.9.4). Run `pnpm install`. Then run `pnpm biome migrate --write` — this auto-rewrites `biome.json` schema URL + renamed rule IDs. Inspect the `biome.json` diff; if anything is flagged as requiring manual attention, fix at root cause (NOT by disabling the rule). Then run `pnpm biome check .` — must exit 0 (or match the pre-bump baseline; if baseline had N warnings, post-bump must be ≤ N). Full gate. Commit:

    `refactor(16-D21-biome): bump @biomejs/biome 1 → 2 + run biome migrate`

    Include the regenerated `biome.json` in the commit.
  </action>
  <verify>
    <automated>pnpm install && pnpm biome check . && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"@biomejs/biome": "\^2' package.json` — returns the bumped line
    - Run `grep -n 'schema.*biome.*2' biome.json` — returns the migrated schema URL (`2.x`)
    - Run `pnpm biome check .` — exits 0 (or matches baseline)
    - Run `turbo run typecheck` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-D21-biome): …` landed; gate green.</done>
</task>

</tasks>

<verification>
- Sequential order: D13 → D14 (5 sub-commits) → D15 → D16 → D17 → D18 → D19 → D20 → D21.
- No committed major preserved list violated (Tailwind v4, React 19, TanStack Query 5, Motion 12 all untouched — only explicitly authorized PRD majors crossed).
- After D20 + D21: run `cargo deny check advisories` + `pnpm audit --audit-level=high` (security gate at end of Phase D).
</verification>

<success_criteria>
- [ ] All 13 commits landed (D13, D14a–e, D15–D21)
- [ ] Every manifest reflects PRD major targets
- [ ] No new `@ts-ignore` / `as any` / rule disables
- [ ] `pnpm audit --audit-level=high` reports no new high-severity
</success_criteria>

<output>
After completion, create `.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-04-SUMMARY.md`.
</output>
