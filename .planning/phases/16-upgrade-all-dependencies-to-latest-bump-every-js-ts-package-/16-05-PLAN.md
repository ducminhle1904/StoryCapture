---
phase: 16
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - apps/web/package.json
  - Cargo.toml
  - apps/desktop/src-tauri/Cargo.toml
  - crates/capture/Cargo.toml
  - crates/story-parser/Cargo.toml
  - packages/shared-types/src/ipc.ts
  - CLAUDE.md
  - docs/ARCHITECTURE.md
  - pnpm-lock.yaml
  - Cargo.lock
autonomous: false
requirements: []
tags: [dependency-upgrade, gated, framework-majors, phase-e, docs-sync]
must_haves:
  truths:
    - "next-auth bumped only if dist-tags shows newer beta than 5.0.0-beta.31"
    - "Next 15 → 16 landed with App Router + RSC semantics verified"
    - "Prisma 6 → 7 (client + CLI + adapter) landed with regenerated client"
    - "windows 0.58 → 0.62 unifies capture dep graph"
    - "tauri-specta/specta RC bump + regenerated packages/shared-types/src/ipc.ts"
    - "CLAUDE.md + docs/ARCHITECTURE.md reflect final pinned versions"
  artifacts:
    - path: "apps/web/package.json"
      provides: "next 16, @prisma/client 7, prisma 7"
    - path: "Cargo.toml"
      provides: "windows 0.62 workspace pin"
    - path: "packages/shared-types/src/ipc.ts"
      provides: "Regenerated IPC types from new tauri-specta/specta"
    - path: "CLAUDE.md"
      provides: "Doc drift resolved: screencapturekit 1.5.4 pinned, windows-capture 2.0, objc2 0.6"
    - path: "docs/ARCHITECTURE.md"
      provides: "Same doc sync as CLAUDE.md"
  key_links:
    - from: "apps/desktop/src-tauri/Cargo.toml (tauri-specta RC)"
      to: "packages/shared-types/src/ipc.ts"
      via: "codegen script per docs/ARCHITECTURE.md IPC section"
      pattern: "tauri-specta"
---

<objective>
Phase E — Gated framework majors. Each step is user-gated at execution time per CLAUDE.md "Plan Before Breaking / Big Changes" rule. Plus the mandatory docs sync at E28.

Purpose: Land the highest-risk framework bumps (Next 16, Prisma 7) behind explicit user approval; collapse `windows 0.58/0.62` duplication; refresh `tauri-specta` RC + regenerate IPC; sync agent docs.
Output: Up to 7 atomic commits (E22 conditional, E24, E25, E26, E27, E28; E23 SKIP by PRD).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/deps-upgrade-plan.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-CONTEXT.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md
@CLAUDE.md
@docs/ARCHITECTURE.md
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Next 16 middleware / RSC cache | Untrusted HTTP request crosses middleware boundary |
| Prisma 7 client ↔ DB | Generated client mediates all DB access |
| tauri-specta IPC contract | Rust host ↔ TS frontend — compromised types could mask runtime bugs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | Next 16 RSC cache semantics | mitigate | Review upgrade guide at E24 exec; verify `unstable_cache` / `revalidate` call sites still honor intended invalidation |
| T-16-07 | Information Disclosure | Prisma 7 generated client output path | accept | Confirm Next.js RSC bundle does not accidentally ship `generated/prisma/*` to client (check bundle analyzer post-E25) |
| T-16-08 | Spoofing | tauri-specta RC type drift | mitigate | Diff `packages/shared-types/src/ipc.ts` post-regen; any non-trivial change requires typecheck across desktop before committing |
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task E22-pre: Decision — bump next-auth?</name>
  <decision>Bump next-auth above 5.0.0-beta.31?</decision>
  <context>
    Per PRD + RESEARCH § Open Questions #3: npm `latest` tag for next-auth = v4 stable. v5 still beta. Bump is conditional on newer v5-beta existing.
  </context>
  <options>
    <option id="bump">
      <name>Bump if newer beta exists</name>
      <pros>Stays on latest beta, picks up fixes</pros>
      <cons>Beta-to-beta may reintroduce instability</cons>
    </option>
    <option id="skip">
      <name>Skip E22 entirely</name>
      <pros>Keeps current known-good beta</pros>
      <cons>Deferred upgrade debt</cons>
    </option>
  </options>
  <resume-signal>User runs `pnpm view next-auth dist-tags` + picks `bump` or `skip`. If `bump`, proceed to E22-exec; if `skip`, skip E22 and go to E24.</resume-signal>
</task>

<task type="auto">
  <name>Task E22-exec: Bump next-auth (conditional on E22-pre = bump)</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Open Questions #3)
    - apps/web/package.json
  </read_first>
  <action>
    Only executes if E22-pre resolved to `bump`. Run `pnpm view next-auth dist-tags` to get the exact beta version (e.g., `beta: 5.0.0-beta.35`). Edit `apps/web/package.json`: `next-auth: <exact-new-beta-version>`. Full gate + `turbo run test --filter=web`. Commit:

    `chore(16-E22-next-auth): bump next-auth 5.0.0-beta.31 → <new-beta>`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"next-auth":' apps/web/package.json` — returns the NEW beta version
    - Run `turbo run test --filter=web` — exits 0
    - Commit message includes the exact dist-tags output captured at exec time
  </acceptance_criteria>
  <done>Commit landed OR task skipped per E22-pre decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task E24-pre: Decision — Next 15 → 16?</name>
  <decision>Cross the Next.js 15 → 16 committed-major line?</decision>
  <context>
    CLAUDE.md commits Next 15. PRD authorizes the bump but marks it gated. Per CLAUDE.md "Plan Before Breaking", this is a breaking change and requires explicit user approval.
  </context>
  <options>
    <option id="approve">
      <name>Approve E24</name>
      <pros>Picks up App Router + RSC improvements; unblocks Prisma 7 (E25) which we keep gated separately anyway</pros>
      <cons>RSC cache semantics + middleware API may have changed; test surface not fully known until bump</cons>
    </option>
    <option id="defer">
      <name>Defer E24 to a dedicated later phase</name>
      <pros>Keeps Phase 16 narrowly scoped to dep bumps; Next 16 gets its own planning round</pros>
      <cons>E25 (Prisma 7) may also defer — lose the batch</cons>
    </option>
  </options>
  <resume-signal>User: `approve` or `defer`.</resume-signal>
</task>

<task type="auto">
  <name>Task E24-exec: Next 15 → 16 (web)</name>
  <files>apps/web/package.json, apps/web/next.config.js (or .ts), pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 10)
    - apps/web/package.json
    - apps/web/next.config.* (whichever extension)
    - Grep: `grep -rn "next.config\|export const revalidate\|unstable_cache" apps/web/`
    - Next 16 upgrade guide (fetch via WebFetch at exec time — Next docs change frequently)
  </read_first>
  <action>
    Only executes if E24-pre = `approve`. Read Next 16 upgrade guide at exec time. Edit `apps/web/package.json`: `next: ^16.2.4`. KEEP `@prisma/client` on 6.x (E25 is separate — do not couple). Run `pnpm install`, then migrate `next.config` schema + any `unstable_cache`/`revalidate` call sites at root cause. Verify tRPC 11 + NextAuth v5 still compatible (check their changelogs at exec time). Full gate + `turbo run build --filter=web` + `turbo run test --filter=web`. Commit:

    `refactor(16-E24-next): bump next 15 → 16 in apps/web`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"next": "\^16' apps/web/package.json` — returns the bumped line
    - Run `grep -n '"@prisma/client": "\^6' apps/web/package.json` — STILL returns 6.x (decoupled from E25)
    - Run `turbo run build --filter=web` — exits 0
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit landed OR task skipped per E24-pre decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task E25-pre: Decision — Prisma 6 → 7?</name>
  <decision>Cross the Prisma 6 → 7 committed-major line?</decision>
  <context>
    CLAUDE.md commits Prisma 6. PRD authorizes gated bump. `@auth/prisma-adapter` must still support Prisma 7 (verify at exec time). Client output path + migration SQL format may shift.
  </context>
  <options>
    <option id="approve">
      <name>Approve E25</name>
      <pros>Current stack; pairs well with Next 16 if approved</pros>
      <cons>Migration SQL + generated-client surface may drift</cons>
    </option>
    <option id="defer">
      <name>Defer E25</name>
      <pros>Low-risk: keep current known-good Prisma 6</pros>
      <cons>Postpones inevitable upgrade</cons>
    </option>
  </options>
  <resume-signal>User: `approve` or `defer`.</resume-signal>
</task>

<task type="auto">
  <name>Task E25-exec: Prisma 6 → 7 (web)</name>
  <files>apps/web/package.json, apps/web/prisma/schema.prisma (no non-upgrade edits), pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 9)
    - apps/web/package.json
    - apps/web/prisma/schema.prisma (do NOT modify)
    - Grep: `grep -rn "@prisma/client\|PrismaClient" apps/web/`
    - `@auth/prisma-adapter` peerDeps (run `pnpm view @auth/prisma-adapter peerDependencies` at exec time)
  </read_first>
  <action>
    Only executes if E25-pre = `approve`. Verify `@auth/prisma-adapter` supports Prisma 7 first. If not, STOP and escalate.

    Edit `apps/web/package.json` in ONE commit:
    - `@prisma/client: ^7.7.0` (was 6)
    - `prisma: ^7.7.0` (devDep, was 6)

    Run `pnpm install`, then `pnpm prisma generate` (regenerates client output). Run `pnpm prisma migrate status` — existing migrations must still apply cleanly. Do NOT edit `schema.prisma` for anything besides upgrade necessity. Fix any generated-client import breakage in tRPC routers at root cause. Full gate + `turbo run test --filter=web`. Commit:

    `refactor(16-E25-prisma): bump prisma + @prisma/client 6 → 7 in apps/web`
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter web exec prisma generate && pnpm --filter web exec prisma migrate status && turbo run typecheck --filter=web && turbo run build --filter=web && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"@prisma/client": "\^7' apps/web/package.json` — returns the bumped line
    - Run `grep -n '"prisma": "\^7' apps/web/package.json` — returns the bumped line (devDep)
    - Run `pnpm --filter web exec prisma migrate status` — exits 0
    - Run `turbo run test --filter=web` — exits 0
  </acceptance_criteria>
  <done>Commit landed OR task skipped per E25-pre decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task E26-pre: Decision — windows 0.58 → 0.62?</name>
  <decision>Bump `windows` crate 0.58 → 0.62 in capture + workspace?</decision>
  <context>
    `crates/capture/Cargo.toml` intentionally pins `windows = "0.58"` while `windows-capture 2.0` transitively pulls `windows 0.62`. Bumping our direct dep collapses duplication but is a WGC-surface breaking change. Requires Windows runner for full verification.
  </context>
  <options>
    <option id="approve">
      <name>Approve E26</name>
      <pros>Single version in graph; future-proof</pros>
      <cons>WGC surface changes; Windows CI is the true verification gate</cons>
    </option>
    <option id="defer">
      <name>Defer E26</name>
      <pros>Multi-version in graph accepted for now; low immediate pain</pros>
      <cons>Tech debt persists</cons>
    </option>
  </options>
  <resume-signal>User: `approve` or `defer`.</resume-signal>
</task>

<task type="auto">
  <name>Task E26-exec: windows 0.58 → 0.62 (capture + workspace)</name>
  <files>Cargo.toml, crates/capture/Cargo.toml, Cargo.lock</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Risk Surface Inventory row E26)
    - Grep: `grep -rn "use windows::\|Direct3D11\|GraphicsCaptureItem" crates/capture/src/windows/`
    - crates/capture/Cargo.toml (comment explaining 0.58 pin)
  </read_first>
  <action>
    Only executes if E26-pre = `approve`. Edit:
    - Workspace `Cargo.toml`: `windows = "0.62.2"` (was 0.58)
    - `crates/capture/Cargo.toml`: `windows = "0.62"` (remove or update the comment explaining 0.58 pin — document the bump rationale instead, ≤1 line per CLAUDE.md)

    Run `cargo check --workspace`, then Windows-target check if available (`cargo check --target x86_64-pc-windows-msvc -p capture`). Root-cause-fix any WGC API drift. Confirm `cargo tree -p capture | grep ^windows` shows only one version. If no Windows runner available, commit body must note "Windows CI gate deferred to next push" per RESEARCH § Open Questions #5. Commit:

    `refactor(16-E26-windows): bump windows 0.58 → 0.62 (capture + workspace) to unify graph`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'windows = "0.62' Cargo.toml crates/capture/Cargo.toml` — returns 2 bumped lines
    - Run `grep -rn 'windows = "0.58"' Cargo.toml crates/` — returns 0 lines
    - Run `cargo tree -p capture | grep "^windows v" | sort -u | wc -l` — returns 1
    - Run `cargo check --workspace --all-targets` — exits 0
  </acceptance_criteria>
  <done>Commit landed OR task skipped per E26-pre decision.</done>
</task>

<task type="auto">
  <name>Task E27: tauri-specta / specta RC bump + regenerate IPC types</name>
  <files>apps/desktop/src-tauri/Cargo.toml, crates/story-parser/Cargo.toml, packages/shared-types/src/ipc.ts, Cargo.lock</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 6, § Pattern: tauri-specta RC bump + regen (E27))
    - apps/desktop/src-tauri/Cargo.toml (current `tauri-specta =2.0.0-rc.21`, `specta =2.0.0-rc.22`)
    - crates/story-parser/Cargo.toml (optional specta feature)
    - docs/ARCHITECTURE.md (IPC section describing codegen script)
    - packages/shared-types/src/ipc.ts (auto-generated — never hand-edit)
  </read_first>
  <action>
    At exec time, run `cargo search tauri-specta` + `cargo search specta` to get current 2.x-rc latest. If current pins are already latest, E27 is a no-op — note in commit or skip. Otherwise:

    Edit:
    - `apps/desktop/src-tauri/Cargo.toml`: `tauri-specta = "=<new-rc>"`, `specta = "=<new-rc>"`, `specta-typescript = "0.0.11"`
    - `crates/story-parser/Cargo.toml`: specta optional feature version bump to match

    Run existing IPC codegen script (per `docs/ARCHITECTURE.md` IPC section — locate the script, likely a pnpm task or Rust binary). This regenerates `packages/shared-types/src/ipc.ts`. Inspect the diff; if non-trivial, run `turbo run typecheck` to find consumer breakage in `apps/desktop/src/ipc/*.ts` and fix at root cause. Full gate. Commit:

    `refactor(16-E27-tauri-specta): bump tauri-specta + specta RC + regenerate ipc.ts`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run --workspace && turbo run typecheck && turbo run build && pnpm --filter desktop test</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'tauri-specta = "=' apps/desktop/src-tauri/Cargo.toml` — returns the new RC pin
    - Run `grep -n 'specta = "=' apps/desktop/src-tauri/Cargo.toml` — returns the new RC pin
    - Run `head -5 packages/shared-types/src/ipc.ts` — header still marks file as auto-generated (not hand-edited)
    - Run `turbo run typecheck` — exits 0
    - `packages/shared-types/src/ipc.ts` in commit diff (regenerated output committed)
  </acceptance_criteria>
  <done>Commit `refactor(16-E27-tauri-specta): …` landed; gate green. If no-op, commit omitted and noted in 16-05-SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task E28: Docs sync (CLAUDE.md + docs/ARCHITECTURE.md)</name>
  <files>CLAUDE.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - CLAUDE.md (current references to `screencapturekit 1.70.x`, `windows-capture 1.5.x`, `objc2 0.5`)
    - docs/ARCHITECTURE.md (may also reference outdated versions)
    - Final pinned versions from this phase's preceding commits (`grep -n 'screencapturekit\|windows-capture\|objc2' crates/capture/Cargo.toml Cargo.toml`)
  </read_first>
  <action>
    Update agent-facing docs per CLAUDE.md "Keep Agent Docs In Sync" rule. Edit:

    **CLAUDE.md** — Technology Stack section:
    - `screencapturekit` reference: change from `1.70.x` to `=1.5.4` (pinned per CLAUDE.md risk flag)
    - `windows-capture` reference: change from `1.5.x` to `2.0` (code already uses 2.0.0)
    - `objc2` reference: change from `0.5` to `0.6.4`
    - If Phase E26 landed: update `windows` reference to `0.62.2`
    - If Phase D20 landed: update `typescript` reference to `6.x`
    - If Phase D21 landed: update `Biome` reference to `2.x`
    - If Phase D19 landed: update `vite` reference to `8.x`
    - If Phase E24 landed: update `next` reference to `16`
    - If Phase E25 landed: update `prisma` / `@prisma/client` reference to `7`

    **docs/ARCHITECTURE.md** — mirror the same pin updates wherever versioned references appear.

    Keep CLAUDE.md lean per project rule: short quick-rules bullets; push prose into `docs/ARCHITECTURE.md`. NEVER remove existing risk flags; only update version numbers.

    Commit:

    `docs(16-E28): sync CLAUDE.md + docs/ARCHITECTURE.md with Phase 16 final pins`
  </action>
  <verify>
    <automated>grep -E "screencapturekit|windows-capture|objc2" CLAUDE.md docs/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -c "screencapturekit.*1.70" CLAUDE.md docs/ARCHITECTURE.md` — returns 0 matches (stale ref removed)
    - Run `grep -c "screencapturekit.*1.5.4" CLAUDE.md` — returns ≥1 (pinned version documented)
    - Run `grep -c "windows-capture.*1.5.x\|windows-capture.*1\.5\." CLAUDE.md docs/ARCHITECTURE.md` — returns 0
    - Run `grep -c "windows-capture.*2.0" CLAUDE.md` — returns ≥1
    - Run `grep -c "objc2.*0.5" CLAUDE.md docs/ARCHITECTURE.md` — returns 0
    - Run `grep -c "objc2.*0.6" CLAUDE.md` — returns ≥1
    - Run `git log --oneline -1` — shows `docs(16-E28): …`
  </acceptance_criteria>
  <done>Commit `docs(16-E28): …` landed; stale version references eliminated.</done>
</task>

</tasks>

<verification>
- All decision checkpoints resolved before their exec tasks run.
- E22, E23 (SKIP per PRD), E24, E25, E26 are all gated; only approved ones commit.
- E27 + E28 always run (non-gated final tasks).
- At end of Phase E: run `cargo deny check advisories bans licenses` + `pnpm audit --audit-level=high` — no new high-severity.
</verification>

<success_criteria>
- [ ] E22-pre + E24-pre + E25-pre + E26-pre resolved with user choices recorded in SUMMARY
- [ ] All approved exec tasks landed as atomic commits
- [ ] E27 regenerated `packages/shared-types/src/ipc.ts` in-commit
- [ ] E28 docs sync complete — no stale version references remain
- [ ] Final gate green: `cargo check + cargo nextest + turbo run typecheck + turbo run build`
- [ ] `cargo deny check` + `pnpm audit --audit-level=high` clean
</success_criteria>

<output>
After completion, create `.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-05-SUMMARY.md` and update `.planning/STATE.md` with Phase 16 completion status.
</output>
