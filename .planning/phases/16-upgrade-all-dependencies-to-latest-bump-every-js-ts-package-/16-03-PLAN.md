---
phase: 16
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - apps/desktop/src-tauri/Cargo.toml
  - crates/automation/Cargo.toml
  - crates/capture/Cargo.toml
  - crates/effects/Cargo.toml
  - crates/encoder/Cargo.toml
  - crates/intelligence/Cargo.toml
  - crates/storage/Cargo.toml
  - crates/story-parser/Cargo.toml
  - crates/util/Cargo.toml
autonomous: true
requirements: []
tags: [dependency-upgrade, rust, breaking, phase-c]
must_haves:
  truths:
    - "objc2 family unified at 0.6 across workspace + desktop host + capture"
    - "rusqlite at 0.39 across src-tauri + storage + effects + encoder, migrations intact"
    - "reqwest at 0.13 across src-tauri + intelligence, SSE streaming passes"
    - "sha2 at 0.11 across util + intelligence + src-tauri"
    - "scraper at 0.26 in automation; DOM validator tests pass"
    - "nix at 0.31 in capture unix; mkfifo path builds"
    - "tower 0.5, schemars 1.x, rand 0.10, toml 1.x, ts-rs 12 landed in separate commits"
    - "serde_yaml replaced in intelligence dev-deps"
  artifacts:
    - path: "Cargo.toml"
      provides: "objc2 = 0.6, rusqlite = 0.39, reqwest = 0.13, sha2 = 0.11, windows pinned (deferred E26)"
    - path: "crates/intelligence/Cargo.toml"
      provides: "schemars 1.x, rand 0.10, toml 1.x, tower 0.5, serde_yml/serde_yaml_ng replacement"
  key_links:
    - from: "crates/storage/src/{app_db,project_db,phase3}.rs"
      to: "rusqlite 0.39 API"
      via: "params!, OptionalExtension, Error::FromSqlConversionFailure"
      pattern: "rusqlite::"
    - from: "crates/intelligence/src/llm/{anthropic,openai}.rs"
      to: "reqwest 0.13 bytes_stream + eventsource-stream"
      via: "SSE streaming"
      pattern: "bytes_stream|eventsource"
---

<objective>
Phase C — Coordinated 0.x breaking Rust bumps. Each coordination group lands as its own commit; each must pass the per-commit gate before moving on. 8 sequential tasks.

Purpose: Upgrade every 0.x crate with API-surface breakage. Root-cause fixes only — no `#[allow(deprecated)]` shortcuts.
Output: 8 atomic commits (C5–C12), each green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/notes/deps-upgrade-plan.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-CONTEXT.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md
@CLAUDE.md
@docs/ARCHITECTURE.md
@docs/DOMAIN.md
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM provider HTTPS | `reqwest` 0.13 + SSE stream from Anthropic/OpenAI — untrusted response crosses process boundary |
| SQLite file on disk | `rusqlite` 0.39 reads local user DB — trust boundary at file-format layer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | `reqwest` 0.13 TLS defaults | mitigate | Verify `rustls-tls` feature still enabled post-bump; reject if default silently switched to `native-tls` |
| T-16-02 | Tampering | `sha2` 0.11 digest API change | mitigate | Rerun author-snapshot hashing tests; ensure SHA-256 output length = 32 bytes, hex-encoded string unchanged for known input |
| T-16-03 | Denial of Service | SSE stream malformed events post-reqwest 0.13 | accept | Existing provider probe tests cover; no new attack surface |
</threat_model>

<tasks>

<task type="auto">
  <name>Task C5: objc2 family 0.5 → 0.6 (workspace + desktop host + capture)</name>
  <files>Cargo.toml, apps/desktop/src-tauri/Cargo.toml, crates/capture/Cargo.toml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (§ Rust workspace + § src-tauri + § capture)
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 3, § Risk Surface Inventory row C5)
    - crates/encoder/Cargo.toml (already on 0.6 — reference alignment)
    - crates/capture/src/macos/ (grep `use objc2`, `ClassType`, `msg_send!`, `Retained`, `AnyObject`, `NSString::from_str`)
    - apps/desktop/src-tauri/src/lib.rs (uses `ClassType`)
  </read_first>
  <action>
    Bump in ONE commit (all macOS-affected Obj-C FFI):

    Workspace `Cargo.toml`:
    - `objc2 = "0.6.4"` (was 0.5)
    - If workspace declares `objc2-app-kit`, `objc2-foundation`, `objc2-core-media`, `objc2-core-video`, `objc2-av-foundation` — bump to `0.3.2` (lockstep with encoder).

    `apps/desktop/src-tauri/Cargo.toml` (macOS target section):
    - `objc2 = "0.6.4"`, `objc2-app-kit = "0.3.2"`, `objc2-foundation = "0.3.2"`

    `crates/capture/Cargo.toml` (macOS target section):
    - `objc2 = "0.6.4"`, `objc2-foundation = "0.3.2"`. LEAVE `screencapturekit = "=1.5.4"` untouched. LEAVE `core-graphics = "0.24"` untouched (bump to 0.25 handled here if PRD warrants — confirm at exec time via `cargo search core-graphics`). Confirm `screencapturekit 1.5.4` transitive `objc2` dep is compatible (inspect Cargo.lock post-bump).

    Run `cargo check --target aarch64-apple-darwin --workspace` to collect ALL breakage. Root-cause-fix: port `ClassType`, `Retained`, `msg_send!`, `NSString::from_str` sites to the new 0.6 retained-pointer model. NO `#[allow(deprecated)]`. Then `cargo check --target x86_64-pc-windows-msvc --workspace` to confirm Windows unaffected. Run gate. Commit:

    `refactor(16-C5-objc2): unify objc2 family at 0.6.4 across workspace + desktop + capture`

    If Windows-build can't be verified on current runner, note in commit body "Windows CI gate deferred to next push" (best-effort per RESEARCH § Open Questions #5).
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'objc2 = "0.6.4"' Cargo.toml` — returns the bumped line
    - Run `grep -n 'objc2 = "0.6.4"' crates/capture/Cargo.toml` — returns the bumped line
    - Run `grep -rn 'objc2 = "0.5"' Cargo.toml apps/desktop/src-tauri/Cargo.toml crates/` — returns 0 lines
    - Run `cargo check --workspace --all-targets` — exits 0
    - Run `cargo nextest run --workspace` — exits 0
    - Run `cargo tree -p capture | grep objc2 | sort -u | wc -l` — returns 1 (single version, no duplicates)
  </acceptance_criteria>
  <done>Commit `refactor(16-C5-objc2): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C6: rusqlite 0.34 → 0.39 (src-tauri + storage + effects + encoder)</name>
  <files>Cargo.toml, apps/desktop/src-tauri/Cargo.toml, crates/storage/Cargo.toml, crates/effects/Cargo.toml, crates/encoder/Cargo.toml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (PRD § Rust src-tauri, storage, effects, encoder)
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 1, § Pattern: Rusqlite 0.34 → 0.39 coordination (C6), § Risk Surface Inventory row C6)
    - crates/storage/src/app_db.rs
    - crates/storage/src/project_db.rs
    - crates/storage/src/phase3.rs
    - Grep: `grep -rn "rusqlite::\|params!\|OptionalExtension\|prepare_cached\|Error::FromSqlConversionFailure" crates/storage/ crates/effects/ crates/encoder/ apps/desktop/src-tauri/`
  </read_first>
  <action>
    Verify `rusqlite_migration` 2.5.x compatibility with `rusqlite` 0.39 first: `cargo search rusqlite_migration` → confirm release notes list `rusqlite = "0.39"`. If not compatible, STOP and escalate to user (per CLAUDE.md "No Workarounds").

    Edit workspace + member manifests in ONE commit:
    - Workspace `Cargo.toml`: `rusqlite = { version = "0.39", features = ["bundled"] }`
    - `apps/desktop/src-tauri/Cargo.toml`: `rusqlite = "0.39"` (if not using workspace inheritance)
    - `crates/storage/Cargo.toml`: `rusqlite = "0.39"`
    - `crates/effects/Cargo.toml`: `rusqlite = { version = "0.39", optional = true }`
    - `crates/encoder/Cargo.toml`: `rusqlite = "0.39"`

    Run `cargo check -p storage -p effects -p encoder -p storycapture` to collect ALL breakage. Root-cause-fix API drift: `Error::FromSqlConversionFailure` constructor arity, `prepare_cached` lifetimes, `Transaction::commit`, `ToSql`/`FromSql` bounds. NO workarounds. Then full gate. Integration tests at `crates/storage/tests/` MUST pass. Commit:

    `refactor(16-C6-rusqlite): bump rusqlite 0.34 → 0.39 across src-tauri + storage + effects + encoder`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p storage -p effects -p encoder && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'rusqlite = ' Cargo.toml` — shows version `0.39`
    - Run `grep -rn 'rusqlite = "0.34"' crates/ apps/desktop/src-tauri/` — returns 0 lines
    - Run `cargo nextest run -p storage` — exits 0 (integration tests at `crates/storage/tests/` pass)
    - Run `cargo nextest run -p effects -p encoder` — exits 0
    - Run `cargo check --workspace --all-targets` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-C6-rusqlite): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C7: reqwest 0.12 → 0.13 (src-tauri + intelligence)</name>
  <files>Cargo.toml, apps/desktop/src-tauri/Cargo.toml, crates/intelligence/Cargo.toml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 2, § Risk Surface Inventory row C7)
    - crates/intelligence/src/llm/anthropic.rs
    - crates/intelligence/src/llm/openai.rs
    - Grep: `grep -rn "reqwest::\|Client::builder\|bytes_stream\|ClientBuilder" crates/intelligence/ apps/desktop/src-tauri/`
  </read_first>
  <action>
    Verify `eventsource-stream` 0.2.3 supports `reqwest` 0.13 first (inspect its Cargo.toml on crates.io). If not, STOP and escalate.

    Edit:
    - Workspace `Cargo.toml` (if reqwest is workspace dep): `reqwest = "0.13"` with feature parity — MUST preserve `rustls-tls` / streaming features exactly as currently declared (check pre-bump feature list; 0.13 may rename feature flags).
    - `apps/desktop/src-tauri/Cargo.toml`: `reqwest = "0.13"` feature parity
    - `crates/intelligence/Cargo.toml`: `reqwest = "0.13"` feature parity

    Fix any API drift in `ClientBuilder`, `bytes_stream()`, feature-flag names. Run `cargo nextest -p intelligence` — wiremock provider probes MUST pass. Run full gate. Commit:

    `refactor(16-C7-reqwest): bump reqwest 0.12 → 0.13 in src-tauri + intelligence`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p intelligence && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -rn 'reqwest = "0.13' Cargo.toml apps/desktop/src-tauri/Cargo.toml crates/intelligence/Cargo.toml` — returns the bumped lines
    - Run `grep -rn 'reqwest = "0.12"' Cargo.toml apps/desktop/src-tauri/Cargo.toml crates/intelligence/Cargo.toml` — returns 0 lines
    - Run `cargo nextest run -p intelligence` — exits 0 (SSE + provider probe tests)
    - Run `cargo tree -p intelligence | grep reqwest | sort -u | wc -l` — returns 1
  </acceptance_criteria>
  <done>Commit `refactor(16-C7-reqwest): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C8: sha2 0.10 → 0.11 (util + intelligence + src-tauri)</name>
  <files>Cargo.toml, apps/desktop/src-tauri/Cargo.toml, crates/util/Cargo.toml, crates/intelligence/Cargo.toml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Risk Surface Inventory row C8)
    - Grep: `grep -rn "sha2::\|Sha256::\|Digest::" crates/util/ crates/intelligence/ apps/desktop/src-tauri/`
  </read_first>
  <action>
    Edit:
    - Workspace `Cargo.toml` (if present): `sha2 = "0.11"`
    - `apps/desktop/src-tauri/Cargo.toml`: `sha2 = "0.11"`
    - `crates/util/Cargo.toml`: `sha2 = "0.11"`
    - `crates/intelligence/Cargo.toml`: `sha2 = "0.11"`

    Fix any Digest API drift (0.11 may rename `Digest::new` / `Digest::finalize`). Root-cause-fix. Run `cargo nextest -p util -p intelligence` plus full gate. Verify author-snapshot hashing tests green (no hash-output-string diff for known input — per T-16-02 threat mitigation). Commit:

    `refactor(16-C8-sha2): bump sha2 0.10 → 0.11`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p util -p intelligence && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -rn 'sha2 = "0.11' Cargo.toml apps/desktop/src-tauri/Cargo.toml crates/util/Cargo.toml crates/intelligence/Cargo.toml` — returns the bumped lines
    - Run `grep -rn 'sha2 = "0.10"' Cargo.toml apps/desktop/src-tauri/Cargo.toml crates/util/Cargo.toml crates/intelligence/Cargo.toml` — returns 0 lines
    - Run `cargo nextest run -p util -p intelligence` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-C8-sha2): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C9: scraper 0.20 → 0.26 (automation)</name>
  <files>crates/automation/Cargo.toml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Risk Surface Inventory row C9)
    - Grep: `grep -rn "scraper::\|Selector::parse\|Html::parse" crates/automation/`
  </read_first>
  <action>
    Edit `crates/automation/Cargo.toml`: `scraper = "0.26"` (was 0.20). html5ever ABI bump — fix any Selector API drift. Run `cargo nextest -p automation` (author-time DOM validator tests). Full gate. Commit:

    `refactor(16-C9-scraper): bump scraper 0.20 → 0.26 in automation`
  </action>
  <verify>
    <automated>cargo check -p automation && cargo nextest run -p automation && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'scraper = "0.26"' crates/automation/Cargo.toml` — returns the bumped line
    - Run `cargo nextest run -p automation` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-C9-scraper): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C10: nix 0.29 → 0.31 (capture unix audio fifo)</name>
  <files>crates/capture/Cargo.toml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Risk Surface Inventory row C10)
    - Grep: `grep -rn "nix::\|mkfifo\|fcntl" crates/capture/src/`
  </read_first>
  <action>
    Edit `crates/capture/Cargo.toml`: `nix = "0.31"` (was 0.29, unix target). Fix `mkfifo`/`fcntl` API drift (0.30/0.31 changed module paths). Run `cargo check -p capture` on macOS + Linux targets. Full gate. Commit:

    `refactor(16-C10-nix): bump nix 0.29 → 0.31 in capture unix audio fifo`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p capture && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'nix = "0.31' crates/capture/Cargo.toml` — returns the bumped line (or `nix = { version = "0.31"` if object form)
    - Run `cargo check -p capture --target aarch64-apple-darwin` — exits 0
    - Run `cargo nextest run -p capture` — exits 0
  </acceptance_criteria>
  <done>Commit `refactor(16-C10-nix): …` landed; gate green.</done>
</task>

<task type="auto">
  <name>Task C11: tower/schemars/rand/toml/ts-rs (5 sub-commits)</name>
  <files>crates/intelligence/Cargo.toml, crates/effects/Cargo.toml, crates/story-parser/Cargo.toml</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 4, 5, § Risk Surface Inventory rows C11)
    - crates/intelligence/src/llm/retry.rs (rand usage)
    - crates/intelligence/src/nl/schemas.rs (schemars usage + insta snapshot)
    - crates/intelligence/src/tts/script.rs (schemars usage)
    - crates/intelligence/src/bin/eval_report*.rs (toml usage)
    - Grep: `grep -rn "tower::" crates/intelligence/`
    - Grep: `grep -rn "#\[derive.*TS" crates/effects/ crates/story-parser/`
  </read_first>
  <action>
    Five SEPARATE commits, gate green between each:

    **C11a — tower 0.4 → 0.5** (`crates/intelligence/Cargo.toml`): `tower = "0.5"`. Fix Service/Layer API drift. Gate + commit `refactor(16-C11a-tower): bump tower 0.4 → 0.5 in intelligence`.

    **C11b — schemars 0.8 → 1.2.1** (`crates/intelligence/Cargo.toml`): `schemars = "1.2.1"`. Fix `JsonSchema` derive + `schema_for!` API drift. Inspect `insta` snapshots at `crates/intelligence/src/nl/schemas.rs` (line ~233) — if schema output string diff is material (draft-07 → draft-2020-12), update the snapshot AND note it in commit body (LLM prompts that embed the schema may need Phase-X follow-up — escalate to user). Commit `refactor(16-C11b-schemars): bump schemars 0.8 → 1.2.1 in intelligence`.

    **C11c — rand 0.8 → 0.10** (`crates/intelligence/Cargo.toml`): `rand = "0.10"`. Repo uses only `rand::random::<u64>()` in `llm/retry.rs` — confirm `rand::random` still exists in 0.10 or migrate to `rand::rng().random::<u64>()`. Commit `refactor(16-C11c-rand): bump rand 0.8 → 0.10 in intelligence`.

    **C11d — toml 0.8 → 1.1.2** (`crates/intelligence/Cargo.toml`): `toml = "1.1.2"`. Fix error-type changes in `eval_report` bin. Commit `refactor(16-C11d-toml): bump toml 0.8 → 1.1.2 in intelligence`.

    **C11e — ts-rs 10 → 12.0.1** (`crates/effects/Cargo.toml` + `crates/story-parser/Cargo.toml`, lockstep): `ts-rs = "12.0.1"`. Fix `#[derive(TS)]` attribute drift. Regenerate TS types under `generated/` and diff — if material, commit regenerated output with the crate bump. Commit `refactor(16-C11e-ts-rs): bump ts-rs 10 → 12.0.1 (effects + story-parser lockstep)`.

    Each sub-commit must pass the gate before moving to the next.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p intelligence -p effects -p story-parser && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'tower = "0.5' crates/intelligence/Cargo.toml` — returns the bumped line
    - Run `grep -n 'schemars = "1' crates/intelligence/Cargo.toml` — returns the bumped line
    - Run `grep -n 'rand = "0.10' crates/intelligence/Cargo.toml` — returns the bumped line
    - Run `grep -n 'toml = "1' crates/intelligence/Cargo.toml` — returns the bumped line
    - Run `grep -n 'ts-rs = "12' crates/effects/Cargo.toml crates/story-parser/Cargo.toml` — returns 2 bumped lines
    - Run `git log --oneline | grep -c '16-C11'` — returns 5 (five separate commits)
    - Run `cargo nextest run -p intelligence -p effects -p story-parser` — exits 0
  </acceptance_criteria>
  <done>All 5 sub-commits landed; gate green between each.</done>
</task>

<task type="auto">
  <name>Task C12: Replace deprecated serde_yaml with serde_yml or serde_yaml_ng</name>
  <files>crates/intelligence/Cargo.toml, crates/intelligence/tests/</files>
  <read_first>
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pitfall 12, § Open Questions #2)
    - crates/intelligence/Cargo.toml (current `serde_yaml = "0.9"` dev-dep)
    - Grep: `grep -rn "serde_yaml::" crates/intelligence/tests/`
  </read_first>
  <action>
    At execution time, decide the replacement fork:
    - Run `cargo search serde_yml` and `cargo search serde_yaml_ng`
    - Compare latest publish dates on crates.io
    - Pick the more actively maintained fork with cleanest swap. Cite the comparison in commit body.

    Edit `crates/intelligence/Cargo.toml`:
    - Remove `serde_yaml = "0.9"` from `[dev-dependencies]`
    - Add `<chosen-fork> = "<latest>"` (e.g., `serde_yaml_ng = "0.10"` — verify latest at exec time)

    In test files using `serde_yaml::`: either update imports to the new crate name directly, OR add `use <chosen-fork> as serde_yaml;` at the top of each test file. Prefer direct import updates (cleanest, no alias layer).

    Run `cargo nextest -p intelligence --tests`. Full gate. Commit:

    `refactor(16-C12-serde-yaml): replace deprecated serde_yaml with <chosen-fork> in intelligence dev-deps`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run -p intelligence --tests && cargo nextest run --workspace && turbo run typecheck && turbo run build</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'serde_yaml = "0.9"' crates/intelligence/Cargo.toml` — returns 0 lines
    - Run `grep -En 'serde_yml|serde_yaml_ng' crates/intelligence/Cargo.toml` — returns the replacement line
    - Run `cargo nextest run -p intelligence --tests` — exits 0
    - Run `cargo deny check advisories` — does NOT list `serde_yaml` as advisory warning
  </acceptance_criteria>
  <done>Commit `refactor(16-C12-serde-yaml): …` landed; gate green.</done>
</task>

</tasks>

<verification>
- Tasks execute sequentially: C5 → C6 → C7 → C8 → C9 → C10 → C11 (5 sub-commits) → C12.
- Per-commit gate exits 0 after each.
- `cargo deny check advisories` at end of Phase C — no new advisories introduced.
- `pnpm audit --audit-level=high` at end of Phase C — no new high severity.
</verification>

<success_criteria>
- [ ] 12 atomic commits landed (C5, C6, C7, C8, C9, C10, C11a–e, C12)
- [ ] All 0.x crates at PRD target versions
- [ ] No workarounds introduced (no `#[allow(deprecated)]`, no test skips)
- [ ] `cargo deny check advisories bans licenses` passes
- [ ] Integration tests (storage, intelligence, automation, capture) green
</success_criteria>

<output>
After completion, create `.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-03-SUMMARY.md`.
</output>
