---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - apps/desktop/package.json
  - apps/web/package.json
  - packages/ui/package.json
  - packages/shared-types/package.json
  - scripts/playwright-sidecar/package.json
  - scripts/notarize/smoke-app/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: []
tags: [dependency-upgrade, rust, npm, phase-a, safe-bumps]
must_haves:
  truths:
    - "Workspace Rust patch/minor crates pinned to PRD target versions"
    - "Per-workspace npm safe-bump packages pinned to PRD targets"
    - "cargo check + cargo nextest green on workspace"
    - "turbo run typecheck + turbo run build green repo-wide"
    - "pnpm-lock.yaml + Cargo.lock regenerated and committed"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace [workspace.dependencies] with A1 bumps"
      contains: "tokio = \"1.52\""
    - path: "apps/desktop/package.json"
      provides: "Desktop npm safe bumps"
    - path: "apps/web/package.json"
      provides: "Web npm safe bumps"
    - path: "pnpm-lock.yaml"
      provides: "Regenerated after all A2 edits"
    - path: "Cargo.lock"
      provides: "Regenerated after A1"
  key_links:
    - from: "Cargo.toml [workspace.dependencies]"
      to: "crates/*/Cargo.toml"
      via: "workspace = true inheritance"
      pattern: "workspace = true"
---

<objective>
Phase A — Safe patch/minor-only bumps across workspace Rust crates and every npm workspace. No API surface risk.

Purpose: Clear the easy wins first; reduce noise before Phase B/C/D/E breaking bumps.
Output: 2 atomic commits (A1 Rust, A2 npm) passing the per-commit verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/notes/deps-upgrade-plan.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-CONTEXT.md
@.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md
@CLAUDE.md
@docs/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task A1: Workspace Rust patch/minor bumps (single commit)</name>
  <files>Cargo.toml, Cargo.lock, apps/desktop/src-tauri/Cargo.toml, crates/automation/Cargo.toml, crates/capture/Cargo.toml, crates/effects/Cargo.toml, crates/encoder/Cargo.toml, crates/intelligence/Cargo.toml, crates/storage/Cargo.toml, crates/story-parser/Cargo.toml, crates/util/Cargo.toml, scripts/notarize/smoke-app/src-tauri/Cargo.toml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (PRD § Rust workspace table + Phase A step 1)
    - .planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-RESEARCH.md (§ Pattern: Workspace Rust patch/minor bump)
    - Cargo.toml (workspace root)
    - Each crate Cargo.toml to confirm workspace inheritance
  </read_first>
  <action>
    Edit workspace `Cargo.toml` [workspace.dependencies] and each member Cargo.toml to pin EXACTLY to these PRD target versions (no caret expansion — use the target version from PRD verbatim; if `pnpm view`/`cargo search` at execution shows a newer patch within the same minor, take the newer):

    Workspace root:
    - `tokio = { version = "1.52", features = [...] }` (was 1.40)
    - `serde = { version = "1.0.228", features = ["derive"] }` (was 1)
    - `serde_json = "1.0.149"` (was 1)
    - `thiserror = "2.0.18"` (was 2)
    - `anyhow = "1.0.102"` (was 1)
    - `tracing = "0.1.44"` (was 0.1)
    - `tracing-subscriber = "0.3.23"` (was 0.3)
    - `uuid = "1.23.1"` (was 1)
    - `parking_lot = "0.12.5"` (was 0.12)

    NOTE: Per CONTEXT D-B3, ALL Tauri plugin Rust bumps (tauri*, tauri-build, tauri-plugin-*) are atomic with JS `@tauri-apps/*` and deferred to Phase B (16-02). A1 MUST NOT touch any `tauri*` crate in `apps/desktop/src-tauri/Cargo.toml` or `scripts/notarize/smoke-app/src-tauri/Cargo.toml`.

    Actual A1 bumps for src-tauri (excluding tauri*): `specta-typescript = "0.0.11"`, `tracing-appender = "0.2.5"`, `log = "0.4.29"`, `keyring = "3.6.3"`, `once_cell = "1.21.4"`, `async-trait = "0.1.89"`, `time = "0.3.47"`, `url = "2.5.8"`, `base64 = "0.22.1"`, `hex = "0.4.3"`

    crates/automation/Cargo.toml: `async-trait = "0.1.89"`, `url = "2.5.8"`, `tempfile = "3.27.0"`
    crates/capture/Cargo.toml: `async-trait = "0.1.89"`, `xcap = "0.9.4"`, `image = "0.25.10"`, `bytemuck = "1.25.0"`, `tempfile = "3.27.0"`, `core-foundation = "0.10.1"`. SKIP `nix`, `sysinfo`, `core-graphics`, `objc2*`, `windows*`, `screencapturekit` (handled in C/E phases).
    crates/effects/Cargo.toml: `indexmap = "2.14.0"`, `image = "0.25.10"`, `rayon = "1.12.0"`, `insta = "1.47.2"` (dev), `tempfile = "3.27.0"` (dev). SKIP `rusqlite`, `ts-rs` (Phase C).
    crates/encoder/Cargo.toml: `async-trait = "0.1.89"`, `tokio-util = "0.7.18"`, `bytes = "1.11.1"`, `futures = "0.3.32"`, `tempfile = "3.27.0"`, `libc = "0.2.185"`, `insta = "1.47.2"` (dev), `image = "0.25.10"` (dev). SKIP `rusqlite`, `objc2*` (Phase C).
    crates/intelligence/Cargo.toml: `eventsource-stream = "0.2.3"`, `bytes = "1.11.1"`, `tokio-stream = "0.1.18"`, `futures-util = "0.3.32"`, `async-trait = "0.1.89"`, `serde = "1.0.228"`, `serde_json = "1.0.149"`, `tower-lsp = "0.20.0"`, `dashmap = "6.1.0"`, `ropey = "1.6.1"`, `thiserror = "2.0.18"`, `anyhow = "1.0.102"`, `tracing = "0.1.44"`, `tracing-subscriber = "0.3.23"`, `regex = "1.12.3"`, `uuid = "1.23.1"`, `hex = "0.4.3"`, `symphonia = "0.5.5"`, `httpdate = "1.0.3"`, `insta = "1.47.2"` (dev), `tempfile = "3.27.0"` (dev), `wiremock = "0.6.5"` (dev). SKIP `reqwest`, `sha2`, `schemars`, `tower`, `rand`, `toml`, `serde_yaml` (Phase C).
    crates/storage/Cargo.toml: `rusqlite_migration = "2.5.0"`, `time = "0.3.47"`, `slug = "0.1.6"`. SKIP `rusqlite` (Phase C).
    crates/story-parser/Cargo.toml: `pest = "2.8.6"`, `pest_derive = "2.8.6"` (lockstep — A1 is fine since it's a minor), `strsim = "0.11.1"`, `uuid = "1.23.1"`, `insta = "1.47.2"` (dev), `proptest = "1.11.0"` (dev). SKIP `ts-rs`, `specta` (Phase C/E).
    crates/util/Cargo.toml: `hex = "0.4.3"`. SKIP `sha2` (Phase C).
    scripts/notarize/smoke-app/src-tauri/Cargo.toml: `serde = "1.0.228"`, `serde_json = "1.0.149"`. SKIP `tauri`/`tauri-build` (Phase B).

    After editing: run `cargo check --workspace --all-targets`, then `cargo nextest run --workspace`. If any failure — fix at root cause (NO `#[allow(deprecated)]`, NO version pin-back). Then commit:

    `chore(16-A1): bump workspace Rust patch/minor deps`
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets && cargo nextest run --workspace</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n 'tokio = { version = "1.52"' Cargo.toml` — returns the bumped line
    - Run `grep -n 'thiserror = "2.0.18"' Cargo.toml` — returns the bumped line
    - Run `grep -n 'pest = "2.8.6"' crates/story-parser/Cargo.toml` — returns the bumped line
    - Run `grep -En '^tauri[-_a-z]*\s*=' apps/desktop/src-tauri/Cargo.toml scripts/notarize/smoke-app/src-tauri/Cargo.toml | xargs -I{} echo "{}"` — no tauri* line should be changed in the A1 diff (verify with `git diff HEAD~1 -- apps/desktop/src-tauri/Cargo.toml scripts/notarize/smoke-app/src-tauri/Cargo.toml | grep -E '^[+-]tauri'` — returns 0 lines)
    - Run `cargo check --workspace --all-targets` — exits 0
    - Run `cargo nextest run --workspace` — exits 0
    - Run `git log --oneline -1` — shows `chore(16-A1): …` commit
    - `Cargo.lock` present in the commit diff
  </acceptance_criteria>
  <done>
    Single commit `chore(16-A1): …` landed; gate commands exit 0; Cargo.lock regenerated in-commit; no tauri* Rust crate touched.
  </done>
</task>

<task type="auto">
  <name>Task A2: Per-workspace npm patch/minor bumps (single commit)</name>
  <files>apps/desktop/package.json, apps/web/package.json, packages/ui/package.json, packages/shared-types/package.json, scripts/playwright-sidecar/package.json, scripts/notarize/smoke-app/package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/notes/deps-upgrade-plan.md (PRD § manifests 2, 3, 5, 6, 7, 8, 9)
    - apps/desktop/package.json
    - apps/web/package.json
    - packages/ui/package.json
    - packages/shared-types/package.json
    - scripts/playwright-sidecar/package.json
    - scripts/notarize/smoke-app/package.json
  </read_first>
  <action>
    Edit each manifest with the PRD target versions. SKIP Tauri `@tauri-apps/*` (Phase B), SKIP `vitest` in playwright-sidecar (Phase B B4), SKIP all majors listed in PRD (Phase D/E). Safe bumps per manifest:

    apps/desktop/package.json — runtime:
    - `@fontsource-variable/inter: ^5.2.8`, `@fontsource-variable/jetbrains-mono: ^5.2.8`
    - `@tanstack/react-query: ^5.99.2`, `@tanstack/react-query-devtools: ^5.99.2` (lockstep)
    - `cmdk: ^1.1.1` (patch within v1)
    - `motion: ^12.38.0` (minor within committed v12)
    - `react: ^19.2.5`, `react-dom: ^19.2.5`
    - `zustand: ^5.0.12` (minor within v5)
    - SKIP: `@base-ui-components/react` (defer beta→rc to user review or Phase D14 sibling), `lucide-react` (major D14), `react-hotkeys-hook` (major D14), `react-resizable-panels` (major D14), `sonner` (major D14), `tailwind-merge` (major D13)

    apps/desktop/package.json — dev:
    - `@tailwindcss/vite: ^4.2.4`, `tailwindcss: ^4.2.4`
    - `@types/react: ^19.2.14`, `@types/react-dom: ^19.2.3`
    - `@vitest/ui: ^4.1.5`, `vitest: ^4.1.5`
    - SKIP: `@types/node` (gated 22→25), `@vitejs/plugin-react` (major D19), `typescript` (major D20), `vite` (major D19), `@tauri-apps/cli` (Phase B)

    apps/web/package.json — runtime:
    - `react: ^19.2.5`, `react-dom: ^19.2.5`
    - `@tanstack/react-query: ^5.99.2` (match desktop)
    - `@auth/prisma-adapter: ^2.11.2` (patch)
    - `superjson: ^2.2.6` (patch)
    - `@aws-sdk/client-s3: ^3.1033.0`, `@aws-sdk/s3-request-presigner: ^3.1033.0` (lockstep)
    - `@maxmind/geoip2-node: ^6.3.4`
    - `tailwindcss: ^4.2.4`
    - SKIP: `next` (E24), `@prisma/client` (E25), `next-auth` (E22), `zod` (D15), `jose` (D16), `pino` (D17), `resend` (D18)

    apps/web/package.json — dev:
    - `@tailwindcss/postcss: ^4.2.4`
    - `@types/react: ^19.2.14`, `@types/react-dom: ^19.2.3`
    - `tsx: ^4.21.0`
    - SKIP: `prisma` (E25), `pino-pretty` (D17), `@types/node`, `typescript` (D20)

    packages/ui/package.json:
    - `react: ^19.2.5`, `react-dom: ^19.2.5` (peer + dev)
    - `@types/react: ^19.2.14`, `@types/react-dom: ^19.2.3`
    - `vitest: ^4.1.5`
    - SKIP: `@base-ui-components/react`, `tailwind-merge` (D13), `@vitejs/plugin-react` (D19), `typescript` (D20)

    packages/shared-types/package.json:
    - SKIP: `@tauri-apps/api` (Phase B)

    scripts/playwright-sidecar/package.json:
    - `playwright-core: ^1.59.1`
    - SKIP: `vitest` (B4)

    scripts/notarize/smoke-app/package.json:
    - SKIP: `@tauri-apps/cli` (Phase B)

    Then: `pnpm install` at repo root to regenerate `pnpm-lock.yaml`. Run the full gate. Fix any failures at root cause. Commit:

    `chore(16-A2): bump workspace npm patch/minor deps`
  </action>
  <verify>
    <automated>pnpm install && turbo run typecheck && turbo run build && pnpm --filter desktop test && turbo run test --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - Run `grep -n '"@tanstack/react-query": "\^5.99.2"' apps/desktop/package.json` — returns the bumped line
    - Run `grep -n '"@tanstack/react-query": "\^5.99.2"' apps/web/package.json` — returns the bumped line
    - Run `grep -n '"playwright-core": "\^1.59.1"' scripts/playwright-sidecar/package.json` — returns the bumped line
    - Run `grep -n '"motion": "\^12.38.0"' apps/desktop/package.json` — returns the bumped line
    - Run `pnpm install` — exits 0, `pnpm-lock.yaml` regenerated
    - Run `turbo run typecheck` — exits 0
    - Run `turbo run build` — exits 0
    - Run `pnpm --filter desktop test` — exits 0 (or matches pre-bump baseline 201/209 — no new failures)
    - Run `turbo run test --filter=web` — exits 0
    - Run `git log --oneline -1` — shows `chore(16-A2): …` commit
    - `pnpm-lock.yaml` present in the commit diff
  </acceptance_criteria>
  <done>
    Single commit `chore(16-A2): …` landed; gate exits 0; pnpm-lock.yaml regenerated.
  </done>
</task>

</tasks>

<verification>
- Both commits landed in order: A1 before A2.
- No Tauri package (JS or Rust) was touched (deferred to Phase B).
- No committed major was crossed (all bumps within the pinned major per CLAUDE.md).
- `cargo check + cargo nextest` exits 0 after A1; `turbo run typecheck + turbo run build` exits 0 after A2 (A2 owns the npm-side gate).
</verification>

<success_criteria>
- [ ] Workspace Rust safe bumps at PRD targets
- [ ] Per-workspace npm safe bumps at PRD targets
- [ ] Lockfiles regenerated
- [ ] Per-commit gate green on both commits
- [ ] No `#[allow(deprecated)]` / no `@ts-ignore` / no `--no-verify` used
</success_criteria>

<output>
After completion, create `.planning/phases/16-upgrade-all-dependencies-to-latest-bump-every-js-ts-package-/16-01-SUMMARY.md`.
</output>
