---
phase: 15-editor-post-production-feature-boundary-cleanup
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md
  - .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-REGRESSION.md
  - .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-A11Y.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - docs/ARCHITECTURE.md
autonomous: false
requirements: [D-11, D-12, D-13]
tags: [phase-15, regression, a11y, summary, docs-sync]
must_haves:
  truths:
    - "Typecheck, build, and full vitest suite pass across apps/desktop and packages/ui"
    - "Operator-completed a11y spot-check of the three new surfaces (Post-Production landing, Editor toolbar Send-to-Post-Prod button, Editor scene-list rail with parse-error chip) is recorded in SUMMARY"
    - "Phase 13 export wiring + Phase 14 sc-* chrome are verified unchanged by grep diff audit"
    - "Phase 15 SUMMARY exists; STATE.md + ROADMAP.md reflect Phase 15 complete"
    - "docs/ARCHITECTURE.md Editor/Post-Production boundary section is updated per CLAUDE.md doc-sync rule"
  artifacts:
    - path: .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md
      provides: "Phase 15 summary aggregating the 5 wave SUMMARYs"
      contains: "Phase 15"
    - path: docs/ARCHITECTURE.md
      provides: "Updated Editor/Post-Production boundary description"
      contains: "post-production"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md
      via: "Phase 15 marked [x] complete with plans list"
      pattern: "Phase 15"
---

<objective>
Wave 5 — Final regression + a11y + documentation sync. Run the full test matrix, have the operator perform an a11y spot-check on the three new surfaces, write the Phase 15 SUMMARY, and sync `docs/ARCHITECTURE.md` per CLAUDE.md "MANDATORY — Keep Agent Docs In Sync" rule.

Purpose: D-13 closure (each wave leaves app green; Wave 5 is the final regression gate). The cross-phase doc-sync item lands here — CLAUDE.md mandates docs update alongside impactful code changes, so it is non-deferrable (see 15-CONTEXT.md Amendment log 2026-04-21).

Output: Phase SUMMARY, STATE + ROADMAP updates, ARCHITECTURE.md revision, operator-recorded a11y notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-CONTEXT.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-PATTERNS.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-01-SUMMARY.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-02-SUMMARY.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-03-SUMMARY.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-04-SUMMARY.md
@CLAUDE.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression matrix + record Phase 13/14 preservation audit</name>
  <files>
    .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-REGRESSION.md
  </files>
  <read_first>
    - 15-PATTERNS.md "Behavior preservation during relocation" shared pattern
    - .planning/phases/14-port-claude-design-into-apps-desktop/14-CONTEXT.md (D-12 surface inventory)
  </read_first>
  <action>
    1. Run the full matrix and capture exit codes:
       ```bash
       cd apps/desktop && pnpm tsc --noEmit
       pnpm --filter @storycapture/desktop build
       pnpm --filter @storycapture/desktop vitest run
       pnpm --filter @storycapture/ui vitest run
       cd crates/story-parser && cargo nextest run   # no Rust changes expected; confirms unchanged
       ```

    2. Preservation grep audit — confirm Phase 13 + Phase 14 surfaces are untouched. Record in `15-REGRESSION.md`:
       ```bash
       git diff main --stat -- apps/desktop/src/App.tsx apps/desktop/src/components/title-bar.tsx apps/desktop/src/components/sidebar.tsx
       # expected: 0 changes
       git diff main --stat -- apps/desktop/src/features/post-production/export-modal.tsx apps/desktop/src/features/post-production/state/output-prefs.ts
       # expected: 0 changes
       git diff main --stat -- apps/desktop/src-tauri/src/ipc_spec.rs packages/shared-types/src/ipc.ts
       # expected: 0 changes (no new IPC per CONTEXT.md)
       ```
       Record the actual diff output in the regression file.

    3. Create `.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-REGRESSION.md` with: test matrix exit codes, grep audit output, total file count touched by Phase 15, commit list (`git log --oneline` filtered by `15-` scope).
  </action>
  <verify>
    <automated>test -f .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-REGRESSION.md && grep -q "vitest\|tsc" .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-REGRESSION.md</automated>
  </verify>
  <done>
    - All commands return 0.
    - Grep diff confirms zero changes to legacy chrome + Phase 13 export files + ipc_spec.
    - Regression file exists with full output.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator a11y spot-check on three new surfaces</name>
  <what-built>
    Wave 1-4 delivered:
    - `/post-production` landing (project picker + empty-state CTA)
    - Editor toolbar "Post-Production" button (disabled when no recording, enabled Link afterward)
    - Editor scene-list rail (always visible; parse-error fallback chip)
  </what-built>
  <how-to-verify>
    Operator runs `pnpm --filter @storycapture/desktop tauri dev` and walks the following:

    1. **Landing route a11y** — Navigate to `/post-production` via sidebar.
       - Tab order: search input → project cards in grid order → ⌘K hint.
       - Screen reader announces landing heading + empty-state copy when no projects.
       - Focus visible on every interactive element (sc-* focus ring).
       - Enter on a focused project card → navigates to `/post-production/<id>`.

    2. **Editor toolbar Send-to-Post-Prod button** — Open `/editor/<projectId>` for a project with 0 recordings.
       - Button visible, `disabled` state announced by screen reader, `aria-label` reads "Send to Post-Production (record a story first)" or equivalent.
       - Record a story → button becomes enabled; screen reader announces label change.
       - Keyboard: Enter activates; navigates to `/post-production/<id>`.

    3. **Scene-list parse-error chip** — Introduce a parse error in the DSL (e.g., delete a quote).
       - Scene list does NOT disappear.
       - Chip appears with text "parse error — showing last known".
       - Chip contrast passes WCAG 2.1 AA (warn tone on sc-surface).
       - Fix parse → chip disappears; scenes refresh.

    Record results, any issues, and screenshots (optional) in `.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-A11Y.md`.
  </how-to-verify>
  <resume-signal>Type "approved" after the three surfaces pass, or describe issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 15-SUMMARY.md + sync docs/ARCHITECTURE.md + update STATE.md + ROADMAP.md</name>
  <files>
    .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md,
    docs/ARCHITECTURE.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - All four wave SUMMARYs (15-01, 15-02, 15-03, 15-04)
    - 15-REGRESSION.md + 15-A11Y.md from Tasks 1-2
    - docs/ARCHITECTURE.md (current Editor / Post-Production sections)
    - .planning/ROADMAP.md Phase 15 entry
    - .planning/STATE.md current position block
  </read_first>
  <action>
    1. Create `15-SUMMARY.md` aggregating the four wave summaries: files touched (total count), decisions honored (D-01..D-13 checklist), Phase 13/14 preservation proof (from regression audit), test counts, commits, deferred items (if any), a11y notes.

    2. Update `docs/ARCHITECTURE.md` per CLAUDE.md "MANDATORY — Keep Agent Docs In Sync":
       - Locate the Editor + Post-Production sections.
       - Revise to describe the post-Phase-15 boundary: Editor = authoring + recording fitness (DSL + LSP + SceneListPanel + PreviewSurface mode="recording" + Send-to-Post-Prod handoff); Post-Production = shot list + timeline + voiceover (now owns VoiceoverCompact) + PreviewSurface mode="composited" + export.
       - Add a one-line pointer to `apps/desktop/src/components/preview-surface/` as the shared preview component.
       - Keep prose tight; no essay-style additions. Concise comments rule applies to docs too.

    3. Update `.planning/STATE.md`:
       - Current position block: Phase 15 — COMPLETE.
       - Accumulated decisions: append a Phase 15 highlight line summarizing boundary realignment.
       - Progress counters: bump `completed_phases` by 1, update `completed_plans` by +5, recompute percent.

    4. Update `.planning/ROADMAP.md`:
       - Phase 15 entry: Goal = "Realign Editor ('does it work?') + Post-Production ('does it feel right?') boundaries via VoiceoverCompact relocation, shared PreviewSurface, /post-production landing, Send-to-Post-Prod handoff, always-visible scene list."
       - Plans section: 5 plans, all `[x]` with brief one-liners copied from each wave's objective.
       - Progress table: add a Phase 15 row with "Code-complete (a11y verified)" + date.

    5. Commit: `docs(15-05): phase 15 summary + architecture doc sync` — NO `Co-Authored-By` trailer. CLAUDE.md also mandates doc updates land in the same task as code; acceptable here because code was landed in waves 1-4 and Wave 5 is the designated sync + summary wave per D-13.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md && grep -q "Phase 15" .planning/phases/15-editor-post-production-feature-boundary-cleanup/15-SUMMARY.md && grep -q "PreviewSurface\|preview-surface" docs/ARCHITECTURE.md && grep -q "Phase 15" .planning/STATE.md && grep -q "15-01-PLAN.md\|15-05-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - `15-SUMMARY.md` exists and references all 4 wave summaries.
    - `docs/ARCHITECTURE.md` contains a `preview-surface` reference and updated Editor/Post-Production boundary copy.
    - `.planning/STATE.md` current position reflects Phase 15 COMPLETE; progress counters updated.
    - `.planning/ROADMAP.md` Phase 15 entry has Goal filled, 5 plans listed, all `[x]`.
  </done>
</task>

</tasks>

<verification>
- All four wave plans + this plan referenced from ROADMAP.
- `git log --oneline --since="phase 15 start"` shows atomic commits per wave, none with `Co-Authored-By` trailer.
- STATE.md and ROADMAP.md reflect Phase 15 completion.
- docs/ARCHITECTURE.md synced.
</verification>

<success_criteria>
- Phase 15 SUMMARY exists, regression + a11y + doc-sync complete.
- D-13 satisfied: every wave ended green; Wave 5 is the final gate.
- No `Co-Authored-By` trailer anywhere in Phase 15 commits (CLAUDE.md mandatory rule).
- CLAUDE.md doc-sync rule honored.
</success_criteria>

<output>
Create `.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-05-SUMMARY.md` only if Wave 5 diverges from the plan; otherwise the 15-SUMMARY.md is the canonical closure artifact.
</output>
