---
phase: 15-editor-post-production-feature-boundary-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/features/post-production/voiceover-compact/voiceover-compact.tsx
  - apps/desktop/src/features/post-production/voiceover-compact/index.ts
  - apps/desktop/src/features/post-production/editor-shell.tsx
  - apps/desktop/src/routes/editor.tsx
autonomous: true
requirements: [D-10, D-11]
tags: [phase-15, refactor, post-production, voiceover, boundary-cleanup]
must_haves:
  truths:
    - "VoiceoverCompact no longer imported or referenced from routes/editor.tsx"
    - "VoiceoverCompact lives under features/post-production/voiceover-compact/ and is mounted inside post-production EditorShell"
    - "VoiceCatalogDialog moves out of routes/editor.tsx and renders only inside post-production"
    - "Typecheck, build, and existing vitest suite pass after the move"
  artifacts:
    - path: apps/desktop/src/features/post-production/voiceover-compact/voiceover-compact.tsx
      provides: "Relocated VoiceoverCompact component + helpers"
      contains: "export function VoiceoverCompact"
    - path: apps/desktop/src/features/post-production/voiceover-compact/index.ts
      provides: "Barrel export for the voiceover-compact subfolder"
      exports: ["VoiceoverCompact"]
  key_links:
    - from: apps/desktop/src/features/post-production/editor-shell.tsx
      to: apps/desktop/src/features/post-production/voiceover-compact/voiceover-compact.tsx
      via: "import { VoiceoverCompact }"
      pattern: "features/post-production/voiceover-compact"
    - from: apps/desktop/src/routes/editor.tsx
      to: "(none)"
      via: "absence of VoiceoverCompact import"
      pattern: "VoiceoverCompact"
---

<objective>
Wave 1 — Relocate `VoiceoverCompact` (currently inlined inside `routes/editor.tsx`) into its own feature folder under `features/post-production/voiceover-compact/` and mount it from the Post-Production `EditorShell`. Editor's right rail collapses to a single preview rail (tabs disappear). Purely a move: zero behavioral edits per D-11.

Purpose: Realign the Editor route with "does the video work?" and Post-Production with "does the video feel right?" (D-10). Voice UX belongs in Post-Production only.

Output: New `voiceover-compact` folder, cleaned `routes/editor.tsx`, wired `editor-shell.tsx`, green app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-CONTEXT.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract VoiceoverCompact into features/post-production/voiceover-compact/</name>
  <files>
    apps/desktop/src/features/post-production/voiceover-compact/voiceover-compact.tsx,
    apps/desktop/src/features/post-production/voiceover-compact/index.ts
  </files>
  <read_first>
    - apps/desktop/src/routes/editor.tsx (lines 1-500 — read the full imports block, helper types/functions L46-126, and VoiceoverCompact component L130-455; also line 967 for VoiceCatalogDialog usage)
    - apps/desktop/src/features/post-production/editor-shell.tsx (read fully for mount-point context)
    - 15-PATTERNS.md section "features/post-production/voiceover-compact/voiceover-compact.tsx"
  </read_first>
  <action>
    Per D-10 + D-11 (verbatim move, no behavior changes):
    1. Create directory `apps/desktop/src/features/post-production/voiceover-compact/`.
    2. Create `voiceover-compact.tsx` and copy VERBATIM from `routes/editor.tsx`:
       - Lines 46-126: helper types + functions `VoiceoverStep`, `summariseScript`, `describeTarget`, `buildSuggestedScript`, `buildVoiceoverSteps`, `findSceneIndexForOffset`.
       - Lines 130-455: the `VoiceoverCompact` component.
    3. Inside the new file, retain these imports VERBATIM (copy from editor.tsx imports block; adjust relative paths to absolute `@/` aliases, which they already are):
       ```typescript
       import { invoke } from "@tauri-apps/api/core";
       import { Mic2, Sparkles } from "lucide-react";
       import { useCallback, useEffect, useMemo, useRef, useState } from "react";
       import { TtsClipInspector } from "@/features/voiceover/TtsClipInspector";
       import { TtsScriptEditor } from "@/features/voiceover/TtsScriptEditor";
       import { useVoiceoverStore } from "@/features/voiceover/voiceoverStore";
       import type { Command, SelectorOrText, Story } from "@/ipc/parse";
       ```
    4. Add `export` to `VoiceoverCompact` (currently a local const).
    5. Create `index.ts` with: `export { VoiceoverCompact } from "./voiceover-compact";`
    6. Do NOT edit any helper function body, hook order, effect deps, or Zustand selector call — verbatim per D-11. No added/removed comments. No new code.

    Enumerate Editor-specific inline features (if any) found during extraction — record in SUMMARY for Wave 5 deferred-feature decisions. Expected: none, since VoiceoverCompact already composes only TtsScriptEditor/TtsClipInspector.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | head -40; test -f src/features/post-production/voiceover-compact/voiceover-compact.tsx && test -f src/features/post-production/voiceover-compact/index.ts && grep -q "export function VoiceoverCompact\|export const VoiceoverCompact" src/features/post-production/voiceover-compact/voiceover-compact.tsx && echo "EXTRACT_OK"</automated>
  </verify>
  <done>
    - Files exist at target paths; `VoiceoverCompact` exported.
    - `grep -n "VoiceoverCompact" apps/desktop/src/features/post-production/voiceover-compact/voiceover-compact.tsx` returns a declaration line.
    - Typecheck passes (the new file may still be unused at this point — acceptable mid-task).
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount VoiceoverCompact inside post-production EditorShell + relocate VoiceCatalogDialog</name>
  <files>
    apps/desktop/src/features/post-production/editor-shell.tsx
  </files>
  <read_first>
    - apps/desktop/src/features/post-production/editor-shell.tsx (full file; pay special attention to the inspector/voiceover-tab region noted at L199-202 in 15-PATTERNS.md)
    - apps/desktop/src/routes/editor.tsx (line 967 — VoiceCatalogDialog mount)
    - 15-PATTERNS.md key finding #5
  </read_first>
  <action>
    Per D-10 (voice UX lives only in Post-Production):
    1. Import the new `VoiceoverCompact` into `editor-shell.tsx`:
       ```typescript
       import { VoiceoverCompact } from "./voiceover-compact";
       ```
    2. Import `VoiceCatalogDialog` at the shell level (currently in editor.tsx):
       ```typescript
       import { VoiceCatalogDialog } from "@/features/voiceover/VoiceCatalogDialog";
       ```
    3. Pick minimal-intrusion mount point — the existing `Mic` icon region around L199-202 is the natural slot. Mount `<VoiceoverCompact projectId={projectId} />` there (passing the `projectId` already in scope from the route wrapper). If EditorShell takes `storyId` not `projectId`, pass whichever identifier VoiceoverCompact's props require — verbatim to what editor.tsx passed previously (read-first the call site in editor.tsx).
    4. Mount `<VoiceCatalogDialog projectId={projectId} />` at the shell root, same placement semantics as editor.tsx L967.
    5. Do NOT alter the 6-slice Zustand store, WebGPU preview lifecycle, transport controls, scene list, or export wiring. D-11 + D-12 preserve verbatim.
    6. If EditorShell lacks the required identifier prop, thread it through `routes/post-production.tsx` (thin wrapper) without changing the wrapper's public shape beyond an added prop forwarded from `useParams`.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | head -40; grep -q "VoiceoverCompact" src/features/post-production/editor-shell.tsx && grep -q "VoiceCatalogDialog" src/features/post-production/editor-shell.tsx && echo "MOUNT_OK"</automated>
  </verify>
  <done>
    - `editor-shell.tsx` imports and renders `VoiceoverCompact` + `VoiceCatalogDialog`.
    - Typecheck passes.
    - No new ipc calls; no new store slice; Phase 13 export wiring untouched (grep for `ExportModal` / `export-modal` shows unchanged imports).
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip VoiceoverCompact from routes/editor.tsx; collapse right-rail to single preview rail</name>
  <files>
    apps/desktop/src/routes/editor.tsx
  </files>
  <read_first>
    - apps/desktop/src/routes/editor.tsx (lines 1-150 imports + helpers, 455-520 rail button + state, 685-975 toolbar + rail + catalog — every removal site flagged in 15-PATTERNS.md)
    - 15-PATTERNS.md section "Call sites removed by the move"
  </read_first>
  <action>
    Per D-10 + D-11 (purely deletions and rail collapse — NO behavior edits to surviving code):
    1. Remove imports now unused by editor.tsx:
       - Drop `TtsClipInspector`, `TtsScriptEditor`, `VoiceCatalogDialog`, `useVoiceoverStore` imports (L31-34 region).
       - Drop `Mic2`, `Sparkles` from the `lucide-react` import (L10, L12 region). Keep every other icon.
    2. Delete voiceover-only helpers that moved in Task 1: lines 46-126 (`VoiceoverStep`, `summariseScript`, `describeTarget`, `buildSuggestedScript`, `buildVoiceoverSteps`, `findSceneIndexForOffset`).
    3. Delete the inline `VoiceoverCompact` component (L130-455) — it now lives in the new file.
    4. Delete `type RailTab = "preview" | "voiceover";` (L128 region).
    5. Delete `RailTabButton` (L457-495 region) — only used by the voiceover rail.
    6. Delete `const [railTab, setRailTab] = useState<RailTab>("preview");` (L505 region).
    7. Collapse the tabbed right rail (L852-952 region) into a single preview rail: keep the preview branch; drop the voiceover branch and the tab header row. The surviving rail renders `<PreviewPanel .../>` (or whatever the existing preview component mount is called in that branch) directly — no `AnimatePresence` cross-fade between tabs, no `RailTabButton` pair.
    8. Delete `<VoiceCatalogDialog projectId={projectId} />` at L967 — it now mounts in `editor-shell.tsx`.
    9. Do NOT touch: recording-view toolbar (Wave 4 adds the Send-to-Post-Prod button), scene-list panel gating (Wave 4 adjusts D-08), LSP bridge, CodeMirror extensions, Zustand selectors, Tauri channels, motion/react transitions on surviving elements.

    Right-rail filler (Claude's Discretion per CONTEXT.md): leave the rail as pure preview for now — no filler content this wave. Wave 5 revisits if needed.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | head -40; ! grep -q "VoiceoverCompact\|RailTabButton\|VoiceCatalogDialog\|useVoiceoverStore\|TtsScriptEditor\|TtsClipInspector" src/routes/editor.tsx && ! grep -q "type RailTab" src/routes/editor.tsx && pnpm --filter @storycapture/desktop vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `grep -n "VoiceoverCompact" apps/desktop/src/routes/editor.tsx` returns nothing.
    - `grep -n "RailTabButton\|VoiceCatalogDialog\|useVoiceoverStore\|TtsScriptEditor\|TtsClipInspector" apps/desktop/src/routes/editor.tsx` returns nothing.
    - Typecheck passes; `pnpm --filter @storycapture/desktop build` succeeds.
    - Existing vitest suite is green (no existing tests for inline VoiceoverCompact per 15-PATTERNS.md).
    - Single atomic commit per move: `refactor(15-01): relocate VoiceoverCompact into features/post-production` — NO `Co-Authored-By` trailer.
  </done>
</task>

</tasks>

<verification>
- Typecheck: `cd apps/desktop && pnpm tsc --noEmit` clean.
- Build: `pnpm --filter @storycapture/desktop build` clean.
- Vitest: `pnpm --filter @storycapture/desktop vitest run` all green (baseline + no regressions).
- Behavioral spot: open `/post-production/:storyId` — voiceover UI renders where editor had it before; catalog dialog opens.
- D-11 integrity grep: every Zustand selector name used by VoiceoverCompact is present in the new file identical to the original; every IPC `invoke(...)` call string matches the original.
</verification>

<success_criteria>
- `routes/editor.tsx` contains zero voiceover imports, components, helpers, or JSX.
- `features/post-production/voiceover-compact/` exists with component + barrel export, mounted from `editor-shell.tsx`.
- `VoiceCatalogDialog` renders from Post-Production only.
- App compiles, builds, and existing tests pass.
- Phase 13 export wiring (ExportModal, output-prefs store) untouched — grep confirms.
- Phase 14 sc-* chrome untouched — `App.tsx`, `title-bar.tsx`, `sidebar.tsx` unchanged.
- Commit message follows `refactor(15-01): …` convention with no AI attribution.
</success_criteria>

<output>
After completion, create `.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-01-SUMMARY.md` covering: files moved, deferred-features enumeration (expected: empty), import/export diff counts, test counts green.
</output>
