---
phase: 15-editor-post-production-feature-boundary-cleanup
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - apps/desktop/src/routes/post-production-landing.tsx
  - apps/desktop/src/routes/index.tsx
autonomous: true
requirements: [D-03, D-06, D-12]
tags: [phase-15, routing, landing-route, post-production, empty-state]
must_haves:
  truths:
    - "Visiting /post-production (no param) renders the landing route inside AppLayout"
    - "Landing shows a project picker (grid of projects with recordings available) plus an empty-state CTA when zero projects exist"
    - "Clicking a project card navigates to /post-production/:projectId"
    - "The existing /post-production/:storyId route remains unchanged and still mounts under FullscreenLayout"
    - "Sidebar Post-Production link no longer 404s — no sidebar edits needed (matchPattern already covers both routes)"
  artifacts:
    - path: apps/desktop/src/routes/post-production-landing.tsx
      provides: "Landing route — project picker + empty-state CTA"
      contains: "export default function PostProductionLandingRoute"
  key_links:
    - from: apps/desktop/src/routes/index.tsx
      to: apps/desktop/src/routes/post-production-landing.tsx
      via: "router entry { path: '/post-production', element: <PostProductionLandingRoute /> } under AppLayout"
      pattern: '"/post-production"'
    - from: apps/desktop/src/routes/post-production-landing.tsx
      to: apps/desktop/src/features/dashboard/project-grid.tsx
      via: "import { ProjectGrid } (reused)"
      pattern: "ProjectGrid"
---

<objective>
Wave 3 — Create a new landing route `/post-production` (no param). Reuses `ProjectGrid` + `useProjects` verbatim from the dashboard (differs only in the `onOpen` handler, which navigates to `/post-production/:projectId`). Mounts under `AppLayout` (sidebar chrome). The existing `/post-production/:storyId` route remains under `FullscreenLayout`, unchanged.

Purpose: Honor D-03 (Post-Production reachable at any time with empty-state) and D-06 (landing route + sidebar linkage already correct).

Output: New route file + single router entry insertion. Zero sidebar changes needed per 15-PATTERNS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-CONTEXT.md
@.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-PATTERNS.md
@CLAUDE.md

<interfaces>
From apps/desktop/src/features/dashboard/project-grid.tsx (reused verbatim):
```typescript
// Read the file before use — exact prop shape lives there. Expected shape:
export interface ProjectGridProps {
  projects: Project[];
  onOpen: (id: string) => void;
  // other optional UI props — mirror dashboard's usage
}
```

From apps/desktop/src/ipc/projects.ts:
```typescript
export function useProjects(): UseQueryResult<Project[]>;
export interface Project { id: string; name: string; /* see file */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PostProductionLandingRoute (thin fork of dashboard)</name>
  <files>
    apps/desktop/src/routes/post-production-landing.tsx
  </files>
  <read_first>
    - apps/desktop/src/routes/dashboard.tsx (lines 1-326 — imports, EmptyDashboard L43-130, toolbar L183-222, openProject L146)
    - apps/desktop/src/features/dashboard/project-grid.tsx (full — confirm exact prop surface)
    - apps/desktop/src/ipc/projects.ts (useProjects + Project type)
    - 15-PATTERNS.md section "routes/post-production-landing.tsx"
  </read_first>
  <action>
    Per D-03 + D-06 + Claude's Discretion from CONTEXT.md (reuse dashboard's ProjectGrid):

    1. Create `apps/desktop/src/routes/post-production-landing.tsx`. Use `dashboard.tsx` as the structural template — copy its import list + toolbar + grid layout shape verbatim, then modify three things only:

       a. `onOpen` handler → `const openProject = (id: string) => navigate(\`/post-production/${id}\`);` (swap `/editor/` for `/post-production/`, per 15-PATTERNS.md section on `routes/post-production-landing.tsx`).

       b. Toolbar: omit the `New Story` action from dashboard's right-cluster (landing is not a project-creation surface per 15-PATTERNS.md). Keep search input + `⌘K` hint.

       c. Empty-state copy: reword the `EmptyDashboard` copy region to match CONTEXT.md D-06 wording: headline "No recordings yet" and subline "Record a story to start post-production." Keep the `⌘K` hint and the existing sc-* styling verbatim.

    2. Do NOT add a session_count filter this wave. Per 15-PATTERNS.md section on "Recording exists filter": option (b) is the safe default — show all projects without splitting into with-recordings / without-recordings groups. Session_count N+1 avoidance stays deferred.

    3. Export as default: `export default function PostProductionLandingRoute() { ... }` — this is the router-consumed symbol.

    4. File name is kebab-case `post-production-landing.tsx` per CLAUDE.md + 15-PATTERNS.md.

    5. Inherit Phase 14 sc-* tokens, `ScButton`/`ScCard`/`ScInput` from `@storycapture/ui`, `motion/react` transitions — copy dashboard's usage verbatim. D-12 preservation.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | head -40; test -f src/routes/post-production-landing.tsx && grep -q "export default function PostProductionLandingRoute\|export default PostProductionLandingRoute" src/routes/post-production-landing.tsx && grep -q '/post-production/' src/routes/post-production-landing.tsx && grep -q "ProjectGrid\|useProjects" src/routes/post-production-landing.tsx && echo "LANDING_OK"</automated>
  </verify>
  <done>
    - File exists; default-exported component.
    - Navigates to `/post-production/:id` on project open.
    - Reuses `ProjectGrid` + `useProjects` (grep confirms imports).
    - Typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register /post-production landing in router under AppLayout</name>
  <files>
    apps/desktop/src/routes/index.tsx
  </files>
  <read_first>
    - apps/desktop/src/routes/index.tsx (full file — AppLayout children block, FullscreenLayout block, catch-all Navigate)
    - 15-PATTERNS.md section "routes/index.tsx — router entry"
  </read_first>
  <action>
    Per D-06 (landing under AppLayout; storyId workspace stays under FullscreenLayout):

    1. Import the new route at the top of `routes/index.tsx`:
       ```typescript
       import PostProductionLandingRoute from "./post-production-landing";
       ```

    2. Add a child entry under the `AppLayout` block — placed after the `/` dashboard entry and before `/settings` (project convention: static paths before param paths):
       ```typescript
       { path: "/post-production", element: <PostProductionLandingRoute /> },
       ```

    3. Do NOT modify the existing `/post-production/:storyId` entry under `FullscreenLayout` — it stays with current behavior (chrome-less workspace) per D-06.

    4. Do NOT touch: sidebar.tsx (matchPattern already covers both routes per 15-PATTERNS.md key finding #3), command-palette.tsx, AppLayout, FullscreenLayout, title-bar, or any other chrome — D-12 preservation.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | head -40; grep -q 'path: "/post-production"' src/routes/index.tsx && grep -q "PostProductionLandingRoute" src/routes/index.tsx && grep -q 'path: "/post-production/:storyId"' src/routes/index.tsx && pnpm --filter @storycapture/desktop build 2>&1 | tail -5 && pnpm --filter @storycapture/desktop vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Router has both entries: `/post-production` (landing, under AppLayout) and `/post-production/:storyId` (workspace, under FullscreenLayout).
    - Typecheck + build + vitest all green.
    - Sidebar "Post-Production" link no longer 404s — manual verification: click sidebar link → lands on project picker, not on `Navigate to="/"` catch-all.
    - Commit: `feat(15-03): add /post-production landing route` — NO `Co-Authored-By` trailer.
  </done>
</task>

</tasks>

<verification>
- `curl`-free manual: run app, click Post-Production sidebar entry → landing renders with project grid. Click a project card → navigates to `/post-production/<id>`.
- Existing post-production workspace at `/post-production/:storyId` continues to work unchanged (regression check).
- `grep -n "path: \"/post-production" apps/desktop/src/routes/index.tsx` shows exactly 2 entries.
</verification>

<success_criteria>
- D-03 satisfied: route reachable at any time with empty-state.
- D-06 satisfied: landing exists, under AppLayout, with sidebar link already wired.
- Dashboard untouched (grep diff of `dashboard.tsx` = 0 lines changed).
- Sidebar untouched (grep diff = 0 lines changed).
</success_criteria>

<output>
Create `.planning/phases/15-editor-post-production-feature-boundary-cleanup/15-03-SUMMARY.md`: router diff, new route behavior notes, session_count-split deferral note.
</output>
