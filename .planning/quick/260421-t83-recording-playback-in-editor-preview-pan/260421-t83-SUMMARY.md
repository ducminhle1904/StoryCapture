---
quick_id: 260421-t83
status: complete
completed: 2026-04-21
tasks_completed: 3/3
---

# Quick Task 260421-t83 — Recording Playback in Editor Preview

Closes Phase 15 Deferred Idea: scrubbable playback of latest recording in Editor preview rail.

## Commits

| # | Hash | Subject |
|---|------|---------|
| 1 | 5cd5c1e | feat(quick/260421-t83): add list_project_recordings IPC command |
| 2 | 4959471 | feat(quick/260421-t83): render scrubbable <video> in PreviewSurface recording mode |
| 3 | f902ec7 | feat(quick/260421-t83): live editor preview status strip driven by latest recording |

## What Shipped

- **Rust**: `list_project_recordings` Tauri command + `RecordingInfoDto` (path, captured_at, duration/width/height as Option<None> this pass). Pure `scan_exports_dir` helper filters `*.mp4` (case-insensitive), sorts newest-first by filesystem mtime. 3 unit tests (missing dir, empty dir, filter+sort). Rejects non-UUID with `InvalidArgument`, unknown id with `NotFound`.
- **IPC registration**: `projects::list_project_recordings` in `collect_commands!`; `.typ::<projects::RecordingInfoDto>()` in type block. `packages/shared-types/src/ipc.ts` regenerated by tauri-specta (debug build).
- **Frontend hook**: `useProjectRecordings(projectId)` TanStack Query hook in `apps/desktop/src/ipc/projects.ts`, plus `RecordingInfo` TS type + `KEYS.recordings(id)` query key.
- **PreviewSurface**: `RecordingPreview` now renders HTML5 `<video controls preload="metadata">` via `convertFileSrc(latest.path)` when a recording exists; falls back to the Phase-15 empty-state card verbatim otherwise. `PreviewSurfaceProps` discriminated union unchanged. `useMediaCaption` biome rule suppressed with inline rationale (user-captured screen recording — no caption track exists).
- **Editor status strip**: `EditorRoute` calls `useProjectRecordings`, derives `latest = data?.[0] ?? null`. New status strip under `PreviewSurface` shows "Latest: N min ago" + `{W × H}` when a recording exists; falls back to "Idle" / "1440 × 900" when none. Other labels (Chromium 125, SCK capture, 60 fps) preserved from plan spec.
- **formatRelative**: local helper in editor.tsx, no new dep (no dayjs/date-fns).

## Deviations from Plan

### [Rule 2 - Missing functionality] Status strip did not exist in HEAD

- **Found during**: Task 3
- **Issue**: Plan's Task 3 described editing an existing status strip block ("Idle" / "1440 × 900" / "Chromium 125" / ...) at ~line 521–545 of `apps/desktop/src/routes/editor.tsx`. That block did not exist on HEAD (`main` @ `4959471`) — HEAD's editor.tsx goes straight from the `PreviewSurface` wrapper to closing `</Panel>`. The plan was likely drafted against an uncommitted dirty working tree that has since been reset.
- **Fix**: Added the status strip as a new element directly under the `PreviewSurface` container, using the exact styling and label set the plan specifies. This realizes the plan's intent (live viewport + "Latest:" string) without reshaping the Editor layout.
- **Files modified**: `apps/desktop/src/routes/editor.tsx`
- **Commit**: `f902ec7`

### [Rule 2 - Biome a11y suppression] `useMediaCaption` on `<video>`

- **Found during**: Task 2
- **Issue**: Biome's `lint/a11y/useMediaCaption` rule flags `<video>` without a `<track>` child. The preview source is an on-device user-captured screen recording — no caption track exists and this is editor preview only, not published media.
- **Fix**: Added an inline `biome-ignore lint/a11y/useMediaCaption` comment with rationale. No rule disabled globally.
- **Files modified**: `apps/desktop/src/components/preview-surface/preview-surface.tsx`
- **Commit**: `4959471`

## Specta Generated Types Note

`tauri-specta` encodes Rust `i64` as TypeScript `bigint` (not `number`). The generated file currently shows:

```ts
export type RecordingInfoDto = {
  path: string;
  captured_at: bigint;
  duration_ms: bigint | null;
  width: number | null;
  height: number | null;
};
```

The hand-written `RecordingInfo` in `apps/desktop/src/ipc/projects.ts` uses `number` per the plan's TS type spec. This is consistent with existing project patterns (other DTOs in this file also use `number`), and the IPC boundary will coerce JSON numbers correctly at runtime for values well under `Number.MAX_SAFE_INTEGER` (unix millis + dimensions). Aligning the hand-written type with the generated `bigint` is out of scope here — follow-up if a real mismatch surfaces.

## Verification

- `cargo check` (apps/desktop/src-tauri): clean.
- `cargo test scan_ --lib`: 3 passed, 0 failed.
- `pnpm -F @storycapture/desktop exec tsc --noEmit`: clean (0 errors).
- `pnpm exec biome check` on task files: task-2 files clean; task-3 file has 3 pre-existing biome errors (a11y/useSemanticElements at line 284, organizeImports, format) that also exist on HEAD and are out of scope per the executor SCOPE BOUNDARY rule.

## Out-of-Scope Dirty Working Tree

Pre-existing uncommitted changes were present in the working tree when this task began:

- `apps/desktop/src/features/editor/codemirror-setup.ts`
- `apps/desktop/src/features/editor/story-editor.tsx`
- `crates/story-parser/src/ast.rs`
- `crates/story-parser/src/lenient_tokenize.rs`

These are unrelated to t83 and were NOT committed by this task. They remain as uncommitted modifications for the user to resolve separately.

## Self-Check: PASSED

Verified files exist:
- FOUND: apps/desktop/src-tauri/src/commands/projects.rs (list_project_recordings + RecordingInfoDto)
- FOUND: apps/desktop/src-tauri/src/ipc_spec.rs (both registrations)
- FOUND: apps/desktop/src/ipc/projects.ts (useProjectRecordings + RecordingInfo)
- FOUND: apps/desktop/src/components/preview-surface/preview-surface.tsx (convertFileSrc + <video>)
- FOUND: apps/desktop/src/routes/editor.tsx (formatRelative + latest + status strip with "Latest:")
- FOUND: packages/shared-types/src/ipc.ts (regenerated listProjectRecordings + RecordingInfoDto)

Verified commits:
- FOUND: 5cd5c1e
- FOUND: 4959471
- FOUND: f902ec7
