//! `automation` — `BrowserDriver` trait + chromiumoxide / Playwright sidecar drivers.
//!
//! Scaffold stub — implementation lives in later plans (Phase 1, AUTO-* requirements).

pub fn _scaffold_marker() {}
