//! `effects` — typed filter-graph AST for post-production (auto-zoom, cursor, transitions, sound).
//!
//! Scaffold stub — implementation lives in Phase 2 (POST-* requirements).
//! Phase 1 only establishes the crate boundary so Phase 2 doesn't retrofit.

pub fn _scaffold_marker() {}
