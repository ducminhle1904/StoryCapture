//! `encoder` — FFmpeg sidecar lifecycle + hardware-encoder feature detection.
//!
//! Scaffold stub — implementation lives in later plans (Phase 1, ENC-* requirements).

pub fn _scaffold_marker() {}
