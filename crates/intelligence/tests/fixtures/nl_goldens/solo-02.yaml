id: solo-02
bucket: solo
user_prompt: "show dashboard after signup"
expected:
  min_steps: 4
  max_steps: 8
  required_verbs: [navigate, type, click, wait_for]
  forbidden_verbs: []
  must_parse: true
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
