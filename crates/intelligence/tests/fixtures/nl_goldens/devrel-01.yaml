id: devrel-01
bucket: devrel
user_prompt: "Full onboarding walkthrough: navigate to app.example.com, sign up with name and email, verify the welcome dashboard loads, take a screenshot of the final state"
expected:
  min_steps: 5
  max_steps: 10
  required_verbs: [navigate, type, click, wait_for, screenshot]
  forbidden_verbs: []
  must_parse: true
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
