id: edge-04
bucket: edge
user_prompt: "Click the blue button on the settings page"
expected:
  min_steps: 2
  max_steps: 4
  required_verbs: [navigate, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to settings, click a button identified by visual description (ambiguous selector).
    Should: use a heuristic selector strategy (aria label, text content, or CSS class with color).
    Must not: fabricate a precise CSS selector without basis.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
