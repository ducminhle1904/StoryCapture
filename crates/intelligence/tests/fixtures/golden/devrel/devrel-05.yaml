id: devrel-05
bucket: devrel
user_prompt: "Show deploying an AWS Lambda function via the console. Navigate to the Lambda console, click Create function, select Author from scratch, name it 'demo-handler', select Python 3.12 runtime, click Create. Then paste a simple handler code in the inline editor and click Deploy."
expected:
  min_steps: 7
  max_steps: 14
  required_verbs: [navigate, click, type, select, wait_for]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to Lambda console, click create, fill name, select runtime, click create, edit code, click deploy.
    Must not: assume IAM role creation, skip runtime selection.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
