id: devrel-06
bucket: devrel
user_prompt: "Demonstrate publishing a component library in Figma. Navigate to figma.com, open the team project, select the design system file, click the Assets panel, select components to publish, click Publish library, add a description for the update, and confirm. Wait for the success notification."
expected:
  min_steps: 6
  max_steps: 12
  required_verbs: [navigate, click, type, wait_for]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to Figma, open file, access assets panel, initiate publish, add description, confirm.
    Must not: assume admin access, skip description step.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
