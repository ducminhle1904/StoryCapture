id: devrel-10
bucket: devrel
user_prompt: "Customize a Datadog dashboard. Navigate to app.datadoghq.com, go to Dashboards, click New Dashboard, name it 'API Performance', add a Timeseries widget showing avg request latency from the trace.http.request metric, add a Query Value widget for error rate, and save the dashboard."
expected:
  min_steps: 7
  max_steps: 14
  required_verbs: [navigate, click, type, select]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to Datadog, create dashboard, name it, add timeseries widget with metric, add query value widget, save.
    Must not: assume existing dashboard, skip widget configuration.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
