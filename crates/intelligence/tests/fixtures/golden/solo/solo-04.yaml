id: solo-04
bucket: solo
user_prompt: "upload avatar"
expected:
  min_steps: 3
  max_steps: 6
  required_verbs: [navigate, click, upload]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to profile/settings, click avatar area or upload button, upload file.
    Must not: assume drag-and-drop only, skip navigation to settings.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
