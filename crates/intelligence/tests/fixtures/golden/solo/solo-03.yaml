id: solo-03
bucket: solo
user_prompt: "delete a project"
expected:
  min_steps: 3
  max_steps: 6
  required_verbs: [navigate, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to projects list, click target project or menu, click delete button, confirm deletion.
    Must not: assume bulk delete, skip confirmation dialog.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
