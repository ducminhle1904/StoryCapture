id: solo-01
bucket: solo
user_prompt: "login flow demo"
expected:
  min_steps: 3
  max_steps: 6
  required_verbs: [navigate, type, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to login page, type username, type password, click submit.
    Must not: assume 2FA, captcha, multi-layer redirect.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
