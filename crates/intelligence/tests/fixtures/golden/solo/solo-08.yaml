id: solo-08
bucket: solo
user_prompt: "export data as CSV"
expected:
  min_steps: 3
  max_steps: 6
  required_verbs: [navigate, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to data/reports page, click export button, select CSV format or click download.
    Must not: assume API-based export, skip format selection.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
