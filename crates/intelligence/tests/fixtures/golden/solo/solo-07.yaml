id: solo-07
bucket: solo
user_prompt: "cancel subscription"
expected:
  min_steps: 3
  max_steps: 7
  required_verbs: [navigate, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to billing/subscription page, click cancel button, confirm cancellation.
    Must not: assume immediate deletion, skip confirmation step.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
