id: solo-06
bucket: solo
user_prompt: "change password"
expected:
  min_steps: 4
  max_steps: 7
  required_verbs: [navigate, type, click]
  forbidden_verbs: []
  must_parse: true
  step_mapping_rubric: |
    Must have: navigate to security/settings, type current password, type new password, type confirm password, click save.
    Must not: assume forgot-password flow, skip current password field.
assert:
  schema_valid: true
  verb_whitelist_compliant: true
  first_try_parse: true
