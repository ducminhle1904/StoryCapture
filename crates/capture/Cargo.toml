[package]
name = "capture"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

# Platform-gated native deps. Plan 01-07 fills these in with concrete crates
# (`screencapturekit` for macOS, `windows-capture` for Windows, `xcap` fallback).
[target.'cfg(target_os = "macos")'.dependencies]
# Placeholder — populated in Plan 01-07 (CAP-01, CAP-04).

[target.'cfg(target_os = "windows")'.dependencies]
# Placeholder — populated in Plan 01-07 (CAP-02).
