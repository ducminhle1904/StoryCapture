# Recording V3 release inputs

This directory is intentionally uncertified in source checkouts. Protected release automation may
stage only `manifest.json` and `evidence.json` here after every sustained, signing, and notarization
gate passes. Normal builds therefore remain fail-closed.
