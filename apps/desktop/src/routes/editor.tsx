// Placeholder shell — Task 2 fills in CodeMirror editor + preview + timeline.
// Kept minimal here so router has a valid target after Task 1 commits.
export default function EditorRoute() {
  return null;
}
