/**
 * ExportModal — placeholder; Task 2 of Plan 02-12b replaces this body
 * with the full format/resolution/fps/quality/folder form.
 */

export interface ExportModalProps {
  storyId: string;
}

export function ExportModal(_: ExportModalProps) {
  return null;
}
