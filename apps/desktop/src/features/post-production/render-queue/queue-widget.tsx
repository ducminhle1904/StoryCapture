/**
 * QueueWidget — placeholder; Task 2 of Plan 02-12b replaces this body
 * with the live render-queue popover + progress subscription.
 */

export interface QueueWidgetProps {
  storyId: string;
}

export function QueueWidget(_: QueueWidgetProps) {
  return null;
}
