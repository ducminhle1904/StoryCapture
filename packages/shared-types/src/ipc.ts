// Auto-generated by tauri-specta in Plan 03a (storycapture host).
// This stub matches the surface Plan 03a will produce; once 03a's `cargo build`
// runs in the worktree the file will be regenerated identically.

export type AppError =
  | { kind: "Keyring"; message: string }
  | { kind: "Io"; message: string }
  | { kind: "Internal"; message: string };

export interface AppInfo {
  version: string;
  platform: string;
  data_dir: string;
}

export interface PanicPayload {
  message: string;
  thread: string;
}

// Command signatures (informational — `invoke` is untyped at the @tauri-apps/api boundary)
export interface Commands {
  ping: () => Promise<string>;
  app_info: () => Promise<AppInfo>;
  store_secret: (args: { service: string; key: string; value: string }) => Promise<void>;
  load_secret: (args: { service: string; key: string }) => Promise<string>;
  trigger_panic: () => Promise<void>;
}

// Event channel names emitted by the host
export const APP_PANIC_EVENT = "app:panic" as const;
